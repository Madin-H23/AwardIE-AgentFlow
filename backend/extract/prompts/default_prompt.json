{
  "award": "下面是一张图片中提取的文本（已按行拼接）：\n\n{ocr_text}\n\n**第一步：请先判断这是否是一张奖状证书。**\n\n奖状证书的特征：\n- 明确授予某个具体获奖者（包含获奖者姓名）\n- 包含竞赛名称、获奖等级等信息\n- 是正式的获奖证书，而非通知、名单、模板等\n\n以下情况**不是**奖状证书，应设置 `is_valid_certificate: false`：\n- 获奖通知、获奖名单、获奖公示等通知类文档\n- 空白奖状模板、示例奖状等未填写具体获奖者信息的文档\n- 比赛规则、比赛说明、比赛通知等非证书类文档\n- 其他类型的证书（如认证证书、资质证书、职业资格证书等，如HCIE证书、CISSP证书、PMP证书等）\n- 其他不包含明确授予某个具体获奖者证书信息的文档\n\n如果不是奖状证书，则返回如下json字符串，并且结束处理（忽略后续所有任务）：\n{{\n  \"is_valid_certificate\": false\n}}\n\n只有明确授予某个具体获奖者（包含获奖者姓名）的奖状证书，才应设置 `is_valid_certificate: true`。\n\n**第二步：如果这是奖状证书，根据以下字段要求进行信息抽取。**\n\n**字段要求：**\n{fields_desc}\n- `is_valid_certificate`: 固定为true\n{translate_note}\n重要要求：\n1. 只返回一个有效的JSON对象，不要包含任何说明文字、示例代码或注释\n2. 不要返回Markdown代码块标记（如 ```json 或 ```）\n3. 不要返回任何解释性文字，只返回JSON对象本身\n4. JSON中不要包含注释（// 或 /* */）\n5. 如果某个字段在文本中不存在或无法确定，请将该字段值设为 `null`。\n\n请直接返回JSON对象：",
  "patent": "下面是一张专利证书的 OCR 文本（已按行拼接）：\n\n{ocr_text}\n\n请你只根据以上文本，抽取并返回一个 JSON 对象，用于「专利证书识别与信息抽取」，字段要求如下：\n{fields_desc}\n\n如果某个字段在文本中不存在或无法确定，请将该字段值设为 null。\n\n重要要求：\n1. 只返回一个有效的JSON对象，不要包含任何说明文字、示例代码或注释\n2. 不要返回Markdown代码块标记（如 ```json 或 ```）\n3. 不要返回任何解释性文字，只返回JSON对象本身\n4. JSON中不要包含注释（// 或 /* */）\n\n请直接返回JSON对象：",
  "software": "下面是一张软件著作权证书的 OCR 文本（已按行拼接）：\n\n{ocr_text}\n\n请你只根据以上文本，抽取并返回一个 JSON 对象，用于「软件著作权证书识别与信息抽取」，字段要求如下：\n{fields_desc}\n\n如果某个字段在文本中不存在或无法确定，请将该字段值设为 null。\n\n重要要求：\n1. 只返回一个有效的JSON对象，不要包含任何说明文字、示例代码或注释\n2. 不要返回Markdown代码块标记（如 ```json 或 ```）\n3. 不要返回任何解释性文字，只返回JSON对象本身\n4. JSON中不要包含注释（// 或 /* */）\n\n请直接返回JSON对象："
}