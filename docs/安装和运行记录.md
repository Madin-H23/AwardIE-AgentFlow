# 日常启动

## windows

```
d:
cd D:\code\venv_competition\Scripts
 .\activate
cd D:\code\教学工具\信息管理rebuild
```



```
claude  --dangerously-skip-permissions 
```



```
$env:PORT=5002; python run.py
```

## Linux

```
cd csddata
source venv/bin/activate
```

## 设置自动启动



### 代码修改

```
sudo systemctl restart info-management
```

### 代码更新+重启

```
# 1. 进入项目目录
cd /home/ubuntu/csddata

# 2. 拉取最新代码（如果用 Git）
git pull

# 3. 如有依赖变更
source venv/bin/activate
pip install -r requirements.txt

# 4. 重启服务
sudo systemctl restart info-management

# 5. 确认运行正常
sudo systemctl status info-management
tail -f logs/app.log
```

### 部署

```
mkdir -p /home/ubuntu/csddata/logs
sudo nano /etc/systemd/system/info-management.service
```

内容：

```
[Unit]
Description=CSD成果管理
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/csddata

# 保留系统 PATH，避免调用系统命令失败
Environment="PATH=/home/ubuntu/csddata/venv/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
Environment="PORT=5001"
Environment="FLASK_ENV=production"

ExecStart=/home/ubuntu/csddata/venv/bin/python run.py
Restart=always
RestartSec=3

StandardOutput=append:/home/ubuntu/csddata/logs/stdout.log
StandardError=append:/home/ubuntu/csddata/logs/stderr.log

[Install]
WantedBy=multi-user.target
```



```
# 3. 重载并启用
sudo systemctl daemon-reload
sudo systemctl enable csddata
sudo systemctl start csddata
sudo systemctl status csddata
```



# 环境安装

## 1. 虚拟环境

```
sudo apt install -y python3 python3-pip python3-venv build-essential libpq-dev
```

```
sudo apt install -y git
git clone ...
```

```
python3 -m venv venv
source venv/bin/activate
#提示符前会出现 `(venv)`，表示已激活。
pip install -r requirements-cpu.txt
```

## 2. paddleocr安装

前面已经安装好了，如果要单独安装：

```
pip install paddlepaddle==3.2.0 -i https://www.paddlepaddle.org.cn/packages/stable/cpu/
pip install python-dotenv
sudo apt install -y libgl1 libglib2.0-0
```

paddle

C:\Users\Administrator\.paddlex\official_models 拷贝到/home/ubuntu/\.paddlex/official_models

验证paddleocr

```
paddleocr ocr -i docs/TODO/省三等奖（信息助行机）.png --device cpu
```

```
pip uninstall paddlepaddle -y
```

## 安装nodejs



```
# 安装 Node.js 18.x LTS
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
# 验证安装
node -v   # 应显示 v18.x.x
npm -v
```



```
cd csddata
npm install

```

还有一个命令忘记了，