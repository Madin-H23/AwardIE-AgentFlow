> ⚠️ 已归档（2026-08-24）：本文档为历史资料，不反映项目现状。现行权威文档见 docs/README.md 路由。

# 业务流程测试用例 (第三层)

本文档包含完整的业务流程测试用例，覆盖所有分支场景。

## 测试准备

### 环境准备
```python
# 1. 备份数据库
import shutil
from datetime import datetime
backup_file = f"database/competitions_backup_{datetime.now().strftime('%Y%m%d_%H%M%S')}.db"
shutil.copy("database/competitions.db", backup_file)

# 2. 准备测试用户
# - 学生: 学号 "2021001" (姓名: 张三)
# - 教师: 工号 "T001" (姓名: 李老师)
# - 管理员: admin

# 3. 准备测试实验室
# - 实验室A (ID: 1, 包含学生张三、教师李老师)
```

### 测试工具函数
```python
# 文件位置: tests/extract/integration/test_helpers.py

class BusinessFlowTestHelper:
    """业务流程测试辅助类"""

    def __init__(self):
        self.app_context = get_app_context_instance()
        self.pending_manager = self.app_context.get_pending_achievement_manager()
        self.review_service = self._get_review_service()

    def upload_test_file(self, file_path: str, submitter_type: str, submitter_id: int):
        """上传测试文件"""
        # 实现文件上传逻辑
        pass

    def get_pending_by_id(self, pending_id: int):
        """获取pending记录"""
        return self.pending_manager.get_pending_by_id(pending_id)

    def user_delete_pending(self, pending_id: int, submitter_id: int):
        """用户删除pending"""
        return self.pending_manager.delete_pending_by_submitter(pending_id, submitter_id)

    def user_submit_pending(self, pending_id: int):
        """用户提交审核"""
        return self.pending_manager.update_status(pending_id, 'submit')

    def user_modify_and_submit(self, pending_id: int, modifications: dict):
        """用户修改并提交"""
        # 1. 获取pending
        pending = self.pending_manager.get_pending_by_id(pending_id)
        # 2. 修改数据
        data = pending.get_achievement_data()
        data.update(modifications)
        # 3. 重新验证
        # 4. 提交
        pass

    def admin_approve(self, pending_id: int, reviewer_id: int):
        """管理员审核通过"""
        from backend.services.review_service import Reviewer
        reviewer = Reviewer(reviewer_type='admin', reviewer_id=reviewer_id)
        return self.review_service.approve_single(pending_id, reviewer)

    def admin_discard(self, pending_id: int, reviewer_id: int, reason: str):
        """管理员放弃"""
        from backend.services.review_service import Reviewer
        reviewer = Reviewer(reviewer_type='admin', reviewer_id=reviewer_id)
        return self.review_service.discard_single(pending_id, reviewer, reason)
```

---

## 第一部分：用户上传流程测试

### TC-BU-01: 学生上传奖状图片
- **测试ID**: TC-BU-01
- **优先级**: 高
- **描述**: 学生上传蓝桥杯奖状图片，验证完整的上传和抽取流程

**前置条件**:
- 学生"2021001"已登录
- 测试文件: `images/测试图片/奖状/2025蓝桥杯网络安全赛道-陈鸿秋-省赛-二等奖.jpg`

**测试步骤**:
```python
def test_student_upload_award():
    # 1. 学生上传文件
    file_path = "images/测试图片/奖状/2025蓝桥杯网络安全赛道-陈鸿秋-省赛-二等奖.jpg"
    result = upload_file(file_path, submitter_type='student', submitter_id=1)

    # 2. 验证返回结果
    assert result.success == True
    assert result.pending_id is not None
    assert result.is_duplicate == False

    pending_id = result.pending_id

    # 3. 验证pending记录
    pending = get_pending_by_id(pending_id)
    assert pending.submitter_type == 'student'
    assert pending.submitter_id == 1
    assert pending.achievement_type == 'award'
    assert pending.status == 'pending'
    assert pending.file_hash is not None
    assert pending.file_path is not None

    # 4. 验证抽取结果
    data = pending.get_achievement_data()
    assert data.get('competition_name') == '蓝桥杯'
    assert data.get('award_level') == '二等奖'
    assert data.get('winner_name') is not None

    # 5. 验证OCR和LLM调用
    assert pending.ocr_text is not None
    assert pending.llm_prompt is not None
    assert pending.llm_response is not None

    # 6. 验证模板匹配
    ext_info = json.loads(pending.ext_info) if pending.ext_info else {}
    assert ext_info.get('template_id') is not None  # 应匹配到蓝桥杯模板

    # 7. 验证验证结果
    validation = pending.get_validation_result()
    assert validation.get('is_valid') == True

    # 8. 验证文件保存
    import os
    assert os.path.exists(pending.file_path)
    assert 'temp/uploads' in pending.file_path

    return pending_id
```

**验证点**:
- [ ] 文件上传成功
- [ ] pending记录创建
- [ ] status = 'pending'
- [ ] achievement_type = 'award'
- [ ] OCR识别成功
- [ ] LLM抽取成功
- [ ] 模板匹配成功 (蓝桥杯学生模板)
- [ ] 验证通过
- [ ] 文件保存到临时目录
- [ ] file_hash正确计算

---

### TC-BU-02: 重复文件上传测试
- **测试ID**: TC-BU-02
- **优先级**: 高
- **描述**: 上传相同文件两次，验证去重机制

**前置条件**:
- 已完成 TC-BU-01，pending记录存在

**测试步骤**:
```python
def test_duplicate_upload():
    # 1. 再次上传相同文件
    file_path = "images/测试图片/奖状/2025蓝桥杯网络安全赛道-陈鸿秋-省赛-二等奖.jpg"
    result = upload_file(file_path, submitter_type='student', submitter_id=1)

    # 2. 验证去重检测
    assert result.success == True
    assert result.is_duplicate == True
    assert result.pending_id is not None  # 返回已存在的pending_id

    # 3. 验证没有创建新记录
    existing_pending = get_pending_by_id(result.pending_id)
    assert existing_pending is not None

    # 4. 验证文件没有重复保存
    # 应该只有一个文件在temp/uploads目录

    return result.pending_id
```

**验证点**:
- [ ] 检测到重复文件
- [ ] 返回已存在的pending_id
- [ ] 不创建新的pending记录
- [ ] 不重复保存文件
- [ ] 返回清晰的提示信息

---

### TC-BU-03: 不同角色上传
- **测试ID**: TC-BU-03
- **优先级**: 中
- **描述**: 验证学生、教师、管理员上传的submitter_type正确

**测试步骤**:
```python
def test_different_roles_upload():
    file_path = "images/测试图片/奖状/2024支付宝小程序国家二等奖.jpg"

    # 1. 学生上传
    result1 = upload_file(file_path, submitter_type='student', submitter_id=1)
    pending1 = get_pending_by_id(result1.pending_id)
    assert pending1.submitter_type == 'student'
    assert pending1.submitter_id == 1

    # 2. 教师上传
    result2 = upload_file(file_path, submitter_type='teacher', submitter_id=1)
    pending2 = get_pending_by_id(result2.pending_id)
    assert pending2.submitter_type == 'teacher'
    assert pending2.submitter_id == 1

    # 3. 管理员上传
    result3 = upload_file(file_path, submitter_type='admin', submitter_id=1)
    pending3 = get_pending_by_id(result3.pending_id)
    assert pending3.submitter_type == 'admin'
    assert pending3.submitter_id == 1

    # 清理测试数据
    delete_pending(result1.pending_id)
    delete_pending(result2.pending_id)
    delete_pending(result3.pending_id)
```

**验证点**:
- [ ] 学生上传submitter_type='student'
- [ ] 教师上传submitter_type='teacher'
- [ ] 管理员上传submitter_type='admin'
- [ ] submitter_id正确

---

### TC-BU-04: 上传不支持的文件类型
- **测试ID**: TC-BU-04
- **优先级**: 中
- **描述**: 上传不支持的文件类型（如.doc, .txt）

**测试步骤**:
```python
def test_unsupported_file_type():
    # 创建一个测试用的.txt文件
    test_file = "temp/test.txt"
    with open(test_file, 'w') as f:
        f.write("这是一个测试文件")

    # 尝试上传
    result = upload_file(test_file, submitter_type='student', submitter_id=1)

    # 验证返回错误
    assert result.success == False
    assert '不支持的文件类型' in result.error or '不支持的文件扩展名' in result.error

    # 清理
    os.remove(test_file)
```

**验证点**:
- [ ] 返回success=False
- [ ] 错误信息清晰
- [ ] 不创建pending记录
- [ ] 不保存文件

---

## 第二部分：用户审核阶段测试

### TC-BR-01: 用户删除pending
- **测试ID**: TC-BR-01
- **优先级**: 高
- **描述**: 用户删除自己上传的pending记录

**前置条件**:
- 已存在pending记录 (status='pending')

**测试步骤**:
```python
def test_user_delete_pending():
    # 1. 上传文件创建pending
    file_path = "images/测试图片/奖状/2024数据安全-颜琪原等-省赛-三等奖.jpg"
    upload_result = upload_file(file_path, submitter_type='student', submitter_id=2)
    pending_id = upload_result.pending_id

    # 2. 记录文件路径用于后续验证
    pending = get_pending_by_id(pending_id)
    file_path = pending.file_path

    # 3. 用户删除
    delete_result = user_delete_pending(pending_id, submitter_id=2)

    # 4. 验证删除成功
    assert delete_result == True

    # 5. 验证pending记录已删除
    deleted_pending = get_pending_by_id(pending_id)
    assert deleted_pending is None

    # 6. 验证临时文件已删除
    assert os.path.exists(file_path) == False

    # 7. 验证无审核日志
    # (用户自己删除，不需要审核日志)
```

**验证点**:
- [ ] pending记录删除
- [ ] 临时文件删除
- [ ] 无审核日志记录
- [ ] 返回成功

---

### TC-BR-02: 用户直接提交 (不修改)
- **测试ID**: TC-BR-02
- **优先级**: 高
- **描述**: 用户直接点击提交，不修改任何字段

**前置条件**:
- 已存在pending记录，验证通过

**测试步骤**:
```python
def test_user_direct_submit():
    # 1. 上传文件 (验证通过)
    file_path = "images/测试图片/奖状/2025蓝桥杯网络安全赛道-陈鸿秋-省赛-二等奖.jpg"
    upload_result = upload_file(file_path, submitter_type='student', submitter_id=3)
    pending_id = upload_result.pending_id

    # 记录原始数据
    original_pending = get_pending_by_id(pending_id)
    original_data = original_pending.get_achievement_data()
    original_validation = original_pending.get_validation_result()

    # 2. 用户直接提交
    submit_result = user_submit_pending(pending_id)

    # 3. 验证状态更新
    assert submit_result == True

    # 4. 验证status变为'submit'
    submitted_pending = get_pending_by_id(pending_id)
    assert submitted_pending.status == 'submit'

    # 5. 验证数据未被修改
    submitted_data = submitted_pending.get_achievement_data()
    assert submitted_data == original_data

    # 6. 验证validation_result未被修改
    submitted_validation = submitted_pending.get_validation_result()
    assert submitted_validation == original_validation

    return pending_id
```

**验证点**:
- [ ] status更新为'submit'
- [ ] achievement_data未被修改
- [ ] validation_result未被修改
- [ ] 用户不能再修改 (前端控制)
- [ ] 进入管理员审核队列

---

### TC-BR-03: 用户修改后提交
- **测试ID**: TC-BR-03
- **优先级**: 高
- **描述**: 用户修改字段后提交，验证重新验证流程

**前置条件**:
- 已存在pending记录，可能验证失败或通过

**测试步骤**:
```python
def test_user_modify_and_submit():
    # 1. 上传一个验证可能失败的奖状
    file_path = "images/测试图片/其他/考级证书.jpg"  # 可能识别为other
    upload_result = upload_file(file_path, submitter_type='student', submitter_id=4)
    pending_id = upload_result.pending_id

    # 检查初始验证状态
    original_pending = get_pending_by_id(pending_id)
    original_validation = original_pending.get_validation_result()
    print(f"初始验证状态: {original_validation.get('is_valid')}")

    # 2. 用户修改字段
    modifications = {
        "competition_name": "蓝桥杯",  # 修改竞赛名称
        "award_level": "一等奖",       # 修改奖项等级
        "year": 2025                   # 修改年份
    }

    # 3. 修改并提交
    modify_result = user_modify_and_submit(pending_id, modifications)

    # 4. 验证修改成功
    assert modify_result == True

    # 5. 验证数据已更新
    modified_pending = get_pending_by_id(pending_id)
    modified_data = modified_pending.get_achievement_data()

    assert modified_data.get('competition_name') == "蓝桥杯"
    assert modified_data.get('award_level') == "一等奖"
    assert modified_data.get('year') == 2025

    # 6. 验证重新验证
    modified_validation = modified_pending.get_validation_result()
    # 验证结果可能变化（取决于修改是否正确）

    # 7. 验证status变为'submit'
    assert modified_pending.status == 'submit'

    return pending_id
```

**验证点**:
- [ ] achievement_data包含修改后的值
- [ ] 调用了验证器重新验证
- [ ] validation_result已更新
- [ ] status更新为'submit'
- [ ] 修改前的原始值被覆盖

---

### TC-BR-04: 验证失败→用户修正→验证通过
- **测试ID**: TC-BR-04
- **优先级**: 高
- **描述**: 构造一个验证失败的案例，用户修正后验证通过

**前置条件**:
- 已存在pending记录，is_valid=False

**测试步骤**:
```python
def test_validation_failure_then_pass():
    # 1. 上传一个可能验证失败的文件
    # 或者手动构造一个验证失败的pending记录
    file_path = "images/测试图片/奖状/2025ciscn-吴凌森等-国赛-二等奖.jpg"
    upload_result = upload_file(file_path, submitter_type='student', submitter_id=5)
    pending_id = upload_result.pending_id

    # 2. 检查初始状态
    pending = get_pending_by_id(pending_id)
    initial_validation = pending.get_validation_result()

    # 如果初始就验证通过，手动修改数据使其失败
    if initial_validation.get('is_valid', True):
        # 手动设置错误数据
        pending.update_field('achievement_data', json.dumps({
            "competition_name": "不存在的竞赛XYZ",
            "award_level": "未知等级ABC",
            "year": 1800
        }))
        # 重新验证
        pending_manager = get_pending_manager()
        pending_manager.update_from_extract_result(pending_id, extract_result, validation_result)

    # 3. 验证初始状态为失败
    pending = get_pending_by_id(pending_id)
    validation = pending.get_validation_result()
    assert validation.get('is_valid') == False
    assert len(validation.get('content_issues', [])) > 0 or len(validation.get('completeness_issues', [])) > 0

    # 记录错误信息
    initial_issues = validation.get('content_issues', []) + validation.get('completeness_issues', [])
    print(f"初始错误: {initial_issues}")

    # 4. 用户修正字段
    corrections = {
        "competition_name": "CISCN",  # 修正为正确的竞赛名称
        "award_level": "二等奖",      # 修正为正确的等级
        "year": 2025                  # 修正为正确的年份
    }

    modify_result = user_modify_and_submit(pending_id, corrections)
    assert modify_result == True

    # 5. 验证修正后通过
    corrected_pending = get_pending_by_id(pending_id)
    corrected_validation = corrected_pending.get_validation_result()

    assert corrected_validation.get('is_valid') == True
    assert len(corrected_validation.get('content_issues', [])) == 0
    assert len(corrected_validation.get('completeness_issues', [])) == 0

    # 6. 验证status为'submit'
    assert corrected_pending.status == 'submit'

    return pending_id
```

**验证点**:
- [ ] 初始is_valid=False
- [ ] 有具体的错误信息
- [ ] 修正后is_valid=True
- [ ] 错误信息清空
- [ ] 重新验证被调用
- [ ] status更新为'submit'

---

## 第三部分：管理员审核阶段测试

### TC-AR-01: 奖状审核通过
- **测试ID**: TC-AR-01
- **优先级**: 高
- **描述**: 管理员审核通过奖状，验证写入awards表

**前置条件**:
- 已存在status='submit'的pending记录 (奖状)

**测试步骤**:
```python
def test_admin_approve_award():
    # 1. 准备待审核的奖状
    file_path = "images/测试图片/奖状/2025蓝桥杯网络安全赛道-陈鸿秋-省赛-二等奖.jpg"
    upload_result = upload_file(file_path, submitter_type='student', submitter_id=6)
    pending_id = upload_result.pending_id

    # 提交审核
    user_submit_pending(pending_id)

    # 记录原始数据
    pending = get_pending_by_id(pending_id)
    original_data = pending.get_achievement_data()
    original_file_path = pending.file_path

    # 2. 管理员审核通过
    approve_result = admin_approve(pending_id, reviewer_id=1)

    # 3. 验证审核成功
    assert approve_result.success == True
    assert approve_result.action == 'approved'
    assert approve_result.target_table == 'awards'
    assert approve_result.target_id is not None

    # 4. 验证awards表新增记录
    award_manager = get_award_manager()
    award = award_manager.get_award_by_id(approve_result.target_id)
    assert award is not None

    # 5. 验证数据一致性
    # 检查关键字段是否一致
    assert award.competition_name_in_file == original_data.get('competition_name')
    assert award.award_level == original_data.get('award_level')
    assert award.winner_name == original_data.get('winner_name')

    # 6. 验证文件移动
    # 从 temp/uploads/ 移动到 files/images/awards/
    assert os.path.exists(original_file_path) == False  # 临时文件已删除
    assert 'files/images/awards' in award.image_hash     # 新路径

    # 7. 验证pending记录删除
    deleted_pending = get_pending_by_id(pending_id)
    assert deleted_pending is None

    # 8. 验证审核日志
    review_log_manager = get_review_log_manager()
    logs = review_log_manager.get_logs_by_pending_id(pending_id)
    assert len(logs) > 0
    assert logs[0].action == 'approved'

    return approve_result.target_id
```

**验证点**:
- [ ] awards表新增记录
- [ ] 数据与achievement_data一致
- [ ] 文件从temp/uploads移动到files/images/awards
- [ ] pending记录删除
- [ ] 审核日志记录
- [ ] 返回target_id正确

---

### TC-AR-02: 专利审核通过
- **测试ID**: TC-AR-02
- **优先级**: 高
- **描述**: 管理员审核通过专利，验证写入patents表

**测试步骤**:
```python
def test_admin_approve_patent():
    # 1. 上传专利文件
    file_path = "images/测试图片/专利/发明1.jpg"
    upload_result = upload_file(file_path, submitter_type='student', submitter_id=7)
    pending_id = upload_result.pending_id

    # 提交审核
    user_submit_pending(pending_id)

    # 2. 管理员审核通过
    approve_result = admin_approve(pending_id, reviewer_id=1)

    # 3. 验证
    assert approve_result.success == True
    assert approve_result.target_table == 'patents'

    # 4. 验证patents表
    patent_manager = get_patent_manager()
    patent = patent_manager.get_patent_by_id(approve_result.target_id)
    assert patent is not None

    # 5. 验证文件路径
    assert 'files/images/patents' in patent.file_path

    return approve_result.target_id
```

**验证点**:
- [ ] patents表新增记录
- [ ] 文件移动到files/images/patents
- [ ] pending记录删除

---

### TC-AR-03: 软著审核通过
- **测试ID**: TC-AR-03
- **优先级**: 高
- **描述**: 管理员审核通过软著

**验证点**:
- [ ] software_copyrights表新增记录
- [ ] 文件移动到files/images/software_copyrights

---

### TC-AR-04: Other文件审核通过 (图片→实验室图片)
- **测试ID**: TC-AR-04
- **优先级**: 高
- **描述**: Other类型的图片文件审核后写入laboratory_images表

**测试步骤**:
```python
def test_admin_approve_other_image():
    # 1. 上传图片类型other文件
    file_path = "images/测试图片/其他/照片1.jpg"
    upload_result = upload_file(file_path, submitter_type='teacher', submitter_id=1)
    pending_id = upload_result.pending_id

    # 提交审核
    user_submit_pending(pending_id)

    # 2. 管理员审核通过，指定实验室
    approve_result = admin_approve(pending_id, reviewer_id=1, lab_id=1)

    # 3. 验证写入laboratory_images表
    assert approve_result.success == True
    assert approve_result.target_table == 'laboratory_images'
    assert approve_result.laboratory_id == 1

    # 4. 验证laboratory_images表
    lab_image_manager = get_lab_image_manager()
    lab_image = lab_image_manager.get_image_by_id(approve_result.target_id)
    assert lab_image is not None
    assert lab_image.laboratory_id == 1

    # 5. 验证文件路径
    assert 'files/images/laboratory_images' in lab_image.image_path
```

**验证点**:
- [ ] laboratory_images表新增记录
- [ ] laboratory_id正确关联
- [ ] 文件移动到files/images/laboratory_images
- [ ] 实验室关联逻辑正确

---

### TC-AR-05: Other文件审核通过 (文档→实验室下载)
- **测试ID**: TC-AR-05
- **优先级**: 高
- **描述**: Other类型的PDF文档审核后写入laboratory_downloads表

**测试步骤**:
```python
def test_admin_approve_other_document():
    # 1. 上传PDF类型other文件
    file_path = "images/测试图片/其他/23陈淼-HCIE证书.pdf"
    upload_result = upload_file(file_path, submitter_type='teacher', submitter_id=1)
    pending_id = upload_result.pending_id

    # 提交审核
    user_submit_pending(pending_id)

    # 2. 管理员审核通过
    approve_result = admin_approve(pending_id, reviewer_id=1, lab_id=1)

    # 3. 验证写入laboratory_downloads表
    assert approve_result.success == True
    assert approve_result.target_table == 'laboratory_downloads'

    # 4. 验证文件路径
    assert 'files/downloads' in approve_result.file_moved_to
```

**验证点**:
- [ ] laboratory_downloads表新增记录
- [ ] 文件移动到files/downloads
- [ ] laboratory_id正确关联

---

### TC-AR-06: 管理员放弃审核
- **测试ID**: TC-AR-06
- **优先级**: 高
- **描述**: 管理员放弃审核，验证删除逻辑

**测试步骤**:
```python
def test_admin_discard():
    # 1. 准备待审核记录
    file_path = "images/测试图片/其他/考级证书1.jpg"
    upload_result = upload_file(file_path, submitter_type='student', submitter_id=8)
    pending_id = upload_result.pending_id

    # 提交审核
    user_submit_pending(pending_id)

    # 记录文件路径
    pending = get_pending_by_id(pending_id)
    file_path = pending.file_path

    # 2. 管理员放弃
    discard_result = admin_discard(pending_id, reviewer_id=1, reason="测试原因")

    # 3. 验证放弃成功
    assert discard_result.success == True
    assert discard_result.action == 'deleted'

    # 4. 验证pending记录删除
    deleted_pending = get_pending_by_id(pending_id)
    assert deleted_pending is None

    # 5. 验证文件删除
    assert os.path.exists(file_path) == False

    # 6. 验证审核日志
    review_log_manager = get_review_log_manager()
    logs = review_log_manager.get_logs_by_pending_id(pending_id)
    assert len(logs) > 0
    assert logs[0].action == 'deleted'
    assert logs[0].review_comment == "测试原因"
```

**验证点**:
- [ ] pending记录删除
- [ ] 临时文件删除
- [ ] 审核日志记录 (包含原因)
- [ ] 不写入主数据库表

---

### TC-AR-07: 批量审核通过
- **测试ID**: TC-AR-07
- **优先级**: 中
- **描述**: 管理员批量审核通过多条记录

**测试步骤**:
```python
def test_admin_batch_approve():
    # 1. 准备多条待审核记录
    pending_ids = []
    files = [
        "images/测试图片/软著/1.jpg",
        "images/测试图片/软著/2.jpg",
        "images/测试图片/专利/实用新型.jpg"
    ]

    for i, file_path in enumerate(files):
        upload_result = upload_file(file_path, submitter_type='student', submitter_id=9+i)
        pending_id = upload_result.pending_id
        user_submit_pending(pending_id)
        pending_ids.append(pending_id)

    # 2. 批量审核通过
    batch_result = admin_batch_approve(pending_ids, reviewer_id=1)

    # 3. 验证结果
    assert len(batch_result) == len(pending_ids)
    assert all(r.success for r in batch_result)

    # 4. 验证所有记录都已处理
    for pending_id in pending_ids:
        pending = get_pending_by_id(pending_id)
        assert pending is None

    # 5. 验证主表记录
    # 应该有对应数量的新记录
```

**验证点**:
- [ ] 所有记录审核成功
- [ ] 所有pending记录删除
- [ ] 主表新增对应数量的记录
- [ ] 文件正确移动

---

### TC-AR-08: 验证未通过时强制审核
- **测试ID**: TC-AR-08
- **优先级**: 中
- **描述**: 验证失败时，使用force=True强制通过

**测试步骤**:
```python
def test_admin_force_approve():
    # 1. 准备一个验证失败的pending
    # 可以通过手动构造实现
    pending_id = create_invalid_pending()

    # 验证初始状态
    pending = get_pending_by_id(pending_id)
    validation = pending.get_validation_result()
    assert validation.get('is_valid') == False

    # 2. 尝试正常审核通过 (应该失败)
    normal_result = admin_approve(pending_id, reviewer_id=1, force=False)
    assert normal_result.success == False
    assert '验证未通过' in normal_result.error

    # 3. 强制审核通过
    force_result = admin_approve(pending_id, reviewer_id=1, force=True)
    assert force_result.success == True

    # 4. 验证记录已创建
    assert force_result.target_id is not None
```

**验证点**:
- [ ] 不带force时审核失败
- [ ] 带force时审核成功
- [ ] 即使验证失败也能写入主表
- [ ] 审核日志记录强制操作

---

## 第四部分：特殊场景测试

### TC-SC-01: 主表已存在相同记录
- **测试ID**: TC-SC-01
- **优先级**: 高
- **描述**: 审核通过时发现主表已有相同file_hash的记录

**测试步骤**:
```python
def test_duplicate_in_main_table():
    # 1. 第一次上传并审核通过
    file_path = "images/测试图片/奖状/睿抗-国家一等奖-高映轩.jpg"
    upload_result1 = upload_file(file_path, submitter_type='student', submitter_id=10)
    pending_id1 = upload_result1.pending_id
    user_submit_pending(pending_id1)
    approve_result1 = admin_approve(pending_id1, reviewer_id=1)

    award_id_1 = approve_result1.target_id

    # 2. 第二次上传相同文件 (应该被去重检测到)
    upload_result2 = upload_file(file_path, submitter_type='student', submitter_id=10)
    # 上传时应该检测到重复并拒绝
    assert upload_result2.is_duplicate == True

    # 3. 或者：修改文件后上传，使其能通过去重
    # 但审核时检测到主表重复
    # (这个需要根据实际去重逻辑实现)
```

**验证点**:
- [ ] 上传时检测到重复文件
- [ ] 或审核时检测到重复记录
- [ ] 不创建重复的主表记录
- [ ] 返回清晰的提示

---

### TC-SC-02: 实验室关联逻辑验证
- **测试ID**: TC-SC-02
- **优先级**: 高
- **描述**: 验证实验室关联的优先级逻辑

**子场景2.1**: 指导教师关联
```python
def test_lab_association_by_supervisor():
    # 1. 准备包含指导教师的奖状
    # 确保"李老师"(ID:1)属于"实验室A"(ID:1)

    # 2. 上传并审核通过
    # 3. 验证laboratory_id = 1
    # 4. 验证关联原因
```

**子场景2.2**: 提交教师关联
```python
def test_lab_association_by_submitter_teacher():
    # 1. 教师上传文件 (无指导教师信息)
    # 2. 验证使用教师所在实验室
```

**子场景2.3**: 无关联
```python
def test_no_lab_association():
    # 1. 学生上传文件 (无指导教师信息)
    # 2. 验证laboratory_id = None
```

---

### TC-SC-03: 用户成果可见性验证
- **测试ID**: TC-SC-03
- **优先级**: 高
- **描述**: 验证不同用户登录后查看成果的可见性

**测试步骤**:
```python
def test_user_visibility():
    # 1. 准备测试数据
    # - 学生A (ID:1) 的奖状
    # - 教师B (ID:2) 指导的奖状
    # - 与用户C无关的奖状

    # 2. 学生A登录查看成果
    visible_to_student_a = get_student_awards(student_id=1)
    # 验证: 能看到自己的奖状

    # 3. 教师B登录查看成果
    visible_to_teacher_b = get_teacher_awards(teacher_id=2)
    # 验证: 能看到自己指导的奖状

    # 4. 用户C登录查看成果
    visible_to_user_c = get_student_awards(student_id=3)
    # 验证: 看不到A和B的奖状
```

**验证点**:
- [ ] 学生只能看到自己相关的奖状
- [ ] 教师只能看到自己指导的奖状
- [ ] 不相关数据不可见
- [ ] 实验室关联的other文件对实验室成员可见

---

## 第五部分：端到端完整流程测试

### TC-E2E-01: 学生上传奖状完整流程
- **测试ID**: TC-E2E-01
- **优先级**: 高
- **描述**: 完整的从上传到审核到查看的流程

**测试步骤**:
```python
def test_e2e_student_award():
    """完整的端到端测试：学生上传奖状"""
    # 阶段1: 学生上传
    print("=" * 60)
    print("阶段1: 学生上传文件")
    print("=" * 60)

    file_path = "images/测试图片/奖状/2025蓝桥杯网络安全赛道-陈鸿秋-省赛-二等奖.jpg"

    # 上传
    upload_result = upload_file(
        file_path=file_path,
        submitter_type='student',
        submitter_id=100  # 测试学生ID
    )

    assert upload_result.success == True
    pending_id = upload_result.pending_id

    # 验证pending记录
    pending = get_pending_by_id(pending_id)
    assert pending is not None
    assert pending.status == 'pending'
    assert pending.achievement_type == 'award'

    # 验证抽取结果
    data = pending.get_achievement_data()
    validation = pending.get_validation_result()

    print(f"竞赛名称: {data.get('competition_name')}")
    print(f"奖项等级: {data.get('award_level')}")
    print(f"验证状态: {validation.get('is_valid')}")

    # 阶段2: 学生提交审核
    print("\n" + "=" * 60)
    print("阶段2: 学生提交审核")
    print("=" * 60)

    submit_result = user_submit_pending(pending_id)
    assert submit_result == True

    pending = get_pending_by_id(pending_id)
    assert pending.status == 'submit'

    # 阶段3: 管理员审核通过
    print("\n" + "=" * 60)
    print("阶段3: 管理员审核通过")
    print("=" * 60)

    approve_result = admin_approve(pending_id, reviewer_id=1)
    assert approve_result.success == True

    award_id = approve_result.target_id

    # 阶段4: 验证数据一致性
    print("\n" + "=" * 60)
    print("阶段4: 验证数据一致性")
    print("=" * 60)

    award_manager = get_award_manager()
    award = award_manager.get_award_by_id(award_id)

    assert award.competition_name_in_file == data.get('competition_name')
    assert award.award_level == data.get('award_level')

    # 阶段5: 学生查看成果
    print("\n" + "=" * 60)
    print("阶段5: 学生查看成果")
    print("=" * 60)

    # 模拟学生登录查看
    visible_awards = get_student_visible_awards(student_id=100)

    # 验证能查看到刚才提交的奖状
    award_ids = [a.id for a in visible_awards]
    assert award_id in award_ids

    print(f"\n✓ 完整流程测试通过！")
    print(f"✓ 奖状ID: {award_id}")
    print(f"✓ 学生可以查看到该奖状")

    return award_id
```

**验证点**:
- [ ] 上传成功
- [ ] 抽取正确
- [ ] 验证通过
- [ ] 提交成功
- [ ] 审核通过
- [ ] 数据一致
- [ ] 学生可见

---

### TC-E2E-02: 教师上传专利完整流程
- **测试ID**: TC-E2E-02
- **优先级**: 中
- **描述**: 教师上传专利的完整流程

**验证点**:
- [ ] 教师上传成功
- [ ] 抽取正确
- [ ] 审核通过
- [ ] 写入patents表
- [ ] 教师可以查看

---

### TC-E2E-03: 无模板奖状处理流程
- **测试ID**: TC-E2E-03
- **优先级**: 高
- **描述**: 无匹配模板奖状的人工干预流程

**测试步骤**:
```python
def test_e2e_no_template_award():
    """无模板奖状的处理流程"""

    # 1. 上传无模板奖状
    file_path = "images/测试图片/奖状/2025ciscn-吴凌森等-国赛-二等奖.jpg"
    upload_result = upload_file(file_path, submitter_type='student', submitter_id=101)
    pending_id = upload_result.pending_id

    # 2. 检查模板匹配情况
    pending = get_pending_by_id(pending_id)
    ext_info = json.loads(pending.ext_info) if pending.ext_info else {}

    print(f"模板匹配: {ext_info.get('template_id', 'None')}")

    # 3. 如果验证失败，用户手动修正
    validation = pending.get_validation_result()
    if not validation.get('is_valid'):
        print(f"验证失败，原因: {validation.get('completeness_issues', [])}")

        # 用户修正字段
        modifications = {
            "competition_name": "CISCN全国大学生信息安全竞赛",
            "award_level": "二等奖",
            "competition_level": "国赛"
        }

        modify_and_submit(pending_id, modifications)

    # 4. 管理员审核通过
    approve_result = admin_approve(pending_id, reviewer_id=1)

    # 5. 验证最终结果
    assert approve_result.success == True
```

**验证点**:
- [ ] 无模板匹配
- [ ] LLM抽取结果可用
- [ ] 用户可以修正
- [ ] 修正后验证通过
- [ ] 审核通过
- [ ] 数据正确入库

---

## 测试数据清理

### 清理函数
```python
def cleanup_test_data():
    """清理测试数据"""

    # 1. 删除测试期间创建的pending记录
    pending_manager = get_pending_manager()
    test_pendings = pending_manager.get_pendings_by_submitter_id(test_student_ids)

    for pending in test_pendings:
        pending_manager.safe_delete_with_file(pending.id)

    # 2. 删除测试期间创建的主表记录
    # 可选：根据测试需求决定是否清理

    # 3. 清理临时文件
    import shutil
    if os.path.exists('temp/uploads'):
        for file in os.listdir('temp/uploads'):
            if file.startswith('test_'):
                os.remove(os.path.join('temp/uploads', file))
```

---

## 附录：测试执行清单

### 执行清单
```
第一部分：用户上传流程
☐ TC-BU-01: 学生上传奖状图片
☐ TC-BU-02: 重复文件上传测试
☐ TC-BU-03: 不同角色上传
☐ TC-BU-04: 上传不支持的文件类型

第二部分：用户审核阶段
☐ TC-BR-01: 用户删除pending
☐ TC-BR-02: 用户直接提交
☐ TC-BR-03: 用户修改后提交
☐ TC-BR-04: 验证失败→用户修正→验证通过

第三部分：管理员审核阶段
☐ TC-AR-01: 奖状审核通过
☐ TC-AR-02: 专利审核通过
☐ TC-AR-03: 软著审核通过
☐ TC-AR-04: Other文件审核 (图片→实验室图片)
☐ TC-AR-05: Other文件审核 (文档→实验室下载)
☐ TC-AR-06: 管理员放弃审核
☐ TC-AR-07: 批量审核通过
☐ TC-AR-08: 验证未通过时强制审核

第四部分：特殊场景
☐ TC-SC-01: 主表已存在相同记录
☐ TC-SC-02: 实验室关联逻辑验证
☐ TC-SC-03: 用户成果可见性验证

第五部分：端到端完整流程
☐ TC-E2E-01: 学生上传奖状完整流程
☐ TC-E2E-02: 教师上传专利完整流程
☐ TC-E2E-03: 无模板奖状处理流程
```
