# #35 学生端门户 对照记录

> 用户需求(2026-08-31):教师端和学生端按管理员端同样要求(逐页对照 v1,1:1,仅换 EP)改造。
> 侦察结论:v1 师生端统一继承 `user_base.html`(355 行)=**顶部固定导航门户**(区别于 admin 的 base_console 侧边栏体系);学生主色 orange。
> 决策(布局基准,AskUser 未答按基调自主判断):严格复刻 v1 门户——新建 PortalLayout,与 Console 分壳。

## 一、v1 区块清单(实现前提取,dashboard_ref.html 295 行)

1. **个人信息卡**:orange 渐变圆头像(姓名首字)+姓名+公开主页开关(v1 仅 console.log 未保存)+年级/专业/学号+实验室标签(紫)+技能标签(蓝 chips,>8 折叠)。
2. **统计卡三列**:获奖数量(green)/大创数量(orange)/技术标签(blue)。
3. **成果展示区**:标题+「导出全部」;空态(引导提交);有数据四表:获奖记录(竞赛/级别徽标 A·B·C/等级/年份)、大创(编号/[年]名称/级别/负责人/指导教师/状态)、专利(ID/名称/类型)、软著(ID/名称/登记号)。

导航(v1 user_base):仪表板/成果展示/成果提交/[教师:成果审核]/AI 助手/个人资料;fixed 顶栏+白底毛玻璃+底部边框,active 项 orange 下划线。

## 二、v2 落法与偏差声明

- **PortalLayout.vue**:顶栏 60px(fixed/毛玻璃/底部线)+导航按角色(教师版本轮无「仪表板」——teacher/dashboard_ref 对照属 Goal D)+用户名+登出;主色 `--portal-accent`(orange,亮暗成对);内容区 max-width 1280 居中(v1 max-w-7xl)。
- **路由分壳**:Console 壳=admin 12 页;Portal 壳=/portal/student/dashboard、/portal/student/achievements、/submit、/teacher/review、/profile、/chat。**旧路径重定向保持**(路径本体不变,E2E/书签兼容)。守卫:学生访问 `/` 重定向门户仪表板;学生登录直接落门户;教师暂落工作台(Goal D 补教师仪表板)。
- **StudentPortalController**:`/student/portal/summary`(users+laboratory_students+innovation_project_students 聚合)、`/student/portal/achievements`(获奖 join competitions/大创 join 关联表/专利软著按 submitter);仅 student 角色。
- **StudentAchievementsView**:四类 tab+竞赛/年份筛选+行点击详情 dialog(对照 v1 achievements_ref 852 行核心;该页的图片墙/分页细节简化,记录声明)。
- **偏差**:①公开主页开关 v1 也未接保存(源码 TODO)——v2 只读展示;②「导出全部」按钮未渲染(v1 student.export_all 通道挂账);③教师门户仪表板/成果展示/教师导出属 Goal D。

## 三、验收

StudentPortalTest:summary/achievements 仅 student 可访问+数据结构;E2E:学生登录落门户+dashboard/achievements 冒烟;截图对照。
