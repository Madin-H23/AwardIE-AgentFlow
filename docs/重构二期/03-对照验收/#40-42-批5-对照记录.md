# #40/#41/#42 批 5 对照记录

## #40 图片 OCR 批量导入

> v1 源:`app/templates/admin/file_import/upload.html`(2106 行)自动导入通道。

区块:两 tab(自动/手动)+拖拽多选上传区(.pdf/.jpg/.png/.xlsx)+进度条+结果控制台。

**v2 落法与偏差**:导入页双 tab(xlsx 导入/图片批量导入);图片 tab 多选 ≤20 个→`POST /admin/import/awards/batch` 逐张走 SubmissionService.submit 三校验/存储/sha 去重/留痕(submitterType=admin,新增重载)→结果控制台逐张(已入库/跳过/拒绝)。**偏差:OCR 字段自动抽取不落地**——proto 无裸 extract RPC(ExtractAndReview 只出审核结论),本轮为上传归档+人工补录通道(pending 标记 batch_import/ocr_extracted=false),字段抽取待 Worker 扩 extract RPC;图片打包 zip 亦不搬。

## #41 xlsx 模板化报告

> v1 源:`backend/utils/export_utils.py`(openpyxl 报告+图片 zip)。

**v2 落法**:XlsxReportService(POI):表头 royal-blue 底白字粗体+细边框+列宽 18;系年度总结(年度汇总+获奖明细双 sheet)/学生事务/教师个人三张。**偏差:图片 zip 打包不搬**(v1 报告嵌奖状图片,依赖文件存储遍历);CSV 保留为次选格式,导出页加格式切换(xlsx 默认)。

## #42 日志实时流

> v1 源:日志管理 Tab1 的"实时流"源(admin_logs.js 轮询)。

**v2 落法**:`GET /admin/logs/stream?afterId=&timeoutMillis=`(SseEmitter,2s 轮询 system_event_log 增量,anchor 锚点初始化游标,50 条/批);前端实时流 tab:SSE 追加+自动滚底(500 行环形)+暂停按钮+断线停止。**偏差:应用日志(文件 tail)为 v1 Python 侧资产不迁移**;timeoutMillis 参数供 HTTP 测试(生产默认 0=长连,前端断线自动停止)。

## 验收

AdminBatchStreamTest 4 例(批量逐张结果+sha 跳过/xlsx 三张 PK 魔数+附件头/实时流 anchor/非 admin 403);全量 74 JUnit+21 E2E 绿;截图留档。
