# 批5-审核流 03-测试

> 2026-09-24 | 全量 `mvn -B test` **820 例 0 失败** | 本批新增 9 例(ReviewFlowTest)

## 一、测试矩阵(ReviewFlowTest,9 例)

| 用例 | 覆盖点 | 结果 |
|---|---|---|
| approveMaterializesAwardAndCreatesCompetition | 通过→archived;awards 落 1 行;竞赛按名自动建且 is_auto_added=true;学生获奖关联 1 行;审计 [1,6,8] 各 1 条 | ✅ |
| approvePatentSoftwareOtherMaterialize | patent/software/other 三类各物化 1 行 | ✅ |
| approveInnovationDoesNotMaterialize | 大创通过 code 0 但四表皆 0(不物化),审计 action_type=8 留痕 | ✅ |
| rejectRequiresCommentAndMarksRejected | 空原因驳回→1003004001 且状态未变;补原因后→rejected + 审计 7;驳回不物化 | ✅ |
| reviewNonPendingRejectedByStateMachine | archived 再审→1003004000 | ✅ |
| approveIsIdempotentByBusinessFact | 重置 status 后再 approve:awards 仍 1 行、学生关联仍 1 行(查业务事实幂等) | ✅ |
| timelineVisibleToOwnerAndStaffOnly | 本人可见(≥3 条)/教师可见/另一学生 1003004002 | ✅ |
| teacherPendingListJoinsSubmitterName | 待审列表 join 提交者昵称(键名驼峰契约)+ status 过滤 | ✅ |
| aiSuggestFakeAndGrpcDegrade | fake 合法数据→pass + 免责声明;字段缺失→reject + issues 含 winner_name | ✅ |

## 二、dev 端到端验收(scripts/verify_v3_batch5.py,venv requests)

| 步骤 | 实测 | 结果 |
|---|---|---|
| 提交成果 | id=2 | ✅ |
| AI 建议 | decision=pass + "AI 建议仅辅助参考" | ✅ |
| 教师待审列表 | count=1,含 submitterName | ✅ |
| 驳回空原因拒绝 | 1003004001 | ✅ |
| 审核通过 | status=archived | ✅ |
| 物化入 awards | rows=1 | ✅ |
| 竞赛自动建 | is_auto_added=b'\x01'(true) | ✅ |
| 审计留痕 | action_types=[1,6,8] | ✅ |
| 时间线 | entries=3 | ✅ |
| 幂等(不重复物化) | 重复审批后 awards 仍 1 | ✅ |

## 三、静态门禁

| 门禁 | 结果 |
|---|---|
| p3c 增量(批5 Java) | 1 → **0 违例** |
| 凭据扫描(新增脚本) | 无明文凭据(仅 admin 演示口令) |
| OCR delegate 五步 | 0 High / 0 Critical ✅ |
| security-audit(diff 定向) | 无 High/Critical ✅ |

## 四、截图

- 批5 swagger 端点截图(待审成果 8 端点:submit/my-page/download/withdraw + review/timeline/teacher-pending-list/ai-suggest)见 02-实施 验收节。

## 五、跨批回归(批3 引用检查测试改造)

批5 建了 `awardie_awards`/`awardie_patents` 真表,与批3 `ReferenceCheckTest` 的"临时探针表 CREATE/DROP"冲突(teardown 删真表致全量 14 例炸)。按批3 spec 预案改造:
- 探针表→真表:直接向 `awardie_awards`/`awardie_patents` 插引用行,teardown 只清行不删表;
- "表不存在"路径→组件级直测(checker 传一个不存在的表名,验证跳过不抛),并配一条"真表有引用必拒"反证;
- 补真表 NOT NULL 列(patent_name/competition_name_in_file);
- 补 `awardie_awards.laboratory_id` 列(引用检查清单依赖,v2 同列)。

改造后批3 引用检查 7/7 绿,全量 820 例 0 失败。

## 六、未覆盖与说明

1. **grpc 模式**:Worker stub 属批7,本批 grpc 一律降级为人工审(测试口径);
2. **不迁数据**(与批4 一致):588 条 pending 与 30MB 文件实体留批12;
3. **四决策待复核**:物化时机/成果表最小集/AI 模式/时间线可见性,见 00-需求 第四节。
