# 05 — AI 建议(fake/grpc 双模式,不阻塞审核)

**What to build:** `AiReviewService`(模式 `ai.review.mode` 默认 fake):fake 返回确定性建议(decision=pass/issues/suggestion/BR-2 免责声明);grpc 模式调 Worker `ExtractAndReview`,**任何不可达/异常 → 降级 decision=need_manual + code 4003,不抛**;端点 `GET /{id}/ai-suggest`。gRPC stub 引入属批7,本批以"Worker 不可达→降级"为口径。

**Blocked by:** 04

**Status:** ready-for-agent

- [ ] fake 模式返回完整建议 + 免责声明
- [ ] mode=grpc 且 Worker 不可达 → 降级 need_manual(不 500/不抛)
- [ ] 端点权限点 query
