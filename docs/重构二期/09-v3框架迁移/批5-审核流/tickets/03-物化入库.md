# 03 — 物化入库(四类分发 + 竞赛自动建 + 幂等)

**What to build:** approve 时按 achievementType 分发物化:award→awards(含竞赛按名匹配、缺失自动建 is_auto_added=true、学生获奖关联)、patent/software→对应表、other→other_files、innovation→skipped(不物化);`nullable()` 空串转 NULL 防 UNIQUE 撞;**幂等查业务事实**(file_hash / 唯一键)而非审计留痕(v2 教训:164 条 archived 无物化留痕);每表检查存在再插,与 approve 同事务。

**Blocked by:** 01、02

**Status:** ready-for-agent

- [ ] award approve → awards 落 1 行 + 竞赛自动建(is_auto_added=true)+ 学生关联
- [ ] patent/software/other 各落 1 行
- [ ] innovation approve → 不物化、code 0、留痕 ref=skipped
- [ ] 重复 approve(重置 status)→ 成果表仍 1 行(幂等,查业务事实)
- [ ] UNIQUE 键空值不撞(nullable 兜底)
