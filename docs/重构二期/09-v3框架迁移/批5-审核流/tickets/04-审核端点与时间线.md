# 04 — 审核端点 + 待审列表 + 时间线

**What to build:** `PendingAchievementController` 追加:`POST /{id}/review`(action=approve/reject,权限 review)、`GET /{id}/timeline`(本人/teacher/admin,越权 1003004004)、`GET /teacher-pending-list`(join 提交者 nickname,键名契约);集成测试:状态机三路径、物化四类+竞赛自动建+innovation skipped、幂等、审计码、时间线越权。

**Blocked by:** 02、03

**Status:** ready-for-agent

- [ ] 四端点权限点齐;teacher-pending-list 键名为驼峰契约
- [ ] 审核/物化/时间线用例全绿(含越权与幂等)
