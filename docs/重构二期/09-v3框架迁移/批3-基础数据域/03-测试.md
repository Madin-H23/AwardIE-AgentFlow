# 批3-基础数据域 03-测试

> 2026-09-24 | 全量 `mvn -B test` **811 例 0 失败**(上游 474+8skip / yudao-module-business 5 / yudao-server 43+178+10skip+23,合计 811)

## 一、测试矩阵

| 用例 | 类 | 覆盖点 | 结果 |
|---|---|---|---|
| createGetPageUpdateDeleteFlow | CompetitionsControllerTest | 建→查(全字段含 isAutoAdded=false)→q 模糊分页→全字段改→删→删后 get 返 null→更新已删报 1003001000 | ✅ |
| duplicateNameRejected | 同上 | 重名 create → 1003001001 + 消息 | ✅ |
| updateToExistingNameRejected | 同上 | 改成他人名拒 / 同名改同名(排除自身)放行 | ✅ |
| pageKeywordMissReturnsEmpty | 同上 | q 未命中 → 空列表(非默认值) | ✅ |
| pageFilterByWhiteList | 同上 | whiteList=true/false 双向筛选 | ✅ |
| deleteListRemovesAll | 同上 | 批量删两条全删 | ✅ |
| deleteListRejectsOversizedBatch | 同上 | 1001 条 → 1003001003 且零删除 | ✅ |
| isAutoAddedStaysTrueOnUpdate | 同上 | 系统标记不被人工 update 覆盖 | ✅ |
| unauthenticatedRejected | 同上 | 未登录 → code 401 | ✅ |
| competitionDeleteRejectedWhenReferencedByAward | ReferenceCheckTest | 引用存在 → 1003001002,消息含表名+行数,DB 终态未删 | ✅ |
| competitionDeleteAllowedWhenReferenceLogicallyDeleted | 同上 | 引用行 deleted=1 → 放行 | ✅ |
| competitionDeleteAllowedWhenReferenceTableMissing | 同上 | 引用表不存在 → 放行(不误拒) | ✅ |
| laboratoryDeleteRejectedWhenReferencedByAward | 同上 | 实验室被 awardie_awards 引用 → 1003000002 | ✅ |
| laboratoryDeleteRejectedWhenReferencedByPatent | 同上 | 实验室被 awardie_patents 引用 → 拒绝且消息带表名 | ✅ |
| laboratoryDeleteAllowedWhenReferenceLogicallyDeleted | 同上 | deleted=1 放行 | ✅ |
| deleteListRejectedAtomicallyWhenOneReferenced | 同上 | 一条被引用 → 整体拒绝,另一条仍在(事务回滚) | ✅ |
| createGetUpdateDeleteFlow(批1) | LaboratoriesControllerTest | 批1 既有全链路不回归 | ✅ |
| pageKeywordMissReturnsEmpty(批1) | 同上 | 批1 既有 | ✅ |
| duplicateNameRejected(批3 新增) | 同上 | 重名 → 1003000001 | ✅ |
| updateToExistingNameRejected(批3 新增) | 同上 | 改他人名拒 / 同名改同名放行 | ✅ |
| unauthenticatedRejected(批1) | 同上 | 批1 既有 | ✅ |

**覆盖口径**:引用检查三路径(存在拒 / 逻辑删放行 / 表不存在放行)各有独立用例;断言全部用非默认输入(非默认表名 `awardie_awards`/`awardie_patents`、非默认 id、deleted 显式置 0/1),无"因默认值巧合通过"。

## 二、ETL 验证记录(dev 库实跑)

脚本:`scripts/etl_v2_base_data_to_v3.py`(PG 5433 awardie_dev → MySQL 3307 awardie_v3)

| 项 | 期望 | 实测 |
|---|---|---|
| competitions 行数 | 218 | 218 ✅ |
| competitions id 集合差 | 空 | 空 ✅ |
| white/watch/auto 计数 | 117/51/45 | 117/51/45 ✅ |
| laboratories 行数 | 5 | 5 ✅ |
| laboratories id 集合差 | 空 | 空 ✅ |
| AUTO_INCREMENT | > max(id)=241 | 243 ✅ |
| 二跑幂等 | 计数不变 | 218/5 不变 ✅ |
| 前置清理 | 批1 遗留 11 行 deleted=1 | 已清 ✅ |

## 三、dev 端到端验收(scripts/verify_v3_batch3.py,venv requests)

| 步骤 | 期望 | 实测 |
|---|---|---|
| 登录取 token | code 0 | ✅ |
| 竞赛分页 | total=218 | 218 ✅ |
| 实验室分页 | total=5 | 5 ✅ |
| 新建竞赛 | code 0 | ✅ |
| 重名新建 | 1003001001 | "竞赛名称已存在" ✅ |
| 临时建 awardie_awards 引用后删除 | 1003001002 | "竞赛无法删除,awardie_awards 引用 1 条" ✅ |
| 引用行 deleted=1 后删除 | code 0 | ✅ |
| 清理探针表 | — | 已 DROP ✅ |

## 四、静态门禁

| 门禁 | 结果 |
|---|---|
| p3c 增量(12 个 Java 文件) | 7 → **0 违例** |
| SQL 幂等(awardie-business/business-menus/user-domain 各两跑) | 一致 ✅ |
| 凭据扫描(新增脚本/测试) | 无明文口令 ✅ |
| OCR delegate 五步 | 0 High / 0 Critical ✅ |
| security-audit(diff 定向) | 无 High/Critical ✅ |

## 五、截图

- `swagger-competitions.png`:竞赛域 7 端点(create/update/get/page/delete/delete-list/export-excel)
- `swagger-laboratories.png`:实验室域 7 端点

## 六、未覆盖与说明

1. **ETL 未进 CI**:CI 无 PG 数据源,ETL 为本地 dev 手动实跑(与批2 同纪律),记录如上表;
2. **引用检查的临时表方案**:测试用 `awardie_awards`/`awardie_patents` 临时表模拟批6/8 的真实成果表;真实表建成后本测试须改为直接用真实表(已记入 02-实施 第六节);
3. **实验室详情聚合/下载端点**:依赖批4 的三张关联表,本批不在范围(00-需求 R2 已声明)。
