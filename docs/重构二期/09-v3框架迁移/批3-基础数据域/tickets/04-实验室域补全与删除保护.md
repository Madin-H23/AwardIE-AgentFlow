# 04 — 实验室域补全(名称唯一 + 删除引用保护)

**What to build:** LaboratoriesService 定制:①create/update 前置重名校验(LABORATORIES_NAME_DUPLICATE,排除自身 id);②delete 与 delete-list 接入 02 引用检查(逐个校验+@Transactional);③分页保留 name LIKE / id DESC。扩 `LaboratoriesControllerTest`:重名拒绝、引用存在时 delete 拒绝并带表名行数、引用行 deleted=1 放行;批1 既有 3 例不回归。确认实验室关联表(downloads/images/instructors/students/assistants)清单在批4 追加,本批注释留锚点。

**Blocked by:** 02(引用检查组件)

**Status:** ready-for-agent

- [ ] create 重名 → code LABORATORIES_NAME_DUPLICATE
- [ ] delete 被 awardie_awards 引用 → 拒绝,消息含表名+行数
- [ ] 引用行 deleted=1 → 删除放行
- [ ] 批1 既有 3 例(createGetUpdateDeleteFlow / pageKeywordMissReturnsEmpty / unauthenticatedRejected)全绿
- [ ] 实验室名称唯一校验的 update 分支排除自身(改名不误判重名)
