# 02 — 删除前引用检查骨架组件(AchievementReferenceChecker)

**What to build:** business 模块新增引用检查组件(常量白名单驱动的表清单):先查 information_schema 判表存在(不存在→跳过),存在则 `SELECT COUNT(*) WHERE <fk列>=? AND deleted=0`(无 deleted 列则退化),count>0 抛业务异常并带出表名+行数;竞赛清单(awards.competition_id、templates.competition_id)与实验室清单(awards/patents/software_copyrights/innovation_projects/other_files 的 laboratory_id);错误码 LABORATORIES_IN_USE / COMPETITIONS_IN_USE;不落任何外部可注入的表名(纯代码常量)。测试内建临时引用表实证三条路径:存在引用拒绝(deleted=0)、引用行 deleted=1 放行、引用表不存在放行。

**Blocked by:** 01(错误码段与竞赛域 service 需先存在;组件本身可并行,但接入点依赖 01/03)

**Status:** ready-for-agent

- [ ] 组件落在 `dal/mysql/reference` 或 `service/reference`,表名列名为代码内常量
- [ ] 三条路径各有集成测试实证(拒绝消息含表名+行数)
- [ ] 引用检查不吞异常、不静默降级为放行(表不存在是唯一跳过条件,且有注释说明批4-8 到位后自动生效)
- [ ] p3c 增量 0
