# 03 — 竞赛域定制(名称唯一 + 删除引用保护 + 全字段 update)

**What to build:** CompetitionsService 定制:①create 前置重名校验(错误码 COMPETITIONS_NAME_DUPLICATE,DB 唯一索引兜底);②is_auto_added 建档强制 false、RespVO 只读;③delete 与 delete-list 接入 02 的引用检查(逐个校验,任一被引用整体拒绝,@Transactional);④分页 q 模糊按 competition_name LIKE、id DESC 排序;⑤update 全字段承载 v2"只切名单"语义。新增 `CompetitionsControllerTest`(建/查/分页命中与未命中/全字段改/删成功+删后 null+更新已删报错/重名拒绝/未登录 401)。

**Blocked by:** 01(骨架)、02(引用检查组件)

**Status:** ready-for-agent

- [ ] create 重名 → code 1003001001(COMPETITIONS_NAME_DUPLICATE)
- [ ] delete 被引用 → code COMPETITIES_IN_USE,消息含 "awardie_awards" 与行数
- [ ] delete-list 混合(一条被引用)→ 整体拒绝且 DB 无变化
- [ ] update 已删记录 → code 1003001000
- [ ] 分页 q LIKE 命中/未命中两例
- [ ] 未登录 → HTTP 200 + body code 401
