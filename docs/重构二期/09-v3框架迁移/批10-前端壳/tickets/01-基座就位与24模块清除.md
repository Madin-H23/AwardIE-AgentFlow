# 01 — 基座就位与 24 模块清除

**What to build:** 把芋道前端源码同步入库(基于 `yudaocode/yudao-ui-admin-vue3@aab14fb0`,README 标注同步点与改造摘要);删除 24 个示例业务模块(views + api 双目录,只留 infra/system);清理五处悬空引用(remaining.ts 业务路由段 + 补回 export default、main.ts 的 BPM 插件、ToolHeader 的财务组件、AppLinkSelectDialog 的商城选择器改通用输入、DiyEditor 的 6 个商城营销卡片)。

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] 源码入库,README 标注同步 commit 与改造摘要
- [ ] 24 模块删除完毕,views 从 25 目录降到 8
- [ ] 全工程零悬空引用(`grep -rE "@/views/(ai|bpm|crm|...)" src/` 命中 0)
- [ ] `pnpm build:local` 绿
- [ ] node_modules/dist 不入库(上游 .gitignore 已覆盖,需确认)
