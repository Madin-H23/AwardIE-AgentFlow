# 06 — UX-1 交互移植

**What to build:** 从 v2 移植五项交互(记忆实证五批投入):`PageHeader` 页头、`TableSkeleton` 表格骨架屏、150ms 路由过渡、筛选折叠(列表筛选区可收起)、`--tag-*` 徽章色板。**作为 `src/views/business/components/` 下的自定义组件,不改芋道框架目录**(保证跟随上游时只冲突框架 5 目录)。

**Blocked by:** 03

**Status:** ready-for-agent

- [ ] 五项交互全部移植
- [ ] 放在 business 目录内,未改芋道框架目录
- [ ] build + ts:check + lint 全绿
