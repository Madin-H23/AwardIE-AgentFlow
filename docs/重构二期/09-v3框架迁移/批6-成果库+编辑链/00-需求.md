# 批6-成果库+编辑链 需求文档(00-需求)

> 2026-09-24 | 上游:00-立项.md 批6 行「awards/patents/software/innovation/other 五表+编辑端点(对照 v2 Fix-C/Fix-R/Fix-T)」
> **决策模式:用户指令「自动完成剩下批次」,本批决策按推荐案落地,标注「待用户复核」。**

## 一、背景与目标

批5 把 pending 物化到四张最简成果表(awards/patents/software_copyrights/other_files)+ 大创表(批5 建)。批6 补齐**成果库(Fix-C)**:五类已入库成果的分页列表 + 行编辑 + 行删除,并把四张最简表补到"列表/编辑链"所需字段。出口状态:admin 可在成果库按五类浏览/搜索/编辑/删除,删除有引用时拒绝;大创项目具备完整字段(批8 在其上做 status 方向1+2)。

## 二、事实调查(v2 对照)

| # | 事实 | 证据 |
|---|---|---|
| F1 | v2 成果库 `AdminVaultController`:`/vault/{type}` 五类分页(type=award/patent/software/innovation/other,keyword 按名称模糊)+ 行编辑(awards 核心四字段)+ 行删除(有引用 4009) | AdminVaultController.java |
| F2 | v2 用 `VaultSpec` 枚举固化"表名/名称列/SELECT 列"白名单,杜绝字符串拼接注入面 | VaultSpec 枚举 |
| F3 | v2 awards 列表列含 year/is_abnormal(成果库要按年筛);patents/software/other 各有专属展示列 | VaultSpec.columns() |
| F4 | v2 innovation_projects 独立全表(project_no/project_type/start_date/end_date/status/学生关联表 innovation_project_students) | V1__baseline.sql |
| F5 | v2 大创状态校准(结束日期已过的进行中→已结题)是批8 方向2,不在批6 | InnovationStatusService |
| F6 | 批5 已建四张最简表(物化所需列)+ award_student_winners;大创未物化故批6 才建 | 批5 02-实施 |

## 三、需求条目

### R1 成果库五类列表
- `/business/vault/{type}` 分页(type 五类白名单,非法 → 1003005000),keyword 按名称模糊,含 v2 的 year/laboratory 等展示列;
- 表不存在(后续批才建)→ 返回空列表不 500。

### R2 行编辑
- `POST /vault/{type}/{id}/update`,可编辑列按表白名单(非白名单列忽略);记录不存在 → 1003005001。

### R3 行删除(引用保护)
- `DELETE /vault/{type}/{id}`:awards 被 award_student_winners 引用、innovation 被 innovation_project_students 引用时拒绝(1003005002);删除走逻辑删除。

### R4 表结构补齐
- 四张最简表 ALTER 补列表/编辑列(awards 的 year/is_abnormal 等;other 的 file_type/file_size;patents 的公开号/申请日期;software 的证书号/登记日期);
- 新建大创两表(awardie_innovation_projects + awardie_innovation_project_students)。

### R5 权限
- 成果库是 admin 主责域:`business:vault:query/update/delete` 授 awardie_admin(100)+ super_admin(1);教师/学生不授编辑删除(防越权改他人成果)。

## 四、决策(按推荐案落地,待用户复核)

- **D1 成果库按 v2 五类视图(而非 pending 聚合)**:v3 已有真实成果表,成果库直接查表(比 v2 的 pending JSON 过滤更自然);pending 聚合视图(teacher/admin 待审池)已在批4/5 覆盖——待复核;
- **D2 大创本批只建表不做物化**:物化仍归批8 Excel 通道;本批建表供编辑链——待复核;
- **D3 编辑列白名单制**:非白名单列的更新被忽略(不报错)——待复核。

## 五、总验收
1. 五类列表各可查、keyword 命中/未命中、非法 type 拒;
2. 行编辑白名单列生效、非白名单列不动、记录不存在报错;
3. 删除:无引用可删;awards/innovation 有引用时 1003005002 拒;
4. 学生无编辑删除权限(403);
5. 全量测试 0 失败;p3c 0;三道闸 0 High/Critical;CI 绿。
