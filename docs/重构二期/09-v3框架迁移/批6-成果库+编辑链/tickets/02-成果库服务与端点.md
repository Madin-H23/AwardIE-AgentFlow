# 02 — 成果库服务与端点

**What to build:** `AchievementVaultService`+`AchievementVaultController`:`GET /{type}` 分页(keyword 名称模糊,表不存在返空)、`POST /{type}/{id}/update`(可编辑列白名单)、`DELETE /{type}/{id}`(引用拒绝);VaultSpec 枚举固化表名/名称列/列表列(同 v2,防注入);错误码 1_003_005_000 段;菜单 3004/3041-3043 权限授 100/101。

**Blocked by:** 01

**Status:** ready-for-agent

- [ ] 五类列表 + keyword 双向命中
- [ ] 非法 type → 1003005000;表不存在返空不 500
- [ ] 编辑白名单列生效、非白名单忽略、不存在 1003005001
- [ ] 删除:无引用可删;awards/innovation 引用拒 1003005002
- [ ] p3c 0
