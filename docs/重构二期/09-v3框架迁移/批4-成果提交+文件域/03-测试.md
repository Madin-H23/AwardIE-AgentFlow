# 批4-成果提交+文件域 03-测试

> 2026-09-24 | 全量 `mvn -B test` **811 例 0 失败** | 本批新增 40 例(22 单测 + 18 集成)

## 一、测试矩阵

### 文件域单测(AwardieFileStorageTest,8 例)

| 用例 | 覆盖点 | 结果 |
|---|---|---|
| storeWritesFileWithHashName | 落盘 + sha256 64 位 + 相对路径 + 回读一致 | ✅ |
| storeSameContentTwiceHitsSamePath | 同内容同扩展名 → 同路径(去重) | ✅ |
| assertAllowedRejectsNonWhitelistExtension | `.txt` 拒(消息含白名单) | ✅ |
| assertAllowedRejectsOversize | 10MB+1 拒(消息含 10MB) | ✅ |
| assertAllowedRejectsMagicMismatch | 扩展名 pdf 内容文本 → 魔术字节拒 | ✅ |
| assertAllowedAcceptsRealMagicBytes | jpg/png/pdf 真实魔术字节均放行 | ✅ |
| resolveRejectsDirectoryTraversal | `../../etc/passwd` 拒 | ✅ |
| contentTypeOfMapsExtensions | 四扩展名 + 兜底 octet-stream | ✅ |

夹具用真实魔术字节字面量构造,不用默认假文件。

### 校验器单测(SubmissionValidatorTest,14 例)

| 用例 | 覆盖点 | 结果 |
|---|---|---|
| awardValid | 合法 award 四字段齐 → isValid | ✅ |
| awardMissingRequiredFields | 缺 3 个必填 → 3 条 completeness(逐条含字段名) | ✅ |
| awardDateAcceptsFourFormats | `2024-05-01`/`2024-05`/`2024-5`/`2024/05/01` 四格式各通过 | ✅ |
| awardDateRejectsOutOfRangeAndGarbage | 1999/2101/非日期/非法月日 → content_issues | ✅ |
| patentValid / patentRejectsBadApplicationNumberAndType / patentApplicationNumberTooShort | CN 前缀 + 长度≥5 + 类型白名单三条规则 | ✅ |
| softwareValid | `2023SR12345`(20 开头 11 位)通过 | ✅ |
| softwareRejectsBadRegistrationNumber | 非 20 开头拒 + **12 位(v2 文案示例值)也拒**(规则等价性) | ✅ |
| innovationAndOtherRequiredFields | 两类必填 + 合法值 | ✅ |
| unknownTypeRejected | 未知类型 → 1003002005 | ✅ |
| malformedJsonRejected | 非 JSON → IllegalArgumentException | ✅ |
| blankDataTreatedAsEmptyObject | null/空白 → 4 条 completeness | ✅ |
| validationJsonShapeMatchesV2 | validation_result 含三个键 | ✅ |

### 提交侧集成测试(PendingSubmissionControllerTest,12 例)

| 用例 | 覆盖点 | 结果 |
|---|---|---|
| submitFiveAchievementTypes | 五类各提交一条 + status/version/hash 落库 + 库内 5 行 | ✅ |
| missingFieldDoesNotBlockSubmission | 缺 winner_name → code 0 且 is_valid=false(字段问题不阻断,沿 v2) | ✅ |
| duplicatePendingFileRejected | 同 hash 重复 → 1003002001 且库内仍 1 行 | ✅ |
| teacherSubmissionUsesTeacherType | 教师提交 → submitter_type=teacher(服务端推导) | ✅ |
| unknownTypeRejected | 未知类型 → 1003002005 | ✅ |
| withdrawByOwnerSucceeds | 本人 pending 撤回成功且查不到 | ✅ |
| withdrawByOtherStudentForbidden | 他人撤回 → 1003002004 且行仍在 | ✅ |
| withdrawNonPendingRejected | archived 状态撤回 → 1003002003 | ✅ |
| downloadByOwnerAndTeacher | 本人与教师均可下载 + attachment + image/jpeg + 字节一致 | ✅ |
| downloadByOtherStudentForbidden | 另一学生下载 → 1003002004 | ✅ |
| myPageIsolatesBySubmitter | 两个学生各提交一条,各自只看到自己 1 条 | ✅ |
| unauthenticatedRejected | 未登录 → 401 | ✅ |

### 实验室资产集成测试(LaboratoryAssetsTest,6 例)

| 用例 | 覆盖点 | 结果 |
|---|---|---|
| detailAggregatesInstructorsStudentsAndDownloadCount | 详情聚合 + downloadCount=1 + awardCount=0(批6 前)+ instructors/students 数组 | ✅ |
| detailOfMissingLabReportsNotExists | 不存在 → 1003000000 | ✅ |
| downloadsOrderedByDisplayOrderThenIdDesc | display_order 升序断言 | ✅ |
| labDeleteRejectedWhenAssociationTablesHaveRows | **四张关联表逐一**建行 → 删实验室均 1003000002 + 消息含表名 + 行仍在 | ✅ |
| labDeleteAllowedWhenAssociationRowsLogicallyDeleted | downloads 有 deleted 列,deleted=1 放行 | ✅ |
| labDeleteAllowedWhenOnlyUndeletedRowsInNoDeletedTable | 无关联放行(对照 instructors/students 无 deleted 列的退化分支) | ✅ |

## 二、dev 端到端验收(scripts/verify_v3_batch4.py,venv requests)

| 步骤 | 实测 | 结果 |
|---|---|---|
| 建实验室 | id=12 | ✅ |
| 提交成果 | id=1,validation `is_valid=true` | ✅ |
| 重复文件去重拒绝 | 1003002001"该文件已在待审列表中(内容重复)" | ✅ |
| 扩展名白名单拒绝 | 1003003000 | ✅ |
| 魔术字节校验拒绝 | 1003003002 | ✅ |
| 我的提交 | total=1 | ✅ |
| 下载文件 | `image/jpeg;charset=UTF-8` 8 字节 + attachment + 字节与上传一致 | ✅ |
| 撤回提交 | code 0 | ✅ |
| 重复撤回 | 1003002000 待审成果不存在 | ✅ |
| 实验室详情聚合 | awardCount=0(批6 前恒 0) | ✅ |
| 实验室下载列表 | count=0 | ✅ |

## 三、静态门禁

| 门禁 | 结果 |
|---|---|
| p3c 增量(批4 Java 文件) | 11 → **0 违例** |
| SQL 幂等(三脚本各两跑) | 一致 ✅ |
| 凭据扫描(新增脚本) | 无明文口令 ✅ |
| OCR delegate 五步 | 0 High / 0 Critical ✅ |
| security-audit(diff 定向) | 无 High/Critical ✅ |

## 四、截图

- `swagger-pending-submission.png`:待审成果域 4 端点(submit / my-page / download / withdraw)

## 五、未覆盖与说明

1. **不迁数据(决策 D4)**:588 条 pending 与 30MB 文件实体留批12;dev 库验收数据为测试自造,验收后已撤回;
2. **孤儿文件**:提交在事务内先落盘后入库,若入库失败会留下无引用文件(与 v2 同构)。不影响正确性,补偿方案见 02-实施 第六节挂账;
3. **登记号规则**:保持 v2 规则等价,12 位值按 v2 规则判为不合规,待用户裁定是否放宽。
