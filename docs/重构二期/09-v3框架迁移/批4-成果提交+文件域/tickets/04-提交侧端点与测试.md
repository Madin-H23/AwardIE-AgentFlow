# 04 — 提交侧四端点 + 集成测试

**What to build:** `PendingAchievementController` 四端点:POST `/submit`(multipart file+achievementType+data,submitterType 从登录用户角色推导不信任前端)、GET `/my-page`(分页,service 层按当前用户 submitter_id 过滤)、DELETE `/withdraw`(仅本人 + status=pending,否则 1003002003/1003002004)、GET `/download`(本人或 teacher/admin,Content-Disposition attachment + contentTypeOf)。新增 `PendingSubmissionControllerTest`:五类成果各提交一条(用真实魔术字节构造夹具,不用默认假文件)、字段校验结果断言、重名 hash 拒、教师提交 submitter_type=teacher、撤回三态、下载 attachment 与越权 4030、未登录 401。测试文件写 target/test-files 并在 @AfterEach 清理。

**Blocked by:** 03(提交 service)

**Status:** ready-for-agent

- [ ] 五类提交各落一行(status=pending、version=1、file_hash 64 位)
- [ ] 缺 winner_name 提交 → code 0 但 is_valid=false + issues 含"winner_name"(字段问题不阻断提交,沿 v2)
- [ ] 同 hash 重复提交 → 1003002001
- [ ] 教师账号提交 → DB submitter_type=teacher
- [ ] 撤回:本人 pending → 成功且再查为 null;本人非 pending → 1003002003;他人 → 1003002004
- [ ] 下载:本人 → attachment + 正确 contentType + 字节一致;另一学生 → 1003002004
- [ ] 未登录 → HTTP 200 + body code 401
- [ ] my-page 只返回本人记录(自播种第二个学生账号验证隔离)
