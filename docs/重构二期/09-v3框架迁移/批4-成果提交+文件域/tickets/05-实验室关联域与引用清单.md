# 05 — 实验室关联四表 + 详情/下载端点 + 引用清单衔接

**What to build:** 建 `awardie_laboratory_downloads` / `awardie_laboratory_images` / `awardie_laboratory_instructors` / `awardie_laboratory_students` 四表(对照 v2 结构,全带 tenant_id 与审计/逻辑删除列,关联表联合主键);实验室详情聚合端点(信息+教师+学生+下载数+成果数,awardCount 依赖批6 的 awards 表→本批返回 0 并注释标明)与下载列表端点(display_order,id DESC);四表追加进 `AchievementReferenceChecker.LABORATORY_REFERENCES`(批3 遗留衔接项)。测试:详情聚合与下载列表可用 + 关联表有行时删实验室被拒。

**Blocked by:** 02(菜单/错误码段)

**Status:** ready-for-agent

- [ ] 四表建表成功(tenant_id 齐全,否则芋道租户拦截器会跳过追加)
- [ ] 详情端点返回教师/学生/下载数;awardCount 当前为 0 且代码注释标明批6 补
- [ ] 下载列表按 display_order, id DESC 排序
- [ ] downloads/images/instructors/students 各自有引用行时删实验室 → 1003000002
- [ ] 引用行 deleted=1 时放行(批3 已覆盖 deleted 语义,此处四表无 deleted 列→走退化分支,需实证)
