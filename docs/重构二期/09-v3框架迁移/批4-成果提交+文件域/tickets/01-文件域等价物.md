# 01 — 文件域等价物 AwardieFileStorage

**What to build:** business 模块新增 `service/file/AwardieFileStorage`:目录存储(`awardie.file.root` 配置,默认 files/v3,测试注入 target/test-files)+ SHA-256 去重(文件名 hash 前 16 位+原扩展名)+ 三校验(扩展名白名单 jpg/jpeg/png/pdf → 10MB 上限 → 魔术字节,顺序沿 v2)+ `resolve()` 路径越界防护 + `readAll` + `contentTypeOf`;错误码 1_003_003_000 起(FILE_TYPE_NOT_ALLOWED / FILE_TOO_LARGE / FILE_CONTENT_MISMATCH / FILE_PATH_ILLEGAL);新增 `AwardieFileStorageTest` 纯单测(不依赖 Spring 上下文,用 target/test-files 临时根)。芋道 infra 文件模块保留不动。

**Blocked by:** 无

**Status:** ready-for-agent

- [ ] 三校验各一拒(`.txt` 扩展名 / 11MB / 伪 pdf 内容),错误码与中文消息正确
- [ ] 合法文件落盘且 sha256 与内容一致;同内容再存一次命中同文件名(去重)
- [ ] `resolve("../x")` 拒绝;`contentTypeOf` 四扩展名映射正确
- [ ] 存储根可配置;测试写 target/test-files 不污染开发目录
- [ ] p3c 增量 0
