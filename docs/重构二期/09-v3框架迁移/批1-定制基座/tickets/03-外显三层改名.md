# 03 — 外显三层改名(坐标/品牌/库名)

**What to build:** Maven groupId `cn.iocoder.boot`→`com.awardie`(根 pom、yudao-dependencies、各启用 module 的 pom,一个原子提交);Spring `spring.application.name` 与 server 模块品牌标识改为 AwardIE v3;数据库 `ruoyi-vue-pro`→`awardie_v3`(建新库、重灌脱敏官方 sql+cleanup、更新 yaml 连接串)。**内部包名 cn.iocoder.yudoo 不动**(ADR-0003)。

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] `mvn -q compile`(awardie-v3)通过;启动日志应用名为 awardie 系
- [ ] 连 awardie_v3 库启动 48080 正常,swagger 200、admin 登录正常(清理后口令)
- [ ] git diff 集中在外显文件(pom/yaml/README 品牌行),无 src 目录结构变动
- [ ] README 标注 AwardIE v3 fork+上游基线 commit+MIT 保留
- [ ] 提交信息 `feat(v3):` 起头,与上游基线提交可区分
