# 05 — v3 CI 工作流(分支+路径双过滤)

**What to build:** 新增 `.github/workflows/ci-v3.yml`:触发=push/PR 到 `refactor/v3-yudao-migration` 且 paths=`awardie-v3/**`;job 链路=MySQL 8 service + Redis service + 建 `awardie_v3_test` 库 + 灌脱敏官方 sql + cleanup + `mvn -B test`(工作目录 awardie-v3)。`ci-v2.yml` 加 branches 过滤排除 v3 分支(反向不空跑)。前端门禁待批10 submodule 初始化后接入,本期只搭后端链。

**Blocked by:** 01(CI 跑的是环境变量口令链路)、02(初始化含 cleanup)、04(有测试可跑,首跑非空转)

**Status:** ready-for-agent

- [ ] 推一个触发 commit 到 v3 分支,ci-v3 自动触发且全绿(后端测试真跑,非 skip)
- [ ] 推 v2 分支(refactor/v2-stack-migration)不触发 ci-v3;推 v3 分支不触发 ci-v2
- [ ] 只改 docs/ 的 v3 分支推送不触发 ci-v3(paths 过滤生效)
- [ ] CI 用的测试库初始化=官方 sql+cleanup 两步,与本地手册一致(写进 02-实施.md)
