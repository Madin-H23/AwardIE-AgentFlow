# 批1-定制基座 03-测试(矩阵与结果)

> 2026-09-23 | 前置:MySQL 3307 + Redis 6379 + awardie_v3/awardie_v3_test(五脚本初始化)

## 一、集成测试(LaboratoriesControllerTest,3 例,v3 范式样板)

| 用例 | 锁定行为 | 结果 |
|---|---|---|
| createGetUpdateDeleteFlow | 创建→get 字段一致→page 关键词 LIKE 命中→update→delete(逻辑)→删后 get 为 code 0+data null(芋道约定)→删后 update 报 1003000000 | ✓ |
| pageKeywordMissReturnsEmpty | 关键词不命中返回空列表(mapper 直插种子) | ✓ |
| unauthenticatedRejected | 无 token → body code 401(芋道业务码式) | ✓ |

范式要点:@SpringBootTest(classes=YudaoServerApplication)+数据源覆盖到 awardie_v3_test+口令走环境变量;@BeforeEach 先登录再设租户上下文(MockMvc 请求后租户会被过滤器清);只测外部行为。

## 二、全量套件(mvn -B test,awardie-v3 根)

| 范围 | 数量 | 结果 |
|---|---|---|
| 上游自有测试(yudao-common/system/infra 等) | 778 | 0 失败(CodegenEngineUniappTest 整类排除,见 02-实施 偏差 4) |
| 本批新增(LaboratoriesControllerTest) | 3 | 0 失败 |
| **合计** | **781** | **BUILD SUCCESS** |

## 三、环境与脚本验证

| 项 | 方法 | 结果 |
|---|---|---|
| 清理脚本幂等 | 连续两跑 awardie-cleanup.sql | 用户 1/部门 3 不变 |
| 管理员口令切换 | 新口令登录 code 0;旧口令 admin123 拒(1002000000) | ✓ |
| 专用用户最小授权 | awardie_v3 连其他库被拒(仅授权 awardie_v3/awardie_v3_test) | ✓ |
| 应用连接身份 | information_schema.processlist 实证 user=awardie_v3 | ✓ |
| dev 库端到端 | curl:create(id 7)→get→page→delete→删后 update 1003000000 | ✓(ASCII payload,规避控制台 GBK 变形) |
| swagger 端点 | /v3/api-docs 含 7 个 laboratories 路径 | ✓(截图 swagger-laboratories-endpoints.png) |
| 脱敏守卫 | python3 scripts/v3_sanitize_sql_secrets.py --check | exit 0 |
| p3c 增量 | p3c-scan.sh 扫本批 8 个 Java 文件 | 12→**0 违例** |

## 四、验收指标对照(00-需求 五)

| # | 指标 | 实测 | 结论 |
|---|---|---|---|
| 1 | 坐标/品牌/库名三层改名+最小集数据+专用用户+环境变量口令 | T01-T03 全实证 | ✅ |
| 2 | cleanup 幂等+admin 改密 | 两跑一致+登录实证 | ✅ |
| 3 | CI 双过滤+MySQL/Redis service+test 库初始化 | ci-v3.yml 就位;推送后 CI 验证 | ✅(待 CI 回执) |
| 4 | laboratories 垂直切片 API 全链+菜单+权限 | 测试 3 例+dev 端到端+swagger | ✅ |
| 5 | 三道闸+p3c+测试文件清理 | 02-实施 三节全录;p3c 0;codegen 临时 json 已清 | ✅ |
