# 批2-用户与角色域 03-测试(矩阵与结果)

> 2026-09-24 | 前置:MySQL 3307+Redis 6379;awardie_v3_test 由官方 sql+cleanup+business+user-domain 初始化

## 一、新增测试

### 1. AwardiePasswordEncoderTest(纯函数,5 例)

| 用例 | 锁定行为 |
|---|---|
| matchesWerkzeugScryptHash | 测试生成的 werkzeug scrypt 哈希:正确口令 true/错口令 false |
| **matchesRealV2Hash** | **v2 实测存量哈希字面量(awardie_dev id=1832)+Mayy123 true/错 false**(跨 JVM 生成方保真) |
| encodeAndMatchesBcrypt | encode→$2 前缀+matches true/错 false |
| rejectsGarbage | null/非哈希/scrypt:bad → false |
| upgradeEncodingOnlyForScrypt | scrypt true/bcrypt false |

### 2. UserMigrationAuthTest(集成,2 例)

| 用例 | 锁定行为 |
|---|---|
| scryptUserLogsInAndUpgradesToBcrypt | 播种 scrypt 用户→登录 code 0+token→DB 哈希变 $2→二次登录 code 0 |
| bcryptUserUnaffected | bcrypt 用户登录 code 0+哈希仍 $2 |

### 3. LaboratoriesControllerTest(批1,重构后不回归 3 例)

自播种用户(存在即复用,避逻辑删除+唯一索引冲突);三角色断言不变。

## 二、全量套件

`mvn -B test`(awardie-v3 根):**788 例 0 失败**(上游自有+批1 3+批2 7)。

## 三、ETL 验证(本地手动,CI 无 PG 不覆盖)

| 项 | 结果 |
|---|---|
| 首跑 | system_users=1834(含 stock admin 被 id=1 覆盖)、profile=1834、role_links=1835(1834+admin 追加 super_admin)、hash 前缀 scrypt 1834、missing_ids=[] |
| 二跑(幂等) | 计数完全一致 |
| 自增 | AUTO_INCREMENT=1835 |
| 已知限制上报 | 非字母数字 login_code 1 个(id 1400「学号」,芋道登录校验不通过,照迁不静默);空口令 0 |
| 冲突前置校验 | v2 login_code 唯一性✓、异位 username 冲突检查(stock admin 为例外放行)✓ |

## 四、dev 端到端(HTTP 实证)

| 步骤 | 结果 |
|---|---|
| admin/Mayy123 登录 | code 0,userId=1832 |
| 212306413/P@ss301 登录 | code 0,userId=1370 |
| 登录后哈希 | 两用户均 scrypt(162)→`$2a$04$`(60) |
| 二次登录(升级后) | 均 code 0 |
| 错误口令 | 1002000000 拒绝 |
| 「学号」登录 | 500=curl GBK 假象(非法 UTF-8),非服务端缺陷 |

## 五、验收指标对照(00-需求 四)

| # | 指标 | 实测 | 结论 |
|---|---|---|---|
| 1 | ETL 1834+幂等+id 保真 | 见三 | ✅ |
| 2 | v2 原口令登录+升级 | 见四+集成测试 | ✅ |
| 3 | 三角色落库+关联 | 见 T03 | ✅ |
| 4 | 测试+ETL 验证记录 | 本文档 | ✅ |
| 5 | 三道闸+p3c+批1 不回归+CI | 02-实施 三/四节;788 例;CI 待回执 | ✅(待 CI) |
