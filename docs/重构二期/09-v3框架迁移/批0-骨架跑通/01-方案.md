# 批0-01-方案:芋道骨架跑通(stock system+infra)

> 出口条件(立项文档批0行):stock 芋道在 48080 启动;swagger 200;admin 登录成功;调通一个系统 API。

**范围**:零代码改动,纯环境与骨架验证——clone/构建/数据库/Redis/启动/登录/API 六连。

**判定标准**:
1. `mvn compile`+`mvn install -DskipTests` 成功(JDK21);
2. MySQL 库+59 表就位(官方 sql 脚本);
3. Redis 6379 PONG;
4. 48080 swagger/knife4j 200;
5. admin 登录拿 token,任一系统 API 返回 code 0。
