# 01 — templates 表结构 + 菜单权限点

**What to build:** 新建 `awardie_templates` 表(证书模板),在 v2 十六字段语义基础上补齐芋道基础设施列(`tenant_id` / `creator` / `create_time` / `updater` / `update_time` / `deleted`),把 `granted_role` 从 `default_fields` JSON 里提为独立列(唯一性"同竞赛+同授予角色"因此可走列比较),四个规则字段用 MySQL `JSON` 类型(与批4/6 的 `validation_result`/`other_members` 一致),`competition_id` 建索引;同步在业务菜单脚本里加菜单 3005 与五个权限点 3051-3055(`business:templates:query/create/update/delete/export`),授权 super_admin 与 awardie_admin,`system_role_menu` 的 INSERT **必写 `tenant_id=1`**(批6 头号缺陷)。不加 `name`/`status`/业务编号——v2 无此语义、无存量值。错误码占 1_003_006_XXX 段。

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] dev 库与 test 库执行成功;列齐全且全部带中文注释(芋道 codegen 硬性要求)
- [ ] 重放脚本不报错(幂等靠 SQL runner 容忍"已存在"错误码,MySQL 8 无 `ADD COLUMN IF NOT EXISTS`)
- [ ] 五个权限点可被 `MenuMapper.selectListByPermission` 反查到;super_admin 与 awardie_admin 授权行 `tenant_id=1`
- [ ] 官方 `system_menu` 中 3005/3051-3055 确认未被占用(已核实最大 id 12939,该段空闲)
- [ ] 批3 预留的竞赛引用保护因建表而自动生效(不改批3 代码,留待 T05 集成测试实证)
