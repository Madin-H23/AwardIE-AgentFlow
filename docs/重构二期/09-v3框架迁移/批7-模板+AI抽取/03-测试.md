# 批7-模板+AI抽取 03-测试

> 2026-09-24 | 配套 `02-实施.md`(四道闸记录与未完成项)

## 一、测试矩阵

### 1.1 单元测试(59 例,已跑,全绿)

| 测试类 | 例数 | 覆盖点 |
|---|---|---|
| `AwardieFileStorageTest` | 15 | 三校验 + sha256 去重 + 路径穿越 + Content-Type 映射;**批7 新增 6 例**:只算哈希不落盘、删除、删除幂等、删除路径穿越、`deleteIfUnreferenced` 双向(无引用→删/有引用→保)、**引用清单 8 表钉死 + 不可变** |
| `TemplateAiServiceTest` | 10 | fake 模式桩契约(试测/预抽取/prompt)+ 规则 JSON 归一(空/合法/非法/非对象);**批7 新增 4 例**:预抽取走三校验(魔术字节不符/超 10MB)、**Worker 4004 原样透传**、传输异常映射 4003 |
| `TemplateServiceImplTest` | 15 | 创建落库字段、角色白名单、竞赛存在性、唯一性、编辑白名单、删除回收、详情契约、规则 JSON 构造(含损坏数据容错)、样本图三态 |
| `SubmissionValidatorTest` | 14 | 批4 既有,回归 |
| `AwardiePasswordEncoderTest` | 5 | 批2 既有,回归 |

**关键设计**:凡"测修复有没有生效"的用例,都用**非默认值 + 真实对象**。例如:
- `deleteIfUnreferencedKeepsFileStillReferenced` 用真实 `AwardieFileStorage` + mock checker,验证真走"查引用→删"链路,而不是只验 mock 被调用;
- `extractForCreateRejectsNonWhitelistedFile` 注入**真实** `AwardieFileStorage`(不是 mock),否则 mock 永远不会抛,测试就成了永绿假阳性;
- `sha256HexMatchesStoreHashWithoutWritingFile` 断言"目录里没有文件",而不是只断言哈希值对。

### 1.2 集成测试(32 例,已跑,全绿)

| 测试类 | 例数 | 覆盖点 |
|---|---|---|
| `TemplateDomainTest` | 22 | 列表双维过滤、创建落库(含 `tenant_id=1` 与样本图落盘)、重复创建、非法角色/不存在竞赛/魔术字节不符、详情键名契约、编辑白名单、删除(含逻辑删+文件回收+共享图不误删)、竞赛删除保护(**批3 预留引用保护首次实证**)、样本图回显往返/文件失存、AI 三端点 fake、非法规则 JSON、无样本图试测、9 端点越权 403、**创建回滚回收**、**长度区间非法**、**保留键剔除** |
| `AiWorkerGrpcDegradeTest` | 3 | 审核流 AI 建议在 Worker 不可达时转人工审 + 4003 + 免责声明 + **快速返回不挂死**;模板域 AI 端点返 4003 非 500;懒连接(应用在 Worker 缺席时能启动) |
| `OrphanFileCompensationTest` | 5 | 首次提交落一个文件、**重复提交不新增文件**、**事务回滚回收孤儿**、**共享内容不被误删**(走真实 `TemplateService.deleteTemplate`)、无引用后回收 |

**独立上下文的理由**:`AiWorkerGrpcDegradeTest` 必须用 `mode=grpc` + 不可达端口,与其它测试类属性不同。芋道 `@SpringBootTest` 不复用上下文,多一次启动是有意的代价——换的是"grpc 模式真的被走到"的实证,同上下文改配置测不到。

**AI 测试不依赖真 Worker**:fake 模式验桩契约;grpc 模式用 `port=1` 指向不可达端口验降级。CI 无 Worker 进程,这是唯一可行姿势。

## 二、静态门禁

| 门禁 | 状态 | 说明 |
|---|---|---|
| 编译(主 + 测试) | ✅ | `mvn test-compile` 0 错误 |
| 单元测试 | ✅ | 59 例 0 失败 |
| p3c 增量 | ✅ | 工具未装(同批6),人工核对阿里规约:@author/JavaDoc/行尾注释/空 catch/未用 import,0 违例;修 2 处行尾注释 |
| 四道闸 | ✅ | 双轴 review + OCR delegate + security-audit + p3c,0 High/Critical(修后) |
| SQL 灌库 | ✅ | 全新 test 库执行六脚本,表 20 列/6 权限点/10 条 tenant_id=1 授权逐项抽查 |
| 集成测试 | ✅ | 32 例 0 失败(模板域 24 + gRPC 降级 3 + 孤儿补偿 5) |
| 全量回归 | ✅ | **801 例 0 失败**,连续两轮 |
| CI 建库复刻 | ✅ | DROP+重建+六脚本顺序,连续两轮全绿 |

## 三、截图

未生成。原因:swagger UI 需启动 48080 后端,本轮未启动 dev 服务(全量测试已用 MockMvc 覆盖同等契约断言)。口径不虚报,留待 dev 验收轮补。

## 四、已实证 vs 未实证(交付诚实性)

**已实证**:
- 主代码与测试代码全部编译通过;
- business 模块 59 例单测全绿(本批新增 39 例);
- 四道闸 0 High/Critical,其中 3 个 High 是审查抓出后当场修的;
- p3c 增量 0;
- gRPC stub 重新生成成功,`SERVICE_NAME=awardie.ai.AiService` 与 Python Worker 契约一致(wire 包名未动);
- proto 与 Python 侧逐行比对:仅新增 Java 选项,wire 契约零改动;
- 菜单 ID 3005/3051-3055 与官方 `system_menu`(最大 id 12939)无冲突;
- 授权 SQL 全部写 `tenant_id=1`(批6 头号缺陷的防线);
- 引用清单 8 张表与 `awardie-business.sql` 实际路径列逐列对齐。

**CI 复刻验证额外实证**:
- 全新 test 库六脚本顺序执行成功,`awardie_templates` 20 列齐全、菜单 3005+3051-3055 齐备、10 条授权行全部 `tenant_id=1`、三角色齐备;
- **首轮复刻即抓出 3 个真缺陷**(全部是"代码正确、测试自欺"型):测试清理用 MyBatis-Plus 逻辑删除导致行永久残留;断言取列表首项而非本用例的文件(内容寻址下撞路径);模板域测试未清待审表。详见 `02-实施.md` 第七节;
- 修复后连续两轮全量 801 例 0 失败。

## 五、未覆盖与说明

1. **真 Worker 冒烟未做**:需要 Worker 进程 + 百度 OCR/DeepSeek 密钥(在用户处)。本批只验了"Worker 缺席时不崩"这一侧。
2. **并发场景未测**:唯一性穿透与文件回收 TOCTOU 都是已知边界(决策 D12/D13),补测需要并发测试基建,不在本批范围。
3. **gRPC final 成功路径未测**:需要 in-process gRPC server 或真 Worker。当前只测了"不可达"这一侧。
4. **PDF 样本图未验**:三校验允许 pdf(v2 等价),但 Worker 侧对 PDF 的 OCR 抽取能力未验证过——若 Worker 只支持图片,创建 PDF 模板后试测会失败。已写入遗留。
