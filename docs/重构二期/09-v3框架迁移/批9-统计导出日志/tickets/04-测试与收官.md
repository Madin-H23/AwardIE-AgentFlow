# 04 — 测试 + 四道闸 + 收官

**What to build:** `AuditLogQueryTest`(分页/四筛/日期边界/动作标签/租户隔离/越权)、`StatsOverviewTest`(summary+category 确切数值/Top 排序含同值 name 升序/未关联桶/租户隔离/空表/越权)、`BusinessExportTest`(两类 × 双格式;CSV 字节级断言含 BOM/表头/列序/学号是 username/转义/公式注入/行数上限/空表/越权);单测 `CsvWriterTest`(逗号/引号/换行/CRLF/公式前缀四类/空值/null)与 `ActionLabelTest`(1/6/7/8 + 未知码兜底)。**用固定数据夹具逐条锁 SQL 数值口径**——v2 的 `AdminStatsTest` 只验结构不验数值,不继承那个水平。全量 0 失败;p3c 增量 0;四道闸(双轴 review + OCR delegate + security-audit + p3c);推前本地复刻 CI 建库(全新 `awardie_v3_test` + CI 同款六脚本顺序 + utf8mb4_bin,本地复跑前先 `redis-cli -n 1 FLUSHDB`);swagger 截图;02-实施 + 03-测试 文档;memory 滚动;commit(`feat(v3):` + ticket 号) + 推送 + CI 绿。

**Blocked by:** 01、02、03

**Status:** ready-for-agent

- [ ] 新增测试全绿;全量 0 失败(基线 111 + 本批新增)
- [ ] 四道闸 0 High/Critical;p3c 0
- [ ] 推前复刻 CI 建库验证;CI success
- [ ] 02-实施 + 03-测试 文档落盘
- [ ] swagger 截图;memory + handoff
- [ ] commit 中文描述、`feat(v3):` 前缀、含 ticket 号;推送成功
