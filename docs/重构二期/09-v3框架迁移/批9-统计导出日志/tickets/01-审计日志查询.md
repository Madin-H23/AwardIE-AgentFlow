# 01 — 业务审计日志查询

**What to build:** `GET /business/logs/audit` 分页查询 `awardie_achievement_audit_log`,支持四种筛选(成果类型 achievementKind / 动作码 actionType / 操作人关键词 operatorKeyword / 日期区间 createTime),租户与逻辑删除显式过滤。动作码 1/6/7/8 转中文标签(actionLabel)。补 `achievement_id` 与 `create_time` 索引(现有按 achievement_id 查时间线是全表扫,批9 全量分页会放大)。菜单 3009 + 权限点 `business:logs:query`。复用现有 DO/Mapper,不新建表。

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] 分页:总数/页内容/越界页返回空
- [ ] 四种筛选各自生效;组合筛选生效
- [ ] 日期区间边界(当天含、不含)
- [ ] 动作码中文标签;未知码有兜底
- [ ] 租户隔离:tenant 2 的行 tenant 1 查不到
- [ ] 空表返回空不 500
- [ ] 索引生效(EXPLAIN 走索引不全扫)
- [ ] 教师/学生 403
