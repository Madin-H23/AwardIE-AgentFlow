# 01 — 大创领域模型 + 表约束补齐

**What to build:** 新建 `InnovationProjectDO` / `InnovationProjectMapper` / `InnovationService` 分层(批6 的大创分支保留不动,新代码独立);`awardie_templates` 之外的 `awardie_innovation_project_students` 补 project FK + `(project_id, student_id, deleted)` 唯一约束 + `project_id`/`student_id` 查询索引;`awardie_innovation_projects` 的 `funding_amount` 列注释写明单位为元。所有新代码的读写必须带 `tenant_id` + `deleted=0`(通用 `AchievementVaultService.update()` 只按 id 更新是既有缺口,不得照抄)。错误码取 `1_003_008_XXX` 段。菜单 3006 + 权限点 3061-3065(`business:innovation:query/create/import/update/calibrate`),授权 super_admin 与 awardie_admin,`system_role_menu` INSERT 必写 `tenant_id=1`。

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] dev/test 两库执行成功;project_students 的 FK 与唯一约束生效
- [ ] 菜单 3005/3006 与权限点 3051-3055/3061-3065 齐备;授权行 `tenant_id=1`
- [ ] 幂等重放不报错(容忍"已存在"错误码)
- [ ] 现有 `AchievementVaultTest` 与 `ReferenceCheckTest` 全绿(本批不动既有服务,不得引入回归)
