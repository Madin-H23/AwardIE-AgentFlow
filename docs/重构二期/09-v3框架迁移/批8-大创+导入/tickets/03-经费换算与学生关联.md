# 03 — 经费换算 + 导入时自动建立学生关联

**What to build:** 两条用户 2026-09-25 拍板的决策落地。**经费(决策 C)**:列语义统一为元(列注释写明),导入时按万元乘 10000 存元——换算只在导入边界发生一次,库内值语义唯一;非法经费是行级错误。**学生关联(决策 B-lite)**:导入时按 `student_leader_id` 到 `system_users.username` 精确匹配(v3 的 username 就是学号,批2 ETL 映射自 v2 `login_code`,实测 1792 个纯数字账号),命中建 `role=leader`/`match_type=student_id_exact`;其他成员列按顿号/逗号/分号切分,能解析出学号形态的项尝试匹配建 `role=member`,只有姓名的跳过;匹配不上**不阻断导入**,计入 `studentsUnmatched` 返回;编号重复被跳过的行不建关联;关联表唯一约束防重复。补一条集成测试锁死"导入 2.5 万元 → 库里 25000 元"。

**Blocked by:** 02(导入通道)

**Status:** ready-for-agent

- [ ] 经费 2.5(万元)入库为 25000(元);0 与超大值正确
- [ ] 非法经费是行级错误,不静默变 null
- [ ] leader 学号命中 → 建 `role=leader`/`match_type=student_id_exact`
- [ ] member 学号命中 → 建 `role=member`
- [ ] 纯姓名成员跳过,计入 `studentsUnmatched`
- [ ] 学号查无此人时导入仍成功,只计数不阻断
- [ ] 重复导入同一编号不重复建关联;唯一约束生效
- [ ] 学生"我的大创"能查到新导入项目(查关联表实证)
