# 05 — 集成测试 + 四道闸 + 收官

**What to build:** `InnovationImportTest`(≥10 例:preview 正常/篡改 rows 无效/token 重复使用/超行数/坏表头/非法类型·日期·经费/重复编号/事务回滚/经费换算/关联建立与未匹配)与 `InnovationCalibrateTest`(≥8 例:全部边界 + 幂等 + 计数 + 审计);单元测试覆盖日期解析(v2 全部格式 + 2 月 30 日/13 月/只有年份/待定/混合分隔/trim)、经费换算、关联匹配。全量 0 失败;p3c 增量 0;四道闸(双轴 review + OCR delegate + security-audit + p3c);推前本地复刻 CI 建库(全新 `awardie_v3_test` + CI 同款六脚本顺序 + utf8mb4_bin)再跑全量——批4/6/7 三次证明有效,批7 靠它抓出三例"测试自欺"(逻辑删除残留、断言取列表首项、共享引用行未清);swagger 截图;02/03 文档;memory 滚动 + handoff;commit(`feat(v3):` + ticket 号) + 推送 + CI 绿。测试清理用**物理 DELETE**(批7 教训:MyBatis-Plus 逻辑删除会留 deleted=1 行,后续"取第一条"拿到历史行导致断言落空)。

**Blocked by:** 01、02、03、04

**Status:** ready-for-agent

- [ ] 新增测试全绿;全量 0 失败(基线 801 + 本批新增)
- [ ] 四道闸 0 High/Critical;p3c 0
- [ ] 推前复刻 CI 建库验证;CI success
- [ ] 02-实施 + 03-测试 文档落盘
- [ ] swagger 截图入档;memory + handoff
- [ ] commit 中文描述、`feat(v3):` 前缀、含 ticket 号;推送成功
