# 批8-大创+导入 02-实施(收官记录)

> 2026-09-25 | 分支 refactor/v3-yudao-migration | 上一提交 862582c4(批7)
> 需求/规格:`00-需求.md`(R1-R7)/ `01-spec.md`;tickets 五张在 `tickets/`

## 一、执行结果

| Ticket | 结果 | 关键实证 |
|---|---|---|
| T01 领域模型+表约束 | ✅ | `InnovationProjectDO`/`InnovationProjectStudentDO`+2 Mapper;关联表补 FK/唯一键/索引/审计列;菜单 3006+权限点 3061-3065;错误码 1_003_008 段 |
| T02 xlsx 导入+token | ✅ | preview 严格表头校验+1000 行上限+首行回显;服务端一次性 token(绑定租户与操作人);confirm 只收 token |
| T03 经费换算+学生关联 | ✅ | 万元×10000 存元(2.5→25000 实证);leader 建关联+member 按 V1 `姓名(学号)` 解析;学号下限收到 8 位 |
| T04 status 编辑+校准 | ✅ | 单项编辑带枚举校验;批量校准严格早于今天、当天不动、只动进行中、CAS 更新、不可解析计数 |
| T05 收官 | ✅ | 单测 19 例 + 集成 21 例;全量 **111 例 0 失败**;四道闸 0 High/Critical;p3c 0;CI 建库复刻连续两轮全绿 |

## 二、CI 复刻验证抓出的三个真缺陷

推前按批4/6/7 纪律**本地复刻 CI 建库**(全新 `awardie_v3_test` + CI 同款六脚本顺序 + `utf8mb4_bin`)再跑全量,首轮即 3 例失败,全是真缺陷:

1. **批6 建表时列不全(High)**:关联表缺 `updater`/`update_time`,而 DO 继承 `BaseDO`(含这两字段),insert 时 MyBatis-Plus 带上它们 → 500 `Unknown column`。**这列错了三批才被发现**(批6 建表时列就不全,批7 只做引用查询不 insert,批8 第一次真正 insert 才暴露)。已补列 + ALTER;
2. **同一脚本内"建表带索引 + ALTER 又加"**:全新库也会报 1061/1060 并**中断整个脚本**,导致后面的菜单与用户域 SQL 全跑不到(菜单 0 条 → 全站 403)。已改为建表不带、只由 ALTER 添加,新建表与旧表两条路径都成立;
3. **测试 SQL 照抄 v2 带 v3 没有的列**(`min_length`/`language`/`need_translate`)——已改。

另发现**跨测试类的 Redis 权限缓存互污**(批6 已知纪律):`LaboratoriesControllerTest` 直插 `system_user_role` 不驱逐缓存,清 `redis-cli -n 1 FLUSHDB` 后即通过。CI 每次干净 Redis 不受影响,但**本地复跑前必须先清缓存**——已记入本地验收流程。

## 三、四道闸记录

### 闸1 双轴 review

**轴一(规范)**:4 项全修——SQL 注释含分号(违反批5 纪律,我自己已踩过一次)、未用 import 与孤儿错误码、项目类型白名单两处重复定义、JavaDoc 与实现不符(按索引读实为按名映射、租户拦截器描述、"含竞赛名"实为无)。

**轴二(spec 符合)**:9 项全修。
- **preview cache 非线程安全(High)**:普通 `LinkedHashMap` 下两个并发 confirm 可能同时取到同一 Entry,"token 一次性"失效。已加 `synchronized` + 补 8 线程并发消费测试;
- **强类型 DTO ≠ 严格表头校验(High)**:FastExcel 缺列不报错(字段变 null)、多列忽略、列序变照读,`EXPECTED_HEADER` 原本只是未被调用的常量。已给 `ExcelUtils` 加 `readHeader()` 读原始表头,preview 严格比对十项文字;
- **测试矩阵不足(High)**:13 例 < 规格 16 例,已补到 21 例;
- `studentsUnmatched` 统计与匹配器实际结果脱节,已改为结构化 `LinkOutcome`;
- 导入缺日期/经费/长度/编号必填校验,已补;
- 端点单复数不一致(spec 写单数、实现用复数):按批3-7 仓库惯例保留复数,已更新 spec;
- 逻辑删除唯一键只支持一轮历史(Medium):`(project_id, student_id, deleted)` 中 deleted 是 BIT(1),第二轮删除会冲突。**记入遗留**,改法涉及生成列,成本高于收益;
- 行摘要哈希未实现(Low):服务端独占缓存下不构成漏洞,已从 spec 删除该决定;`errorCount`/`firstRowEcho` 已登记为增量响应字段。

### 闸2 OCR delegate 审查

与闸1 大部分重叠,另发现两条:
- **导入实际读取所有 sheet(Medium)**:`doReadAll()` 遍历整个工作簿,隐藏 sheet 的数据会混进导入结果。已显式 `sheet(0)`;
- 关联表缺 project FK(Medium)——已补;校准 UPDATE 未维护 `updater`(Low)——已补。

### 闸3 security-audit

2 High,全修。
- **H-1 preview token 未绑定主体(High,已修)**:token 缓存不记 `tenantId`/`operatorId`,A 租户管理员的 token 一旦泄露(访问日志/浏览器历史/Referer),B 管理员可拿它 confirm,把 A 的项目内容写进 B 的库并读到 A 的项目名/成员/学号。已修:Entry 绑定主体,consume 时校验(不匹配即视为无效 token,不泄露"该 token 存在但不属于你");日志不再打 token 明文;补集成测试实证"他人 token 无法 confirm 且不入库";
- **H-2 通用成果库跨租户更新大创(High,已修)**:批6 的 `AchievementVaultService.update` 用 JdbcTemplate `UPDATE ... WHERE id = ?`,无租户条件(JdbcTemplate 不经 MyBatis-Plus 租户拦截器)。本批新增了带租户校验的大创专用编辑,旧路径就成了绕过口。已修:补 `tenant_id = ? AND deleted = b'0'`,controller 传 `TenantContextHolder.getRequiredTenantId()`;补集成测试实证"改他租户的行影响 0 行"。

Medium 2 项(更新时实验室编号未校验租户、缓存无字节预算)、Low 4 项记入遗留。

**结论:0 Critical / 0 High(修后)**。

### 闸4 p3c

0 违例(沿批6-7 的人工核对方式:@author/JavaDoc/行尾注释/空 catch/未用 import)。

## 四、本批修掉的 v2 缺陷

1. **伪 sha256 校验**:v2 的 preview 返回哈希、confirm 接收,controller 与 service 都不用;confirm 完全信任客户端 rows,可任意篡改。v3 改为服务端缓存 + 一次性 token(绑定主体),confirm 只收 token,篡改无效;
2. **导入无事务**:v2 `importRows()` 无 `@Transactional`,中途异常留半截数据。v3 整体一个事务;
3. **无行数上限/无表头校验**:v2 无上限、无行数限制。v3 上限 1000 行(超出明确拒绝)+ 严格表头文字比对;
4. **经费非法静默置 null**:v2 `tryDouble()` 失败变 null。v3 转行级错误,并校验非负与 `DECIMAL(12,2)` 上界(超界原本会让整批回滚);
5. **导入不建学生关联**:v2 导入只写项目表,学生端"我的大创"永远看不到新项目(28 行关联数据是一次性脚本灌的)。v3 导入时按学号自动建 leader + 尽力建 member;
6. **手拼成员 JSON**:v2 手工拼接字符串数组,成员名含引号/反斜杠就拼出坏 JSON。v3 用 Jackson,且按 V1 口径统一为对象数组 `[{姓名,学号}]`;
7. **只写年月按月首日**这类隐含语义,在解析器里显式注释固定,避免后续被"优化"掉;
8. **校准并发**:v2 `UPDATE ... WHERE id = ?` 无状态条件,两次并发会互相覆盖。v3 用 `AND status='进行中'` 做 compare-and-set。

## 五、决策与语义差

**用户拍板(2026-09-25)**:
- D1 导入自动建学生关联 → leader 必建、member 按 V1 格式尽力匹配、匹配不上计入未匹配不阻断;
- D2 经费单位 → 列存元,导入按万元 ×10000 换算;
- D3 不做"审核通过自动物化大创" → v2 也没有,做了就是发明业务;
- D4 前端按钮留批10 → v3 前端源码目录当前为空,批8 抢做会与批10 冲突;
- D5 批6 通用成果库的 innovation 分支保留不动 → 新代码独立,减少回归面;
- D6 `project_no` 唯一性不改 → 全表唯一(逻辑删除后占位),改租户内唯一会影响 ETL 与引用检查,记入遗留。

**与 V2 的语义差(有意保留)**:
1. 审核 innovation 仍 `skipped`(v2 同);
2. 端点路径用复数 `/business/innovations/*`(仓库惯例,spec 已同步);
3. 强类型读取按**列名**匹配(v2 按列下标),列序调换不再读错;
4. 成员格式以 V1 为准(对象数组 / `姓名(学号)` / 分隔文本三种都认)。

## 六、遗留与下批入口

1. **批9 入口**:统计/导出/日志(复用本批的项目类型与状态维度);
2. **批10 入口**:前端壳 + 学生端"我的大创"门户(本批已建好关联表与查询能力,批10 直接接);
3. **本批挂账(需显式立项)**:
   - 逻辑删除唯一键只支持一轮历史(改法:生成列 + 仅活动行参与唯一);
   - 关联表唯一键不含 `tenant_id`(项目 id 全局唯一,当前无冲突面);
   - 更新大创时未校验 `laboratoryId` 属本租户;
   - preview 缓存无字节预算(只有条目数上限);
   - preview 缓存是内存态,**多实例部署时 confirm 可能打到另一实例找不到 token**(生产需换共享缓存);
   - 导入/校准只有应用日志,无结构化操作审计(操作人 + imported/skipped/ids);
4. **待用户复核**:D1-D6;批4/5/6/7 的既有决策。
