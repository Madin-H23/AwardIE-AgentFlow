# 02-实施:挂账清偿批——templates validation-rules 销账 + 大创 status 方向1+2

> 实施日期:2026-09-22 | 分支:refactor/v2-stack-migration

## 一、改动清单(代码 diff 全貌)

### 新增(3 个文件)

| 文件 | 内容 |
|---|---|
| `awardie-backend/src/main/java/com/awardie/admin/InnovationStatusService.java` | 校准 service:@Transactional;SELECT 进行中+非空 end_date 候选 → `parseLooseDate` 宽松解析 → 逐行 UPDATE `status='已结题', updated_at=NOW()` → system_event_log 审计(category='system',detail 手拼 JSON 含 counts+ids,同 InnovationImportService 范式) |
| `awardie-backend/src/test/java/com/awardie/InnovationStatusServiceTest.java` | 纯函数单测 13 例 |
| `awardie-backend/src/test/java/com/awardie/InnovationCalibrateTest.java` | 集成测试 8 例(BaseIntegrationTest/awardie_test,夹具 9 行 CAL-% 标记) |

### 修改(2 个文件)

| 文件 | 改动 |
|---|---|
| `awardie-backend/src/main/java/com/awardie/admin/AdminVaultController.java` | 构造注入 InnovationStatusService+UserRepository;新增 `POST /innovation/calibrate-status`(requireAdmin→findByLoginCode→service→`ApiResponse.ok(result,"已校准 N 条")`) |
| `awardie-frontend/src/views/AdminAchievementsView.vue` | script 增 `calibrating` ref + `calibrateStatus()`(ElMessageBox.confirm→apiJson POST→ElMessage+load,busy try/finally);filter-bar 增大创 tab 专属按钮 `data-testid="vault-calibrate"` |
| `awardie-frontend/e2e/pages.spec.js` | 追加 UX-8 用例(确认框+取消不变更) |

**validation-rules 销账:代码 0 改动**(决策记录见 01-方案 〇节)。

## 二、实施中的偏差与修正

1. **`NumberFormatException` 误入 import**:首版写了 `java.util.NumberFormatException`(实际在 java.lang),编译失败 → 删导入即过;
2. **测试键名错位**:`responseCarriesCalibratedIds` 首版断 `data.ids`,record 序列化实为 `calibratedIds` → 改键名(该失败由首轮 mvn 抓住,未流出);
3. **E2E accept 路径的数据纪律**:原设想 E2E 走完整校准,评估发现 dev 库 11 条真实行会被改写且 teardown 不清理 → 改为取消路径,accept 由集成+手动覆盖(方案阶段已记录,实施按此执行)。

## 三、验证实录

- 后端:`mvn -B test` **158 passed, 0 failed**(137→158,+21);首轮全量即绿(修正键名后);
- 前端:`npm run lint` **0 errors**(540 warnings 为存量);`npm run build` 过;
- 静态门禁:`check_raw_controls.py` ✓(44 文件无裸控件);`check_jdbc_placeholders.py` ✓(25 处 ? 与参数一致);
- E2E:`npx playwright test` **38 passed**(37→38,UX-8 1.0s);
- 手动验收(MCP,全栈重启后新 jar):admin 登录 → 成果库→大创管理 → 状态校准 → 确定 → 消息「已校准 11 条」→ psql 复核 **已结题 11/进行中 9** → 审计行「大创状态校准:校准 11 条,跳过无法识别 0 条」(operator=admin,category=system)→ 二次点击 → 「校准 0 条」、分布不变(幂等)→ 截图 `校准后大创tab混合状态.png`。

## 四、OCR delegate 审查(2026-09-22,commit 2626217)

**Preview**:12 文件,可审 3(后端 2 + 前端 1),排除 9(测试类 default_path、文档 unsupported_ext,均 OCR 默认规则)。

| 文件 | 结论 | 要点 |
|---|---|---|
| InnovationStatusService.java | reviewed | SQL 全参数化;detail JSON 仅整数无转义面;@Transactional 由控制器跨 bean 调用无自调用失效;parseLooseDate 非法值(13 月/2 月 30 日/超大年份)均有捕获;stateless 无线程共享态 |
| AdminVaultController.java | reviewed | 新 POST 路由与 GET /{type}、DELETE /{type}/{id}、PUT /awards/{id} 方法与段数均不撞(运行时 401+集成 200/403 实证);findByLoginCode+orElseThrow 与 AdminImportController 同范式;requireAdmin 前置 |
| AdminAchievementsView.vue | reviewed | busy try/finally 防重复提交;无 v-html;按钮 v-if 限大创 tab+后端强制 admin;消息文案与确认框边界说明一致 |

**Findings:Critical 0 / High 0 / Medium 0 / Low 1**:

- [Low] `InnovationStatusService.calibrateEndedProjects` 对 ids 逐条 `jdbc.update`(循环内 DB 写)。**不修**:管理员一次性动作,N=表行数(现 20,业务量级百位),单事务内;与 InnovationImportService.importRows 逐行写库的同构先例一致,改批量 IN 反而引入动态 SQL 面。记录备查。

**已静默排除的疑似项**:① 前端 `body.data as {...}` 无 null 守卫——与同页 load()/review() 直接取 body.data 的既有风格一致,ApiResponse.ok 契约下 data 非空;② SELECT 无 LIMIT——表规模有界(理由同 Low);③ NUMERIC 正则允许混合分隔符(`2025-06.30`)——有意宽容,单测 toleratesMixedSeparators 锁定。

**覆盖率**:reviewed 3/3,skipped 0。
