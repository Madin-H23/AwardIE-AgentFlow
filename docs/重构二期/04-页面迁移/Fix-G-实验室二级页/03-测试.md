# Fix-G 测试记录

> 测试范围:实验室详情聚合/编辑/下载三端点 + 三页 UI;环境=测试库 awardie_test(集成)/dev(E2E)。

## 一、端点测试(LabPortalEndpointTest,4 例全绿)

| 用例 | 断言要点 | 结果 |
|---|---|---|
| detailAggregates | 聚合返回 code=0/名称/instructors/students/downloadCount/awardCount;不存在 id → 4004 | ✅ |
| updateLabFields | PUT 更新生效且库内核对;name="" → 4000 | ✅(修:后端补名称必填校验) |
| downloadsListForLab | 列表返回插入的文件(标题/大小) | ✅ |
| nonAdminForbiddenOnAll | 学生访问 detail/downloads → 403 | ✅ |

## 二、E2E 回归(31 条全绿)

- 原有 31 条(页面冒烟/守卫/业务闭环/交互深度)无回归;
- 实验室卡片名称链接、下载/编辑入口经 MCP 手工复验可达(本轮新页路由)。

## 三、测试过程中发现并修复的问题

| # | 问题 | 根因 | 修复 |
|---|---|---|---|
| 1 | PUT name="" 仍更新成功 | updateLab 缺名称必填校验 | 后端补 4000;测试同断言通过 |
| 2 | nonAdminForbiddenOnAll "URI is not absolute" | 该用例 get() 用了 getRootUri()+path 拼接(cookie 串拼进 authority) | 改回与其他测试类一致的相对 path(exchange 自动补根);mvn clean 验证非编译缓存问题 |

## 四、遗留
- 封面图上传(依赖文件存储)、实验室成果展示列表(award 关联分页)——方案已声明挂账,下批。

## 五、commit 后 OCR 审查(补跑,2026-09-02 晚)

审查范围 4e2437f..HEAD(5a69c88+b5c5134),preview 6 生产文件可审(测试/截图/文档排除)。**发现 2 High + 1 Critical,已全部修复**;同源排查另波及 3 个既有控制器。

| # | 严重度 | 文件 | 问题 | 处置 |
|---|---|---|---|---|
| 1 | **Critical** | LabDownloadsView.vue | `rows.value = body.data.content`——后端 downloads 返回**纯数组**(非 Page 包裹),`.content` 为 undefined → 模板 `rows.length` 抛 TypeError,下载专区页整体渲染崩溃(任何访问必现) | 改 `Array.isArray(body.data) ? body.data : []`(空态兼容纪律) |
| 2 | **High** | AdminConsoleController.java(detail/downloads) | SQL 驼峰别名不带引号:`cover_image AS coverImage` 等 6 处——**PG 将不带引号标识符折叠为小写**(psql 实证),响应键变 `coverimage`/`createdat`,前端读 `coverImage`/`createdAt` 全部 undefined | 全部改双引号别名 `AS "coverImage"`(台账既有修法) |
| 3 | **High(同源波及)** | StudentPortalController / TeacherPortalController / AdminCompetitionDetailController | 同一折叠缺陷共 **21 处**别名:学生 achievements(awardLevel/projectNo/projectName/projectType/patentName/patentType/softwareName/registrationNumber)、教师(awardLevel×2/winnerName)、竞赛详情(competitionName 等 10 处)——**学生成果页"获奖等级"整列空白早已在线上发生**(巡检截图 w2 可证:iCAN/蓝桥杯行获奖等级空白,而小写别名 level/competition 列正常),Fix-F 竞赛详情编辑页表单载入全空 | 27 处全部双引号修复;各消费方前端均读驼峰,无反向风险 |

**审查方法佐证**:psql 直连 `SELECT cover_image AS coverImage ... LIMIT 0` 列头返回 `coverimage`;JdbcTemplate.queryForList 键名取自 JDBC column label(即服务端折叠后形式)。测试为何没拦住:既有断言只到"值层"(FixG下载文件/2048)与 put() 字面键(downloadCount),SQL 别名键从未被断言——**本次在 LabPortalEndpointTest 补键名级断言**(`"coverImage"`/`"createdAt"`/`"fileTitle"`/`"submitterType"`)锁定契约,防四发。

**修复后验证**:
- 受影响 3 测试类 10/10 绿;全量 JUnit **119/119 绿**;前端 eslint 0 error + vue-tsc/vite build 通过;
- 真实后端实测(dev 库):detail 返回 `"createdAt"`/`"coverImage"`(真实封面路径);学生 achievements 返回 `"awardLevel":"三等奖"`(修复前该列空白);
- E2E 31 条回归见下节。

**教训入台账**:不带引号驼峰别名折叠已是台账已知坑("PG 别名驼峰需双引号"),本次为**四控制器批量复发**——新写 SQL 时 camelCase 别名必须双引号;Map 型端点的测试断言应覆盖键名而非仅值。
