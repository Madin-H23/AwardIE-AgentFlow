# Fix-G 实施记录

> 编码依据:同目录《01-方案.md》。变更 commit 见 git log(G5/G6 同批推)。

## 一、变更文件清单

### 后端
| 文件 | 变更 |
|---|---|
| AdminConsoleController.java | 新增 3 端点:`GET /admin/laboratories/{id}/detail`(信息+教师+学生+下载计数+成果计数聚合)、`PUT /admin/laboratories/{id}`(三字段编辑,名称必填)、`GET /admin/laboratories/{id}/downloads`(下载文件列表);复用已有 DELETE |

### 前端
| 文件 | 变更 |
|---|---|
| LabDetailView.vue(新) | 详情页:头部卡(渐变+图标占位封面/名称/简介/建于)+统计四卡(关联成果/学生/教师/下载)+教师与学生成员两列名单 |
| LabEditView.vue(新) | 编辑页:名称必填+简介 textarea;保存走 PUT |
| LabDownloadsView.vue(新) | 下载专区:文件列表表(标题/大小/时间)+计数徽标+空态 |
| AdminLaboratoriesView.vue | 卡片名称变详情链接;卡片脚部加"下载"入口(与"编辑"并列) |
| router/index.ts | 注册 /admin/laboratories/:id、/:id/edit、/:id/downloads 三路由 |

## 二、实施偏差(对照方案)

| # | 偏差 | 理由 |
|---|---|---|
| 1 | 封面图上传未实现 | 依赖文件存储服务,方案已声明挂账;详情页保留 cover_image 字段展示位 |
| 2 | 编辑页封面交互省略 | 同上,仅 name/description 两字段 |
| 3 | detail 的"成果展示卡片列表"简化为计数呈现 | 完整成果列表需实验室-awards 关联分页,下批补 |

## 三、实施中发现并修复

| 问题 | 修复 |
|---|---|
| 脚本 f-string 跨行拼接语法错误 | four_deletes 改字符串拼接 |
| four_where 展开变量引用先于定义 | 解包块移到 stats 之前 |
| JDBC 传参含中文经 GBK 管道破坏 | 沿用 UTF-8 临时文件通道(q() 已根治) |

## 四、遗留
- 实验室成果展示列表(award 关联分页)——下批
- 封面图上传——挂账(文件服务)
