# Fix-T 测试记录(三段文档 03)

> 范围:AdminAchievementEditController 六端点 + 三编辑页;环境=awardie_test(JUnit)/ dev(E2E+实测)

## 一、端点测试(AdminAchievementEditTest,4 例全绿)

| 用例 | 断言要点 | 结果 |
|---|---|---|
| patentDetailAndUpdate | detail 键名契约(patentName/laboratories...)+ PUT 库内核对(名称/类型/公开号/发明人)+ 4000/4004 | ✅ |
| softwareDetailAndUpdate | detail 键名契约 + PUT 库内核对 + 4000 | ✅ |
| innovationDetailAndUpdateWithLeaderStatus | detail 键名契约 + **leaderStatus 学号精确→matched** + otherMembers JSONB 往返 + PUT 全字段(国家级/已结题/金额)库内核对 + 4000 | ✅ |
| studentForbiddenOnAll | 学生角色三域 GET→403 | ✅ |

## 二、全量回归

| 门 | 结果 |
|---|---|
| JUnit 全量 | **128/128 绿**(124+4 新增) |
| E2E 全量 | **31/31 绿**(1.0m,teardown 零残留) |
| 五门禁(CI 同款) | eslint 0 error / raw-controls ✓ / jdbc-audit ✓ / vue-tsc+vite build ✓ / 后端测试 ✓ |

## 三、真库实测(dev)

- patents 表无数据(与 v1 治理后对齐)→ 4004 行为验证 ✓
- 大创 id=58:契约键全齐;leaderStatus={'曾铭龙','212306301','matched'};**otherMembers 为对象数组 [{"姓名","学号"}]** → 触发缺陷修复(见下);laboratories 下拉 5 条

## 四、测试中发现并修复的问题

| # | 问题 | 根因 | 处置 |
|---|---|---|---|
| 1 | **otherMembers 对象数组会被前端 join 成 [object Object] 且保存退化** | dev 真实数据元素是 `{姓名,学号}` 对象而非字符串——测试库自造字符串数据掩盖了真实形态(结构层断言盲区再现) | 后端 otherMembers 改 List<Object> 原样序列化;前端"未改动→透传原 JSON,改动后→按分隔拆字符串",归一化显示"姓名(学号)";测试补对象元素场景 |
| 2 | ApiResponse record 访问器 data() 非 getData() | 编译期发现 | 修正 |
| 3 | 测试 PUT 体 Map.of 含 null 值抛 NPE(Map.of 不允许 null,上限坑姊妹坑) | 台账补条目 | 三处 HashMap |
| 4 | PatentEditView 插值内拼接说明文字语法错误 | vue-tsc 拦截 | 三元+括注拆分 |

## 五、偏差复核(对照 01-方案)
- templates create/detail 挂账(AI 提取域)✓ —— **收官需用户确认**
- view 并入编辑页(系统信息区)✓;证书文件挂账占位 ✓
- 负责人选择器简化为文本+三态徽章 ✓

## 六、遗留
- certificates 文件上传/下载(与 R 批图片同源)
- templates create/detail(等 AI Worker 扩 extract/prompt RPC 后另立批次)
