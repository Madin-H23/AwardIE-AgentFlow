# Fix-TP 实施记录(三段文档 02)

> 对应方案:01-方案.md | 变更基线:f0dfb7d(架构票 15991b5 登记后)|用户拍板:立项

## 变更文件清单

| 文件 | 类型 | 内容 |
|---|---|---|
| awardie-backend/.../admin/AdminTemplateDetailController.java | 新 | POST /create(multipart 全字段+同竞赛同角色 4009)/GET /{id}/detail(全字段 JSONB ::TEXT+hasImage+竞赛名)/PUT /{id}(@Transactional)/GET /{id}/image(BYTEA 按文件头判型)/POST /{id}/test(fake 确定性桩,grpc 模式 4003) |
| awardie-backend/src/test/.../AdminTemplateDetailTest.java | 新 | 2 用例:multipart 创建+detail 键名契约+图片字节往返+唯一性 4009+角色校验+404;PUT 更新库内核对+fake 试测+学生 403 |
| awardie-frontend/src/views/TemplatesCreateView.vue | 新 | 1:1 区块:基本信息卡(竞赛/角色/样本图上传预览)+详细配置卡(名称/语言/翻译/长度/关键词/样本文本/JSON 编辑区) |
| awardie-frontend/src/views/TemplatesDetailView.vue | 新 | 同构编辑+样本图回显+试测区(fake 结果展示) |
| awardie-frontend/src/router/index.ts | 改 | 注册 admin/templates/create、admin/templates/:id/detail |
| awardie-frontend/src/views/AdminTemplatesView.vue | 改 | 页头"新建模板"按钮+列表"详情"链接 |
| docs/重构二期/05-架构票/...md | 改 | 状态 待排期→**已立项**(2026-09-03 用户拍板) |

## 实施中发现并修复的问题

| # | 问题 | 处置 |
|---|---|---|
| 1 | **INSERT 占位符 11 个 vs 参数 10 个**(拆 blob 语句时旧占位符残留)→ PG"参数 12 未设定"驱动错误 | 精确清点占位符(11)后修正 VALUES;教训:多 JSONB+BYTEA 混合 INSERT 必须机器清点,手数不可靠 |
| 2 | PUT 的 keywords 收到 JSON 数组而 record 定义 String | 契约改为 List<String>(前端 toLines 直发数组) |
| 3 | 测试用 GET 打 POST /test 端点 | 补 post 辅助(与 put 同构) |
| 4 | TemplatesCreateView 漏放保存按钮(save 未引用,eslint 抓) | 页头补保存按钮 |

## 实施偏差(相对 01-方案)
- 无新增;字段管理表按方案以 JSON 编辑区先行(真 RPC 后升级结构化表单)

## commit 后 OCR 审查轮(基线 15991b5..27f5d6b,推送前执行)

total 5 / reviewed 5 / skipped 0 / coverage 100%。

| 严重度 | 问题 | 处置 |
|---|---|---|
| low | create 查重为"先 COUNT 后 INSERT"(TOCTOU),无 DB 唯一约束兜底,并发双建可绕过 | 记录不修:v1 同为 manager 层查重,admin 单通道低并发;如需根治加 (competition_id, default_fields->>'granted_role') 唯一索引,随架构票 |
| low | fake 试测不接收样本图(v1 上传图试测) | 01-方案 偏差1 已声明,真 RPC 落地时一并实现 |

**审查基线滚动至 27f5d6b。**
