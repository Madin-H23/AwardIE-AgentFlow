# Fix-TP 测试记录(三段文档 03)

> 范围:AdminTemplateDetailController 五端点 + 两页面;环境=awardie_test(JUnit)/ dev(E2E+真库冒烟)

## 一、端点测试(AdminTemplateDetailTest,2 用例全绿,覆盖 8 断言组)

| 用例 | 断言要点 | 结果 |
|---|---|---|
| createDetailImageUpdate | multipart 创建→code 0;detail **键名级契约**(templateType/keywords/llmFields/hasImage/competitionName);图片字节往返(image 端点 200+image/jpeg+原字节);**唯一性同竞赛同角色→4009**;角色非法→4000;不存在→4004;PUT 更新库内核对(min_length/language/need_translate/sample_text) | ✅ |
| updateAndFakeTest | PUT 全字段更新(含 keywords 数组/need_translate)库内核对;**fake 试测** mode=fake+字段回显;学生 403 | ✅ |

## 二、全量回归

| 门 | 结果 |
|---|---|
| JUnit 全量 | **130/130 绿**(128+2 新增) |
| E2E 全量 | **31/31 绿**(teardown 零残留) |
| 五门禁 | eslint 0 error / raw-controls ✓ / jdbc-audit ✓ / vue-tsc+vite build ✓ / 后端测试 ✓ |

## 三、真库冒烟(dev,python requests+CSRF 头)

- multipart 创建(教师/样本图 18B/中文关键词)→ code 0;
- 唯一性重复创建 → 4009"该竞赛已有相同角色的模板";
- detail:12 键全齐,keywords JSONB 数组正确,hasImage=true;
- image 端点:200 + image/jpeg + 18 字节原样;
- test(fake):`{"mode":"fake","fields":{"award_level":"一等奖"}}`;
- 冒烟数据已清理(DELETE tid=58,templates 基线 20 条不变)。

## 四、测试中发现并修复的问题

| # | 问题 | 处置 |
|---|---|---|
| 1 | **INSERT 占位符 11 vs 参数 10**(拆 blob 语句时旧占位符残留)→ PG"参数 12 未设定" | 机器清点修正;教训:**多 JSONB+BYTEA 混合 INSERT 用脚本数占位符,手数必错** |
| 2 | PUT keywords 前端发数组而 record 定义 String → 5000 | 契约改 List<String> |
| 3 | 测试误用 GET 打 POST /test | 补 post 辅助 |
| 4 | TemplatesCreateView 漏保存按钮(save 未引用) | eslint 拦截,页头补齐 |
| 5 | curl -F 中文经 Windows 控制台 GBK 变形 → 4000 | 冒烟改用 venv python requests(UTF-8 干净);**教训:中文表单冒烟一律走 python** |

## 五、偏差复核(对照 01-方案)
- AI 抽取/试测=fake 确定性桩 ✓(偏差1,真 RPC 切换点已留)
- "生成的提示词模板"区块挂账占位 ✓(偏差2)
- OCR 自动识别挂账,手工 sample_text 可编辑 ✓(偏差3)
- 样本图仅图片、PDF 挂账 ✓(偏差4)

## 六、遗留
- 真 Extract/GeneratePrompt RPC(架构票已立项排期)
- PDF 样本支持
