# Fix-R 实施记录(三段文档 02)

> 对应方案:01-方案.md | 变更基线:2e6f776 → 本批

## 变更文件清单

| 文件 | 类型 | 内容 |
|---|---|---|
| awardie-backend/.../admin/AdminAwardEditController.java | 新 | GET /{id}/edit-detail 聚合(主字段+四组关联+姓名匹配状态+下拉四数据+默认实验室推断);PUT /{id} @Transactional 全字段+四关联表 delete+insert 重写(supervisorIds 顺序敏感→同步 supervisor_name;grantedRole teacher→教师/student→学生;学生证书强制清空关联学生) |
| awardie-backend/src/test/.../AdminAwardEditTest.java | 新 | 5 用例:detail 键名级契约断言(matched/not_found 状态)/PUT 全字段+关联重写库内核对+supervisor 顺序断言/学生证书清空关联学生/竞赛必填 4000+不存在 4004/学生 403 |
| awardie-frontend/src/views/AwardEditView.vue | 新 | 1:1 区块:页头三按钮/异常 alert(validationResult 解析)/三栏表单/关联信息四区块(证书类型切换显隐+学生建议下拉+徽章三态+指导教师上移下移调序)/OCR 折叠 |
| awardie-frontend/src/router/index.ts | 改 | 注册 admin/awards/:id/edit |
| awardie-frontend/src/views/AdminAchievementsView.vue | 改 | 获奖 tab 行操作加"详情编辑"链接(操作列 130→190);4 字段行编辑 dialog 保留 |

## 实施偏差(相对 01-方案)

| # | 偏差 | 说明 |
|---|---|---|
| 1 | 关联学生徽章不带三态标注 | relatedStudents 按关联表实际行渲染(全 matched 语义);v1 的 related_student_status_list 三态基于 related_student_name 文本,列上无文本字段场景价值低 |
| 2 | 删除复用既有 vault DELETE(/api/v2/admin/vault/awards/{id}) | 不新建端点;FK 拒绝由既有 4009 分支处理 |
| 3 | 学生建议下拉固定显示在输入框下方 | v1 Select2 弹层等价 |

## 实施中发现并修复的问题

| # | 问题 | 处置 |
|---|---|---|
| 1 | 测试断言 row.get() 返回 Object 无 contains | String.valueOf 包裹 |
| 2 | PUT 请求体 17 字段超 Map.of 上限(台账坑) | HashMap 构建 |
| 3 | 首版 eslint 抓到 parseNames 未使用(CI lint 连续第五次同类) | 提交前本地原始输出核对拦下,删除死函数 |
| 4 | 契约补充:matched 状态项需带 id(前端构建 studentWinnerIds 依赖) | resolveNameStatus 补 id 字段 |
