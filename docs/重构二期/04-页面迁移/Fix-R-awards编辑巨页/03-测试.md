# Fix-R 测试记录(三段文档 03)

> 范围:AdminAwardEditController 两端点 + AwardEditView 页;环境=awardie_test(JUnit)/ dev(E2E+实测)

## 一、端点测试(AdminAwardEditTest,5 例全绿)

| 用例 | 断言要点 | 结果 |
|---|---|---|
| editDetailAggregatesWithKeyContract | code=0 + **键名级契约**(17 键:competitionId/studentWinners/supervisors/winnerStatus/competitions...)+ 匹配状态 matched/not_found + 999999→4004 | ✅ |
| updateRewritesFieldsAndAssociations | 全字段库内核对(award_level/year/track/laboratory_id/granted_role)+ **supervisor 顺序→supervisor_name 同步**("FixR教师乙, FixR教师甲")+ 四关联表重写行数 | ✅ |
| studentCertificateClearsRelatedStudents | 学生证书强制清空 related_students + granted_role=学生 | ✅ |
| competitionRequiredAndNotFound | competitionId=null→4000;不存在→4004 | ✅ |
| studentForbiddenOnAll | 学生角色 GET/PUT→403 | ✅ |

## 二、全量回归

| 门 | 结果 |
|---|---|
| JUnit 全量 | **124/124 绿**(119+5 新增) |
| E2E 全量 | **31/31 绿**(1.0m,teardown 清理零残留) |
| 前端门禁 | eslint 原始输出 **0 error**(212 warnings 为既有基线);vue-tsc+vite build 过 |
| 真库实测(dev) | detail 17 契约键齐全;winnerStatus '陈品天'→matched(id:1370);下拉 1793 学生/40 教师/197 竞赛;未登录 401 |

## 三、测试中发现并修复的问题

| # | 问题 | 根因 | 处置 |
|---|---|---|---|
| 1 | 首版 eslint **1 error**:parseNames 未使用 | 契约解析挪到后端后前端 helper 成死代码——CI lint 连续第五次同类 | 提交前本地原始输出核对拦下,删除(纪律再次验证有效) |
| 2 | row.get() 断言无 contains 方法 | queryForMap 取值为 Object | String.valueOf 包裹 |
| 3 | PUT 请求体 17 字段超 Map.of 上限 | 台账已知坑(第 2 次预防性规避) | HashMap 构建 |
| 4 | mvn package 首次失败留下无主清单 jar,java 启动即退 | repackage 未执行 | 重跑 package 后正常;健康探活 200 确认 |
| 5 | **CI frontend-v2 败于 Raw-control gate**(本地四门禁未覆盖) | 指导教师上移/下移用了原生 `<button>`——违反 EP 白名单约定;**本地回归清单缺 check_raw_controls/check_jdbc_placeholders 两道 python 门禁** | 改 el-button text;本地补跑两道 python 门禁全绿;**教训:每批本地回归=CI 五道门禁全对齐,不止四道** |

## 四、偏差复核(对照 01-方案声明)
- 图片区块:占位+image_hash 文本 ✓(偏差1)
- 保存不重验 is_abnormal、GET 不自动清异常 ✓(偏差2/3)
- 指导教师上移/下移替代拖拽,顺序语义保留 ✓(偏差4)
- requireAdmin 单通道 ✓(偏差5)
- OCR 面板只读展示 ✓(偏差6)

## 五、遗留
- 图片文件服务(依赖 v1 存储域迁移)——与实验室封面上传同源挂账
- AwardValidator 实时重验移植(如 v2 需要 is_abnormal 维护链路,另立架构票)
