# Fix-V:教师审核台已审行按钮(三段文档 02)

## 改动清单

| 文件 | 改动 |
|---|---|
| `awardie-frontend/src/views/TeacherReviewView.vue` | ①"审核操作"列:`v-if="scope.row.status === 'pending'"` 包住批准/驳回/输入/确认四件套,`v-else` 渲染状态 el-tag(archived→已归档 success/其余→已驳回 info);②"AI 建议"列同条件隐藏,非 pending 显示 "—"(.muted);③ scoped 样式加 .muted |
| `awardie-frontend/e2e/pages.spec.js` | 增补测试"审核台已审行不再渲染审核按钮(Fix-V)" |

## 关键实现点

- 判断字面量 `'pending'` 与后端状态域严格一致(pending/archived/rejected,实读 SubmissionService/ReviewService 确认,见 01-方案 根因表)。
- el-tag type 三元:`scope.row.status === 'archived' ? 'success' : 'info'`,未知状态兜底 info"已驳回"。
- 模板缩进随 `<template v-if>` 包裹整体下移一层,vue/html-indent 无新增告警。
- 不改后端 `findAll()`(行为变更另行立项,偏差声明见 01-方案)。

## 本地门禁

- `npm run lint`:0 errors;本文件仅存量 1 warning(92:39 min-width,stash 前后对比确认非本批引入)。
- `npm run build`:✓ 17.85s。

## OCR 审查轮(342b8ec..afb9255,与 Fix-U 同轮,0 High / 0 Medium / 1 Low)

| 文件 | 结论 | 依据 |
|---|---|---|
| TeacherReviewView.vue | 过审 | 状态字面量 `'pending'` 与后端域严格一致(SubmissionService L92 setStatus("pending") / ReviewService archived、rejected,实读确认);严格相等 ===;el-tag 三元为单层非嵌套;模板缩进随 template v-if 包裹无新增告警 |
| e2e/pages.spec.js(默认排除,自审) | 过审 | 已审行按钮 count 0 断言命中真实库 archived 行(business-flow 闭环产物);el-tag 文本"已驳回"非 button 不干扰计数 |

**Low(同轮共享,详见 Fix-U 02-实施)**:ai-suggest 端点无状态校验(UI 已隐藏,API 直调可达)——挂账随教师审核域后续批次。

**存量观察(不计入本批)**:`load()` 刷新后 `rejectId/rejectComment` 未重置(原有代码,diff 范围外);pendingList=findAll 语义问题已定性另行立项(01-方案)。

**审查基线滚动至 afb9255。**
