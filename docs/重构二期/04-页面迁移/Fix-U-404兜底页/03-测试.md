# Fix-U:404 兜底页(三段文档 03)

## 测试结果

| 门禁 | 结果 |
|---|---|
| npm run lint | 0 errors(本批净新增 warning 0,存量对比法验证) |
| npm run build | ✓ 17.85s |
| E2E 增补 | "未匹配路由落 404 兜底页(Fix-U)":登录后访问 `/v2/no-such-page-xyz` → 404 标题 + 返回首页链接可见 |
| E2E 全量 | **33 passed(1.2m)** = 31 存量 + 2 新增(global-teardown 自动清理测试数据) |
| JUnit 后端 | **130 条全绿(1:22)**,BUILD SUCCESS |
| 静态门禁 | check_raw_controls ✓ / check_jdbc_placeholders ✓ |

## 覆盖说明

- 守卫路径:未登录访问任意未匹配路径 → 登录页(既有守卫,不重复测试);登录访问未匹配路径 → 404 页(E2E 新测)。
- 返回首页落位:依赖守卫既有角色分派逻辑(#35/#36 已测),不重复建测。

## 遗留与挂账

- 无本批挂账。
