# 文件存储域迁移 批1:文件域服务收敛(三段文档 03)

## 测试结果

| 门禁 | 结果 |
|---|---|
| 后端 JUnit 全量 | **134 passed 0f**(BUILD SUCCESS;含夹具根治后 AdminTemplateDetailTest 3 例) |
| 数据迁移 | awardie_dev 20/20 条 blob→文件,逐条回读哈希校验通过,blob 置空 |
| image 端点实测 | tpl 57:200 + image/png + 598280 bytes + 魔数 89504E47(与迁移前一致) |
| E2E 全量 | **37 passed**(UX-7 模板主链走新 store 写路径) |
| lint / build | 前端零改动(本批纯后端+脚本),前端门禁沿用批 7 绿态 |

## 验收对照(架构票批 1 交付物)

- 代码级唯一写入口:✅ templates 三处读写全部经 FileStorageService,与 submission 同源;
- templates 切换目录存储:✅ 写(store+path)/读(readAll)全链切换,存量数据 20/20 迁移;
- 迁移脚本幂等可重放:✅(只处理 blob 非空且 path 空的行)。

## 遗留(批 3 收口)

- `sample_image_blob` 列物理 DROP(待脚本在 CI/全部环境跑过后,批 3 迁移收口);
- 下载/上传端点统一 + 存量 v1 文件 ETL(批 3 范围)。

## 批 2 前瞻

awards 证书链:awards 加 file_path 引用列(Flyway)→ 物化链(ReviewService.materialize*)携带 pending.file_path → admin 证书上传端点 → awards/edit 页真图显示(清偿 Fix-R/Fix-T)。
