# AI Worker extract/prompt RPC 扩展——实施三段文档 03(测试)

## 测试结果

| 层 | 结果 |
|---|---|
| JUnit(fake 模式,CI 同款) | **131 全绿(0f)**,含新增 extractAndPromptFakeStubs:两端点键名级契约(mode/dataJson/ocrText/prompt)+缺文件 4000+学生 403 |
| 真 Worker 冒烟(`scripts/smoke_worker_rpc.py`,本地手动) | **SMOKE PASS**:Health ok;GeneratePrompt code=0 prompt 1742 字符;ExtractTemplate code=0(598KB 真样本图→17 结构化字段+251 字 OCR 文本,真百度 OCR+deepseek-v4-flash) |
| E2E | 33 passed(1.1m,页面本批未动,确认无回归) |
| 五门禁 | lint(未触前端)/raw-controls ✓/jdbc ✓/build ✓ 14.39s/后端测试 ✓ |

## 验收对照(票·四)

- fake 模式两页走通主链:**部分**——两端点+双模式已就绪,页面"上传→抽取→预览→确认"接线属票交付物 5(紧随页面批次),本批不涉及;
- 真 Worker 抽取抽验:✅ 冒烟覆盖(对照 v1 extract-for-create 行为,17 字段键名与 v1 契约一致);
- 全量回归+五门禁:✅(CI 三 job 终态见 push 后记录)。

## 遗留与挂账

- 页面主链接入(票交付物 5)→ 下一批次(三段文档流程)。
- 模板试测端点(`/{id}/test`)grpc 模式仍 4003(待页面批次接 ExtractTemplate)。
- `template_rule_json` 扩展位未消费(规则化抽取=后续演进,见 01-方案偏差声明)。
