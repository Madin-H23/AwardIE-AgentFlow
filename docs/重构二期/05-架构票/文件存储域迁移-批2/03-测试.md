# 文件存储域迁移 批2:awards 证书链(三段文档 03)

## 测试结果

| 门禁 | 结果 |
|---|---|
| 后端 JUnit 全量 | **135 passed 0f**(+1 证书链用例) |
| E2E 全量 | **37 passed** |
| lint / build | 0 errors / ✓ 9.75s |
| 端到端 | 上传真实图 200/0 → 回读 200 image/png 598280;死引用 404;V4 后 79 条引用文件全在盘 |

## 留档截图

`award-edit-certificate.png`——awards/1702/edit"奖状图片"面板渲染真实证书(蓝桥杯·二等奖,公章/二维码可辨)+ 选择/上传按钮;Fix-R 偏差(hash 占位)就此销账。

## 挂账销账

- Fix-R(awards/edit 图片仅 hash 占位):✅ 真图显示+上传补齐;
- Fix-T(证书文件挂账):✅ 物化链携带 certificate_path,新数据不再断链;
- 批 1 Low(image 端点缺文件落 5000):✅ 404/4004 韧性。

## 遗留(批 3)

- `temp_upload` 类历史死引用 22 条:文件本体已失存,终态=页面提示"可上传补齐";批 3 ETL 做存在性优先的按哈希重建;
- 存储根目录 CWD 依赖(测试 JVM 与运行后端两处):批 3 根目录参数化+一次性归并;
- `sample_image_blob` 列 DROP、下载/上传端点统一、v1 files 存量 ETL(~38 文件)。
