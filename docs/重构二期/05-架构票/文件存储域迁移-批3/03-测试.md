# 文件存储域迁移 批3:端点统一+存量 ETL+blob 退役(三段文档 03)

## 测试结果

| 门禁 | 结果 |
|---|---|
| 后端 JUnit 全量 | **135 passed 0f**(files.root=target/test-files 下) |
| E2E 全量 | **37 passed** |
| ETL | 38 文件 → 4 新写+34 去重;重连 other_files 1;全量回读校验通过 |
| V5 实证 | sample_image_blob 列已不存在;sample_image_path 在位 |
| 下载端点 | 200 + attachment + PNG 魔数(BR-7 语义保持) |
| 测试隔离 | target/test-files 15 文件;files/v2 零新增 |

## 架构票验收对照

- 两套存储方案收敛为一:✅ 唯一写入口 FileStorageService;BYTEA 已退役(V5 DROP);
- awards/edit 真图:✅(批 2 留档截图);
- 模板图片/学生提交/证书图下载语义一致:✅ 展示=inline(readAll+contentTypeOf)、下载=attachment(同一 readAll+resolve);
- 存量 v1 文件可访问:✅ ETL 38/38 哈希化入位(34 个与 v2 已有内容去重);
- 全量回归+五门禁+CI:✅(CI 见 docs commit 关联 run)。

## 遗留说明(非缺陷)

- 22 条 temp_upload 死引用:文件本体已失存,终态=页面提示"可上传补齐"(V4 已置空);
- other_files 历史行 file_hash 与内容 sha256 不匹配者未重连(防脏连设计),如需找回需人工比对。

## 架构票状态:✅ 全部交付物完成,转已完成
