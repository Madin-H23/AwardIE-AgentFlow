# UX-1 批3:列表页体验 + 工作台入口修复(三段文档 03)

## 测试结果

| 门禁 | 结果 |
|---|---|
| npm run lint | 0 errors;1 条存量 warning(TeacherReviewView min-width,前批确认) |
| npm run build | ✓ 22.23s |
| E2E 全量 | **34 passed(1.4m)** = 33 + 1 新增(侧边栏工作台入口);待审管理用例覆盖徽章断言;Fix-V 用例选择器已同步中文态 |
| 后端 | 未触及 |

## 诊断项销账

- A3(admin 待审管理已审行按钮):✅ Fix-V 模式推广完成
- B2(英文工程值直出):✅ 待审管理+教师审核台中文徽章化(useBadge 单一真源)
- B3(主键暴露):部分——AdminAwardsView 示范(.num),全族推广挂账
- 标题错位:✅ /admin/awards 页内标题=侧边栏"待审管理"
- 工作台无入口(用户实测):✅ 侧边栏固定项=工作台

## 遗留与挂账

- 筛选折叠(B5)、全列表页族徽章化/.num 推广、表格 hover 暗色对比度——挂账至批 5 亮暗核查或后续小批
- 批 4(dashboard 精修)按序进行
