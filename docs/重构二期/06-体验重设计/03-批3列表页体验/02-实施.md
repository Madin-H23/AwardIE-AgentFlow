# UX-1 批3:列表页体验 + 工作台入口修复(三段文档 02)

## 改动清单

| 文件 | 改动 |
|---|---|
| `src/layouts/ConsoleLayout.vue` | overview 固定项:数据总览→工作台(House,'/');数据总览挪入"用户数据"组首项 |
| `src/composables/useBadge.ts` | 新增:类型/状态中文映射+statusTagType(单一真源) |
| `src/views/AdminAwardsView.vue` | h2→PageHeader"待审管理";类型/状态列→中文 tag;操作列 Fix-V 模式推广;ID 列 .num |
| `src/views/TeacherReviewView.vue` | 状态列英文直出→中文 tag(useBadge) |
| `e2e/pages.spec.js` | 待审管理用例改写+新增侧边栏工作台入口用例;Fix-V 行选择器补中文态 |

## 本批踩坑

- **徽章化连锁反应**:状态列中文化后,Fix-V E2E 用例以 `archived|rejected` 文本选已审行会失效——选择器补 `已归档|已驳回`(断言随结构同步改的纪律实例)。
- desktop 控制通道(computer-use)截图拍到用户正在使用的真实桌面(非测试环境)——**立即弃用**,实景验证一律走 Playwright MCP 独立实例;E2E 断言已覆盖本批视觉验证点(徽章/入口),无需额外人工截图。

## OCR 审查轮(43c785b..2a4f81b,0 High / 0 Medium / 0 Low)

preview:4 reviewable+spec 排除;逐文件审:

| 文件 | 结论 | 依据 |
|---|---|---|
| useBadge.ts | 过审(一轮修正) | 首版 statusTagType 为链式嵌套三元(OCR 规则组禁止,ESLint 未启嵌套规则未拦)——自查抓出后改查表映射 amend;类型/状态映射兜底 String(v) |
| ConsoleLayout.vue | 过审 | overview 固定项改工作台+数据总览入组,两行配置;portal 侧"XX首页"体系对齐 |
| AdminAwardsView.vue | 过审 | PageHeader/徽章列/Fix-V 推广/.num;rejectId 只在 pending 行交互路径设置,无跨行残留 |
| TeacherReviewView.vue | 过审 | 状态列同源 useBadge |
| e2e/pages.spec.js(排除,自审) | 过审 | 标题断言对齐+徽章断言+Fix-V 选择器补中文态;定向 E2E 6 passed |

**审查基线滚动至 2a4f81b。**
