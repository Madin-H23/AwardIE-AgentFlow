# 批7:Worker RPC 页面批次(三段文档 02)

## 改动清单

| 文件 | 改动 |
|---|---|
| `awardie-backend/…/AdminTemplateDetailController.java` | test() 删 grpc 模式 4003 占位;读 sample_image_blob→extractTemplate→mode/fields/ocrText 契约;无图 4000 |
| `ai_worker/server.py` | GeneratePrompt min/max_length 数值解析 try/except→4000 友好码(Low 挂账销账) |
| `awardie-frontend/…/TemplatesCreateView.vue` | AI 抽取按钮(回填 sampleExtracted+sampleText)+生成提示词按钮+只读预览区+mode tag;postForm 复用 |
| `awardie-backend/src/test/…/AdminTemplateDetailControllerTest.java` | 新增纯 Mockito 3 例(grpc 键名契约/无图 4000/缺模板 4004) |
| `awardie-frontend/e2e/pages.spec.js` | 新增 UX-7 主链用例+留档截图 |

## 实测记录

- **运行态发现**:本地 18080 后端进程 13:14 启动,而 RPC 端点 17:02(ed5813c)才提交——**跑的老 jar 里没有这两个端点**,探针全 5000(全局兜底),一度误判端点 bug;重建 jar+重启后 200。教训:**跨会话接手后,后端进程版本与新代码对齐是探针/联调前置检查项**(对比进程 StartTime 与目标端点 commit 时间);
- Worker 4000 直调探针:坏规则 `{"min_length":"abc"}` → code 4000"模板规则数值非法";好规则 → code 0+1708 字符 prompt(SMOKE PASS);
- 真 Worker 链路:本批 test() grpc 分支与 extract-for-create 共用同一 extractTemplate stub 调用(该 RPC 已于 ed5813c 真 Worker 冒烟 SMOKE PASS),本批以 Mockito 单测覆盖 Java 侧键名契约,不重复起真链路。

## 本批踩坑

- **bash→python→文件三层转义**:heredoc 内 python 字符串写 `\\n` 落盘变真实换行(vue-tsc: Unterminated string literal),vite-error-overlay 遮罩挡全站 E2E——**python 生成代码文件后必须立刻 vue-tsc/编译验证**;
- **textarea 断言**:Playwright `toContainText` 读不到 textarea 的值(prompt 在 model-value),须用 `toHaveValue`;
- 单测自造数据又踩"Map.of 不允许 null 值"台账坑(null 的 sample_image_blob 场景),HashMap 显式 put。

## OCR 审查轮(5cf47b3..f5bfd85,0 Critical / 0 High / 0 Medium / 0 Low)

| 文件 | 结论 | 依据 |
|---|---|---|
| ai_worker/server.py(+10/-2) | 过审 | try/except(TypeError+ValueError)覆盖 None 外的非法类型与非数字串;4000 响应带 disclaimer+trace_id 与既有错误形态一致;已探针实证(坏 4000/好 0) |
| AdminTemplateDetailController.java(+26/-10) | 过审 | fake 分支语义逐行保持(键名契约 mode/fields/ocrText);BYTEA→byte[] 标准方言映射;无图显式 4000;StatusRuntimeException→4003 与兄弟端点同语义;别名 "se" 双引号符合 PG 坑台账;3 例 Mockito 单测覆盖三出口 |
| TemplatesCreateView.vue(+216/-31,--fix 重排为主) | 过审 | postForm 抽取后 save() multipart 字段逐项核对无丢失;aiExtract 回填策略(抽取值必填/样本文本留空才填);genPrompt 规则字段与 Worker 端 rule 消费键一一对应;dataJson 解析失败兜底原样字符串 |
| AdminTemplateDetailControllerTest.java / e2e(排除,自审) | 过审 | 单测三出口(code=0/4000/4004)+TestingAuthenticationToken 造 ROLE_ADMIN;UX-7 断言结构层(回填/预览可见),空库安全 |

**Low 观察不阻断**:test() fake 分支现在也会读 sample_image_blob 列(单行 BYTEA,admin-only 低频,接受)。
**审查基线滚动至 f5bfd85。**
