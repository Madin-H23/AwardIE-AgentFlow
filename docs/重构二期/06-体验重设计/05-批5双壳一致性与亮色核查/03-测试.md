# UX-1 批5:双壳一致性与亮色核查(三段文档 03)

## 测试结果

| 门禁 | 结果 |
|---|---|
| npm run lint | 0 errors(585 warnings 为存量基线) |
| npm run build | ✓ 10.42s |
| E2E 全量 | **35 passed(1.2m)**,含新用例"UX-5 亮色主题核查与表格 hover 实证" |

## 亮色核查截图(目检交付)

`05-批5双壳一致性与亮色核查/` 三张(1280×720,admin 实登):

- `light-workbench.png`——工作台:PageHeader+速览条四键+入口卡分组,亮色下徽章/卡片渲染完整;
- `light-dashboard.png`——数据总览:PageHeader+口径副标题+五类联动提示条;批 4 修复可见(本月新增 40/分类计数五项/无三重重复);
- `light-awards-pending.png`——待审管理:批 3 中文徽章(奖状/待审/已驳回/已归档)+ Fix-V 模式(仅 pending 行渲染通过/驳回按钮)在亮色下成立。

## 探针实证记录(一次性 Playwright 脚本,未入库)

| 场景 | 实测 td hover 背景 |
|---|---|
| console 表(竞赛管理)亮色 | `color(srgb 0.086 0.467 1 / 0.05)` = 品牌蓝 5% |
| console 表 暗色 | 同上(亮暗通用,作用域规则不分支) |
| portal 表(学生仪表板)亮色 | `rgb(245, 247, 250)` = EP 亮色库存 |
| portal 表 暗色 | `rgb(38, 39, 39)` = EP 暗色库存 |

## 诊断项销账

- 批 4 Low(主题切换 chart 不重绘):✅ watch dispose+重绘,E2E 真交互断言;
- 批 3 挂账(表格 hover 暗色对比度):✅ 探针实证亮暗均成立(console 品牌 5% / portal EP 库存),无需改码;
- 双壳页头统一(dashboard 裸 h1):✅ PageHeader 接入;
- 亮色核查:✅ 三页截图目检通过。

## 遗留与挂账(批 5 收官后另立小批)

- 筛选折叠(B5,长筛选表单折叠收起);
- 全列表页族徽章化与 `.num` 等宽数字推广(批 3 只做了待审管理+审核台两页示范)。

## UX-1 工作线收官

五批全部收官:批 1 全局交互地基 → 批 2 工作台重做 → 批 3 列表页体验 → 批 4 数据总览精修 → 批 5 双壳一致性与亮色核查。终态:E2E 35(批次用例 5 个)+ lint/build 门禁绿;诊断清单 A 交互 5 项/B 视觉 7 项全部销账或转挂账小批。
