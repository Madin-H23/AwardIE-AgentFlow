# UX-1 批2:工作台卡片化重做(三段文档 02)

## 改动清单

| 文件 | 改动 |
|---|---|
| `src/views/HomeView.vue` | 全量重写(77 行裸链接 → 页头+速览条+四组入口卡);入口与侧边栏菜单同源 |
| `e2e/pages.spec.js` | "首页展示业务入口与管理端入口"→"工作台卡片化渲染(UX-1 批2)"(分组词+entry-card+logout 断言) |

## 本批踩坑

- **no-unused-vars 现身**(台账坑复发确认):import 的 Upload 图标未使用,`--fix` 不删,原始 lint 输出抓到 1 error,手动删除。
- 副标题文案重复:用户名恰为"系统管理员",模板写死角色词导致"系统管理员 · 系统管理员 · …"——实景截图审查抓出,去掉角色词(验证纪律:截图自审有效)。

## OCR 审查轮(9c36815..fb46973,0 High / 0 Medium / 0 Low)

| 文件 | 结论 | 依据 |
|---|---|---|
| HomeView.vue(+205) | 过审 | 无 v-html/嵌套三元;overview 四键与 AdminStatsController put 清单实读核对;groups 模块级静态配置零响应式开销;apiJson try/catch 静默不阻塞入口主链;data-testid 全保留 |
| e2e/pages.spec.js(默认排除,自审) | 过审 | 断言与组件同步改写,工作台用例实跑 passed |

**审查基线滚动至 fb46973。**
