# UX-1 批4:数据总览精修(三段文档 02)

## 改动清单

| 文件 | 改动 |
|---|---|
| `src/views/AdminDashboardView.vue` | ①汇总卡左栏:grandTotal→compare.this,label"本周期成果新增(2026 年至今)"→"本月新增",sub 删待审/白名单只留上月+环比;②资产卡"成果总数"sub 五类小字→口径说明;③echarts tooltip 读 tokens(panel/line/ink)暗色适配 |
| `e2e/pages.spec.js` | 看板用例补"本月新增"断言 |

## 本批踩坑

- grandTotal computed 保留(密度卡 density 仍依赖),非死代码——删除会断密度计算。

## OCR 审查轮(d8e4136..42cc807,0 High / 0 Medium / 0 Low)

| 文件 | 结论 | 依据 |
|---|---|---|
| AdminDashboardView.vue(+13) | 过审 | tooltip token 读取与既有 ink2/line/brand 同模式,亮暗双值均正确解析;fmt null 兜底;grandTotal computed 保留(density 依赖,非死代码);deltaPct 模板逻辑原样 |
| e2e/pages.spec.js(排除,自审) | 过审 | "本月新增"断言与改动同步,看板用例 passed |

**Low 观察(挂账批 5)**:主题切换后 chart 不重渲染(tooltip/线条色停留切换前主题)——存量行为,与本批同根,批 5 亮暗核查统一处理(可 watch 主题事件重调 setOption)。

**审查基线滚动至 42cc807。**
