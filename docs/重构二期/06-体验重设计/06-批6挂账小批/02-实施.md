# UX-2 批6:挂账小批(三段文档 02)

## 改动清单

| 文件 | 改动 |
|---|---|
| `src/composables/useBadge.ts` | +SUBMITTER_TYPE_LABELS / submitterTypeLabel(单一真源扩展) |
| `src/views/AdminAwardsView.vue` | ①十维筛选折叠:三维常驻+展开更多筛选开关(advanced ref+ArrowDown/Up)+七维 v-if;②类型下拉 :label 改 typeLabel;③提交者类型列中文化 |
| `src/views/TeacherReviewView.vue` | 类型列裸英文→typeLabel tag;提交者类型列→submitterTypeLabel;提交者ID 列 .num |
| `src/views/StudentSubmitView.vue` `StudentDashboardView.vue` `StudentAchievementsView.vue` | 状态列裸列→statusLabel/statusTagType tag(三处同构) |
| `src/views/AdminCompetitionsView.vue` `AdminAchievementsView.vue` `AdminTemplatesView.vue` `LabDownloadsView.vue` | ID/年份列 class-name="num"(14 列) |
| `src/views/AdminDataAnalysisView.vue` `LabDataAnalysisView.vue` | useTheme+watch(themeRef) 按当前 tab 重建图表 |
| `e2e/pages.spec.js` | 新增 UX-6 用例(6 断言+2 留档截图) |

## 本批踩坑

- **局部函数与解构撞名**:两个 analysis 页各有一个读 tokens 的 `function theme()` 工具,`const { theme } = useTheme()` 直接撞名 vue/compiler-sfc 报 "Identifier 'theme' has already been declared"(vite 按需编译,整页白屏;E2E 首跑即抓)→ 别名 `themeRef` 解构。**同页既有同名工具函数时,composable 解构必须别名**;
- **ESLint --fix 净降噪**:触碰文件 --fix 后全库 warning 734→582(净 −152,0 errors 保持),属顺手清了触碰文件存量格式噪音,可接受;
- 批量模板编辑(python 多文件替换)后必须立刻跑 lint/build——本批 +85 条格式 warning 被门禁当场抓出并 --fix 归零。

## OCR 审查轮(3f0dd51..5cf47b3,0 Critical / 0 High / 0 Medium / 0 Low)

| 文件 | 结论 | 依据 |
|---|---|---|
| useBadge.ts(+10) | 过审 | 纯追加,与既有 typeLabel/statusLabel 同构;Record 查表+原值兜底,无嵌套三元 |
| AdminDataAnalysisView / LabDataAnalysisView(+8/+46) | 过审 | themeRef 别名解构避开同页 `function theme()`(撞名已被 E2E 首跑抓出并修);watch 分发与既有 activeTab watch 完全同构;tab2 走 loadTab2 重取数与 tab 切换行为一致 |
| StudentSubmitView / StudentDashboardView / StudentAchievementsView(+17/+13/+13) | 过审 | 状态 tag 未知值兜底 info+原值,与批 3 admin 侧同构;空表零行无断言风险 |
| TeacherReviewView(+26) | 过审 | 类型列 tag 化+提交者类型中文+ID .num,与 AdminAwardsView 批 3 形态一致 |
| AdminAwardsView(+103/-84) | 过审 | 折叠开关仅切 v-if 不改 filters 状态(展开态保留已填值);查询/重置常驻;类型下拉 typeLabel 纯展示层,提交值仍为英文枚举 |
| AdminAchievementsView / AdminTemplatesView / LabDownloadsView / AdminCompetitionsView(格式重排为主) | 过审 | 逐 hunk 核对:除 class-name="num" 外全部为 --fix 属性重排(单行拆多行),无语义变化;vue-tsc build+36 E2E 双重佐证 |
| e2e/pages.spec.js(排除,自审) | 过审 | 折叠默认态 toBeHidden(v-if 移除=hidden 语义成立);`not.toContainText('student')` 在中文化后可证伪;canvas 断言空库安全(renderTab1 空数据也出轴) |

**审查基线滚动至 5cf47b3。**
