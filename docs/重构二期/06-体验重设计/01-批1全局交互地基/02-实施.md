# UX-1 批1:全局交互地基(三段文档 02)

## 改动清单

| 文件 | 改动 |
|---|---|
| `src/styles/tokens.css` | 亮/暗段各补 `--tag-*` 四组(语义徽章);type scale 三变量;尾部追加 `.page-fade-*` 过渡(含 reduced-motion 豁免)与 `.num` 等宽工具类 |
| `src/layouts/ConsoleLayout.vue` | router-view 包 transition(v-slot 解构名 `routeComponent`,避开与 `import type { Component } from 'vue'` 的 vue/no-template-shadow 遮蔽) |
| `src/layouts/PortalLayout.vue` | 同款过渡 |
| `src/components/PageHeader.vue` | 新增:统一页头(title/subtitle/#actions) |
| `src/components/TableSkeleton.vue` | 新增:表格骨架(rows 可配) |
| `src/views/AdminCompetitionsView.vue` | 裸 h2 → PageHeader(示范页) |

## 本批踩坑

- **vue/no-template-shadow**:v-slot="{ Component }" 与既有 `import type { Component }`(vue 类型)遮蔽 → 解构改名 `routeComponent`。
- **unplugin-vue-components 只配 ElementPlusResolver**,自定义组件无自动注册 → 手动 import(与项目惯例一致)。
- 遗留发现:`teacher-submit-error.png` 为 tracked 文件,归档移动(b15934a)后删除记录未随上次 commit——本批补记删除(内容已在巡检目录入库)。

## OCR 审查轮(22cb566..d018436,0 High / 0 Medium / 0 Low)

preview:6 reviewable+1 binary 排除;逐文件审:

| 文件 | 结论 | 依据 |
|---|---|---|
| PageHeader/TableSkeleton | 过审 | 纯展示组件;props 类型完整;$slots.actions 插槽判断正确;无逻辑/安全面 |
| ConsoleLayout/PortalLayout | 过审 | transition 单根约束验证(五页跨壳抽测含 query 场景,零 console 警告);v-slot 解构改名避 shadow;key=route.path 防 query 重过渡 |
| tokens.css | 过审 | --tag-* 亮暗成对、与 --sev-* 命名隔离;reduced-motion 豁免 |
| AdminCompetitionsView | 过审 | h2→PageHeader 单点替换,E2E heading 断言仍命中 |
| teacher-submit-error.png(binary,排除) | 补记 | b15934a 归档移动的 tracked 删除补记,本批随 commit 落账 |

**审查基线滚动至 d018436。**
