# UX-1 批1:全局交互地基(三段文档 03)

## 测试结果

| 门禁 | 结果 |
|---|---|
| npm run lint | 0 errors;本批文件净新增 warning 0(两壳/PageHeader 引入的属性换行与 shadow 告警已当场清零;tokens.css 不在 lint 范围) |
| npm run build | ✓ 12.87s |
| E2E 全量 | 33 passed(1.1m;竞赛管理 heading 断言 h2→h1 仍命中) |
| 浏览器实景走查 | PageHeader 渲染正确;admin/awards→admin/competitions 跨页切换**零 console 错误**;过渡类生效 |
| 后端 | 未触及(纯前端批,五门禁中后端测试由 CI job 覆盖) |

## 效果留档

`../../ux1-competitions-pageheader.png`(PageHeader 示范页实拍;批 1 结束后归档本目录)

## 遗留与挂账

- PageHeader/TableSkeleton 的全量页面接入:批 2(工作台)/批 3(列表页族)顺带,不在批 1 范围(01-方案 范围声明)。
- `.num` 等宽工具类待批 3 数据列接入。
