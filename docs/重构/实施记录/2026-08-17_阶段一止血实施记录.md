# 阶段一·止血 实施记录（第一批）

| 项 | 内容 |
| --- | --- |
| **日期** | 2026-08-17 |
| **分支** | fix/p0-3-debug-rce / feat/db-connection-factory / fix/p0-8-image-rotate / feat/db-migration-stage1 |
| **依据** | SRS v1.8 §8.2 阶段一 / 数据库迁移脚本 §1 / 部署设计 §7.5 开发规范 |
| **备份** | `database/competitions.db.bak.2026-08-17`（10.3 MB，迁移前全量冷备） |

## 一、代码修复（每项带回归测试，全部通过）

| 问题 | 修复内容 | 测试 | 状态 |
| --- | --- | --- | --- |
| **P0-3** Debug RCE | debug 判定收紧为 `FLASK_ENV=development AND FLASK_DEBUG=1`（原先任一即开）；reloader 联动；settings.json 默认 env → production | `tests/security/test_debug_off.py` 7 用例（AST 提取真实判定函数 + 源码防回退） | ✅ |
| **P0-4/5/7** 连接契约 | 新建 `backend/utils/db_connection.py::get_connection`（foreign_keys=ON / WAL / busy_timeout=30000 强制）；接入 student/teacher/pending_achievement 三核心 Manager | `tests/unit/test_db_connection.py` 5 用例（契约/外键真实拦截孤儿/Row 兼容/WAL 持久化/防回退裸连接） | ✅ |
| **P0-8** 竖图旋转 | 删除"高宽比>1.5 且宽<2000 强转 90°"启发式，方向仅由 EXIF 决定 | `tests/unit/test_no_rotate_heuristic.py` 3 用例（AST 防回退 + 行为级：1080x1920 竖图压缩后仍为竖） | ✅ |

真实库验证：主库经工厂连接 `journal_mode=wal / foreign_keys=1 / busy_timeout=30000` 实测生效。

## 二、迁移脚本 §1 执行（含偏差说明）

| 节 | 结果 |
| --- | --- |
| 孤儿清理 | 4 条存量孤儿全部清除（lab 三表为复合主键，rowid 删除未命中，改语义条件删除命中）；`fk_check` = 空 ✓ |
| §1.1 索引 | awards 5 索引 + §1.3 `uk_awards_image_hash` 唯一护栏（存量重复 0 条，护栏创建即证明） |
| §1.2 关联表重建 | 4 张表 191/24/100/34 → 同数（无重复无孤儿损失），补齐 PK/FK/CASCADE（P1-12） |
| §1.4 audit_log | `achievement_audit_log` 建表 + 3 索引（P1-13 表就绪，AuditLogger 阶段二接线） |
| §1.5 乐观锁 | pending_achievements.version 列已加（P1-15 前置） |
| §1.6 admin bug | innovation_projects 20 行 submitter_id='admin' → 0（P1-11 部分） |
| **跳过项** | 词汇统一（granted_role 中文→英文）：**实际检查发现 admin 模板/编辑页直接展示中文值，单独改数据会打破 UI**——推迟至阶段三 ORM 化配套展示映射执行（符合 P2-14 修复建议本意） |

**收尾断言**：`foreign_key_check` 空 / `integrity_check` ok / image_hash 重复组 0。

## 三、执行偏差与教训（诚实记录）

1. **一次纪律违规**：P0-3 首次提交时管道 `\| tail` 吞掉 pytest 退出码，测试未绿即提交——发现后修正测试并补提交；后续批次改为显式 `RC=$?` 判断。
2. **两次脚本返工**：foreign_key_check 列数（4 列非 3 列）、python sqlite3 隐式事务与显式 BEGIN 冲突——均在**任何写操作生效前**失败，库零损伤，备份未动用。
3. 测试自身 bug 一例：简陋正则剥 JSON 注释误删字符串内 URL 的 `//`，改用项目 ConfigLoader 正规解析。

## 四、下一批（阶段一余项 + 阶段二启动）

CSRF + 强 SECRET_KEY + 移除默认密码（P1-1/2/3）→ 上传白名单 + 2GB 收紧 + 公开路由整改（P0-10/P1-23）→ 种子数据（FR-INIT）→ AuditLogger 接线 + 驳回重开 + 软归档（T1）→ 其余 10 个 Manager 接入工厂。

---

# 第二批（安全批，2026-08-17）

| 问题 | 修复 | 测试 |
| --- | --- | --- |
| P1-3 SECRET_KEY | `_is_weak_secret_key`/`validate_secret_key` + ProductionConfig 选中时 fail-fast；.env 本地换 64 位强随机 | test_secret_key.py 10 用例 |
| P1-2 默认密码兜底 | 学生/教师两处明文兜底删除；**存量 29 位空 hash 教师迁移**（随机初始密码，明文清单 database/init_passwords_2026-08-17.txt 已 gitignore，管理员线下分发） | test_default_password_removed.py 8 用例 |
| P1-23 2GB DoS | max_content_length_mb 2048→100 | test_upload_security 内断言防回退 |
| P0-10 上传+XSS | FileUploadService 三重校验（白名单 7 类/魔术字节/100MB）；/files/laboratories 双路由 require_login+mimetype 白名单+强制 attachment（D1） | test_upload_security.py 6 用例 |

**回归口径与发现**：
1. 修复一处**测试隔离缺陷**（debug 用例未清理 dotenv 注入的 FLASK_ENV——收集期 import 链泄漏，测试已改环境自持）
2. 全量跑暴露**存量失败 5 例**（test_llm_adapter 假 key 断言 vs 本机真实 .env、test_data_analysis 依赖真实库）——经基线 commit 对照确认**与本批无关**，登记 TODO T21；当前 CI 口径 = tests/security/ + tests/unit/test_db_connection.py + test_no_rotate_heuristic.py（本会话新写 39 用例全绿）
3. 测试自身三次返工（tell 伪造两法皆败→改调小常量法）——教训：优先选零 mock 的等价测法
