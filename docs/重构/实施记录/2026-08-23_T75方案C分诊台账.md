# T75 方案 C 分诊台账（2026-08-23）

> 探针基线：140 例实跑 2.5s = 114 通过 / 26 失败（certificate 12 + 其余 14）。
> ⚠️ 勘误：goal 文本写"9 例失败/121 例转正"系口算错误，实测 4 文件失败合计 **14 例**、转正 122 例（其中 1 例单例降级 xfail），本台账为准。
> 处置结果：**153 passed + 13 xfailed + 6 xpassed + 0 failed**（五文件口径）。

## 一、4 文件 14 例逐例定性

| # | 用例 | 定性 | 依据 | 处置 |
| --- | --- | --- | --- | --- |
| 1 | template/test_manager.py::test_match_template_teacher_keywords | 期望过时（样例文本不满足现行契约） | award 规则要求 ≥30 字且含「奖」+「赛」；样例 27 字且无「赛」 | conftest 样例加长至 40 字 |
| 2 | template/test_matcher.py::test_match_full_selects_template_with_more_keywords | 期望过时（择优逻辑已移除） | 现行 `_match_award` 按模板顺序首个命中，不再按关键词数择优 | 断言改 template_id==1 + 注释 |
| 3 | template/test_matcher.py::test_match_full_data_security_competition | 期望过时（样例 17 字 < min_length 30） | 同 #1 契约 | 文本加长至 39 字 |
| 4 | template/test_matcher.py::test_match_full_no_match_returns_default_prompt | 期望过时（同 #1） | 同 #1 | 文本加长（含奖赛、避开全部模板关键词） |
| 5 | template/test_matcher.py::test_match_full_simple_type_single_template | 期望过时（patent 规则 min_length=200） | 实测规则：patent/software 分别要求 200/150 字 | 文本加长至 ~246 字 |
| 6 | template/test_matcher.py::test_match_full_simple_type_multiple_templates | 期望过时（同 #5） | 同 #5 | 同 #5 |
| 7 | unit/test_award_extractor.py::TestFieldCleaning::test_clean_multi_name_with_commas | 期望过时（清洗职责简化为透传） | `_clean_field_value` 不再合并空槽，下游 `split_names` 负责切分 | 断言改透传现状 + 注释 |
| 8 | unit/test_award_extractor.py::TestFieldValidation::test_valid_data_passes | 期望过时（新增完整性校验为有意特性） | 缺 date/year/edition 时告警「奖状无日期」——文案完备的正式特性 | 测试数据补 `"year": 2024` |
| 9 | unit/test_award_extractor.py::TestExtractionFlow::test_extract_success | 期望过时（mock LLM JSON 键名过时） | 真实性门控 `_check_valid_certificate` 读规范键 `winner_name`，mock 用旧键 `winners` → 判空拒绝 | mock 键名改 winner_name + 断言同步；另补类型规则自持加载 |
| 10 | unit/test_innovation_extractor.py::TestInnovationExtractor::test_init | 期望过时（description 文案精简） | 现值「大学生创新创业训练项目文件」 | 断言对齐现值 |
| 11 | unit/test_innovation_extractor.py::TestInnovationExtractor::test_is_innovation_file | 期望过时（fixture 配置落后于生产） | 生产 settings.json 含「大学生创新创业」关键词，测试 fixture 缺失 | fixture file_keywords 补齐 |
| 12 | unit/test_innovation_extractor.py::TestDataParser::test_parse_date_range | 期望过时（新增全角～解析为改进） | 现返回 ("2024.01","2025.12") | 断言对齐 |
| 13 | unit/test_innovation_extractor.py::TestExtractionFlow::test_extract_with_mock_context | 期望过时（输出键名中改英） | 项目字典键已为 project_name/department 等英文名 | 断言键名对齐 |
| 14 | unit/test_innovation_extractor.py::TestDataParser::test_parse_students | **疑似模块真缺陷** | 回退正则 `[^\d\s]+?` 吞全角括号（"张三（"）且按"更长姓名优先"覆盖正确解析——pattern2 明明可正确命中却被 pattern4 污染 | **单例降级 xfail(strict=False) 挂账 hewj** |

## 二、横切修复（非单例）

- **类型规则自持加载**：matcher/award 两组补 autouse fixture 加载真实 `backend/extract/config/type_rules.json`——原通过状态依赖 test_manager 先执行时的全局规则泄漏（用例间状态耦合），转正后必须自持；
- **certificate_extractor**：12/18 失败转 `xfail(strict=False)` 挂账；**实测其 6 例现行通过（XPASS）**——解冻分诊时这 6 例大概率可直接转正。

## 三、A 方案剩余项（后续继续）

- certificate_extractor 18 例解冻分诊（其中 6 例 XPASS 大概率免修），前置=hewj 定义现行契约；
- parse_students 全角括号缺陷的裁决与修复（若 hewj 认定属缺陷且批准解冻单点）。

## 四、certificate 契约文档（2026-08-24 补）

hewj 在场模式下完成现行行为契约核查——**12 例失败为单一根因**（测试匿名子类缺
description/judgment_text 两行类属性，撞上 __init__ fail-fast 校验），无任何抽取逻辑/
解析/schema 行为分歧。全文见《设计/2026-08-24_certificate_extractor契约文档.md》
（含 §9 hewj 签认区），签认后 15 分钟内可修复转正。
