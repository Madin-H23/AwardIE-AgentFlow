# 阶段二 · 加固（Harden）实施文档

| 项 | 内容 |
| --- | --- |
| **阶段目标** | 补齐健壮性短板：错误处理、超时重试、性能缓存、审核链路完整性（SRS 8.2 阶段二） |
| **执行周期** | 2026-08-17（延续阶段一当日，三批共 9 分支） |
| **状态** | ✅ **完成（15/15 项闭环）**——tag `stage2-harden` 已打（commit `7fd9b6f`） |
| **验证基线** | **131 用例全绿**（security×6 + unit×10 文件）；全量回归含顺序污染修复 |

---

## 1. 任务矩阵（对照 SRS 8.2 阶段二任务表）

| 任务 | 对应问题 | 状态 | 落点 |
| --- | --- | --- | --- |
| LLM 注入 timeout/max_retries | P1-6 | ✅ | llm_adapter(30s/3次) + 抽取 provider(退避重试) |
| Token 上限 | P1-6 附 | ⏸ 转阶段三 | 单请求 max_tokens/输入截断（AI 层 §8，登记 T23 同批） |
| Agent 工作流单例缓存 | P1-5 | ✅ | get_default 双检锁 + reset_default 热更新，chat.py 切换 |
| 全局 errorhandler | P2-8/T4 | ✅ | AppError 契约 + 统一包装{trace_id,code,message,data} + Retry-After |
| health 接口真实校验 | 4.3 | ✅ | db 探活 + 熔断状态 + status 聚合（不拨测外部免费用） |
| 所有角色首登强制改密 | P1-2 | ✅ | 三表 needs_password_change 列；登录全角色拦截；admin 4 处重置改随机强密码+标记（**T22 完成，默认密码引用清零**） |
| awards 补索引 + 修 with conn 泄漏 | P1-7/P1-2 | ✅ | 迁移 §1.1 五索引 + award.py 四处 closing（阶段一已完成，本阶段复验） |
| 审核流程原子性 | P1-8 | ✅ | 软归档前移为并发防重闸 + 入库失败 unarchive 回滚补偿（完全单事务待阶段三 ORM） |
| LLM/OCR 熔断器 + 降级 | P1-10 | ✅ | CircuitBreaker 三态；LLM+OCR 双维度；SSE/health 感知熔断态 |
| 文件存储定期清理 | P1-9 | ✅ | scripts/cleanup_files.py（孤儿+180 天未提交，默认 dry-run，cron 说明） |
| 审核留痕骨架 + 接线 | P1-13 | ✅ | AuditLogger + 4 动作接线（提交/AI 快照/通过/入库）+ 驳回(动作7) |
| 重开审核驳回路由 | FR-APPROVE-07 | ✅ | teacher/admin 双 API + **审核页驳回按钮 UI（T1 全闭环）** |
| approve 软归档 | 8.6.4 | ✅ | archive 条件更新保留 AI 结论；submissions 过滤 |
| （CR 追加）幂等键 | T3 | ✅ | SQLite 共享表 + @idempotent，批量审核防双击 |
| （CR 追加）SSE 流式端点 | P2-4 | ✅ | 事件协议 open→source→delta→done|error；连接限流≤2；delta 真增量转 T23 |
| （CR 追加）Embedding 超时重试 | P2-21 | ✅ | timeout=30 + tenacity 退避 3 次 |
| （CR 追加）OCR token 缓存 + timeout | P1-20 | ✅ | access_token 进程内缓存 + 识别/token 请求 timeout=30 |

## 2. 阶段二关键决策与偏差

| 项 | 说明 |
| --- | --- |
| 原子事务取舍 | approve 完全单事务需 Manager 连接级重构，随阶段三 ORM 单会话落地；本阶段以"防重闸+补偿"消除主要不一致面（并发重复入库/失败半成品），诚实标注 |
| SSE delta 边界 | 当前先发完整结果；逐 token 增量依赖 workflow 流式化（LangGraph astream），登记 T23 |
| Redis 暂缓 | T2 限流共享存储单机可容忍（memory://），阶段四容器化时随 Redis 一并引入 |
| 测试隔离教训 ×3 | 注册表单例跨用例污染（circuit_breaker→SSE）、子类槽陷阱（workflow 单例）、存量测试表结构同步（三表加列波及）——均即抓即修 |

## 3. 遗留移交（阶段三/四）

| # | 项 | 时点 |
| --- | --- | --- |
| M1 | Token 上限（max_tokens/输入截断）+ T23 workflow 真流式 | 阶段三 |
| M2 | T2 Redis 限流共享存储 + 进度存储 | 阶段四 |
| M3 | 审核完全单事务（ORM 单会话） | 阶段三 |
| M4 | review_logs 冻结、users 三表合并、全表缓存去除、SSE 前端打字机 | 阶段三 |

## 4. 结论

阶段二 15/15 项闭环：韧性四件套（熔断/超时/单例/幂等）+ 审核链路完整性（驳回+软归档+留痕+防重闸）+ 安全配套（强制改密/health）就位；测试资产 124 → **131 用例**。**tag `stage2-harden` 已打，阶段二收官**，进入阶段三（重构：users 合并/ORM/去缓存/SSE 真流式）。
