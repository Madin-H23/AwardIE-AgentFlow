# 阶段四 · 工程化（Engineering）实施文档

| 项 | 内容 |
| --- | --- |
| **阶段目标** | 建立可持续交付能力：容器化、CI、监控、备份（SRS 8.2 阶段四） |
| **执行周期** | 2026-08-18（两批） |
| **完成日期** | 2026-08-18 |
| **状态** | ✅ **完成**——tag `stage4-engineering` 已打。**四阶段全线收官** |
| **验证基线** | **196 用例全绿** |

## 1. 任务矩阵（对照 SRS 8.2 阶段四）

| 任务 | 对应 | 状态 | 落点 |
| --- | --- | --- | --- |
| Dockerfile + docker-compose | P2-9 | ✅ | slim+CPU 依赖+healthcheck；**app-api/app-sse 双实例+Redis+scheduler+nginx**（CR-4/5 落地） |
| CI 流水线 | P2-9/CR-3 | ✅ | ci.yml：lint+静态门禁（禁裸 connect）→ test（**cov-fail-under=70**）→ pip-audit → docker build |
| 业务指标监控 | 4.7 | ✅ | `metrics.py`：上传/审核动作/LLM 调用/熔断 gauge/review_cycle_hours（价值验证）/audit best-effort 可观测 + `/api/metrics` 暴露 + 未装库静默降级 |
| 备份脚本 | 4.8/§5 | ✅ | `scripts/backup.py`：三库+chroma+files+.env，integrity_check+3 表对账，30 天滚动 |
| 进度存储改 Redis | P3-4 | 🔶 就绪未切 | compose 已含 Redis；切换留上线时（memory:// 单容器可用） |
| 依赖锁+安全扫描 | P1-24 | ✅ 扫描/🔶 锁 | CI pip-audit 已接；锁文件（pip-compile）上线时生成 |
| 死代码清理 | D5 | 🔶 部分 | 死路由已改造（admin reject）；AchievementManager 等残留随二期 |
| CSS 收敛 | P3-1 | ⛔ 移交二期 | 涉及全站视觉回归 |

## 2. 上线前待办（9.5 Checklist 核对，部署时逐项勾选）

- [ ] 生产 .env 正式 Key + ocr_runtime 无误禁记录
- [ ] flask.env=production（已默认）+ SECRET_KEY 强随机（.env 已换）
- [ ] 种子数据（84 白名单 + 默认管理员）
- [ ] WAL 三契约验证（foreign_keys=1/journal_mode=wal/busy_timeout）
- [ ] 依赖锁文件生成 + pip-audit 0 高危
- [ ] 备份演练（scripts/backup.py 跑一次 + 恢复演练）
- [ ] 回滚方案确认（8.4）

## 3. 结论

阶段四两批收官：**容器化双实例拓扑 + CI 质量门禁 + 监控可观测 + 备份对账**全部就位。**四阶段全线完成**：P0×10 清零、P1 闭环 12 条、P2 闭环 9 条、测试 0→**196 用例**、四份阶段实施文档 + 四个 tag 构成完整演进档案。
