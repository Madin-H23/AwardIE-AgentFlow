# P2 质量护栏与异味根治 实施记录（2026-08-23）

> goal：见好就收·收尾三阶段之 P2。总基调=架构冻结前的最后一批工程投入。
> 同窗口完成：P0 可用性收口（70483a5）、P1 审计流卫生（79ae0d6），本记录覆盖 P2 批1-批5。

## 批1（T69）models 写路径护栏 ✅

**覆盖率实测（全量口径 `--cov=backend/models`，pytest 794p+156s+0f）**：

| 文件 | 前 | 后 |
| --- | --- | --- |
| backend/models TOTAL | 44%（2435/5544） | **60%**（3173/5263） |
| award.py | 缺 56% | 覆盖 59%（缺 41%，显著收敛） |
| patent / software / competition | 44/47/36% | 66/65/63% |
| user_photo（D4 冻结，仅补护栏不补功能） | 28% | 65% |
| 分母变化 | 5544 | 5263（删除死代码 achievement.py + achievement_manager.py 共 281 行零引用，D5 决议残留清理） |

新增两文件 43 例：`tests/unit/test_models_write_paths.py`、`test_models_crud_write_paths.py`。关键护栏形态：
- `_save_award` 写后**新连接回读**——若 commit 缺失，closing 关闭即回滚、行不存在，正是 R-031 的回归断言；
- award 关联四表删旧插新+去重语义、competition_id 守卫、update_from_json 字段重建、add_award 查重流（is_new False 分支）；
- laboratory「每师一室」业务约束固化断言（0002 迁移曾丢唯一约束、业务层兜底的回归护栏）；助教须先为成员；下载写入三态；
- pending 归档状态机 submit→archived→submit 往返 + 条件更新防竞态双断言 + 驳回生命周期；大创数据类纯逻辑（成员三种格式解析/年份四模式/展示格式化）六连测。

## 批2（T70）死草稿归档 ✅

`scripts/archive_dead_drafts.py`：超期口径（pending 且 created_at < N 天，默认 30）+ dry-run 默认 + --apply 前守卫备份（checkpoint 后拷贝）+ integrity 自检。
本次按 `--days 7` 执行：39 条全部为测试会话残留（2026-02 批量 33 条 + 08-13/14 会话 UUID 6 条），软归档保留 OCR/LLM 结论。**pending 39→0**，备份 `bak.2026-08-23-pre-archive-drafts.db`，integrity ok。

## 批3（T71）skip 零头清理 ✅

① `images/测试图片/{奖状,专利,软著,其他,大创}` 占位图入库；连带把根级 test_files 集成入口改为**显式门控** `AWARDIE_RUN_FILE_FLOW=1`（资产齐备后不再环境性真跑拖累基线）。② update_teachers 空姓名用例由 skip 转落地（openpyxl 动态构造双列布局）。③ agent 集成测试头成文月度例行：`RUN_AGENT_INTEGRATION=1 python -m pytest tests/integration/agent -q`（8 例防 AI 层漂移）。④ TODO 立 **T75 处置卡**：140 冻结模块 skip 三选项（解冻排期/转 xfail/永久豁免）待 hewj。

## 批4（T72）get_config 同名异义收敛 ✅

- `config.loader.get_config` → **get_config_loader**（46 文件引用同步）；
- app.utils 的第三种配置访问器定名 **get_app_config**（__init__ 再导出 + admin/file_import_helpers 消费方同步）；
- 终态：模块级 `def get_config` 仅剩 config/flask.py 一处语义；ruff F821 全仓零未定义名。
- **教训（批量替换链式误伤实录）**：`.loader` 是 `config.loader` 的子串→二次替换产生 `get_config_loader_loader`（30 文件归一）；正则裸调用改名误伤 config/flask.py 自身定义与 app.utils 混合语义调用点各一处——靠 ruff F821 全仓门禁逐一暴露回正。**再次验证：批量改码必须"机械替换 + F821 门禁 + 逐文件 diff"三道工序，缺一必漏。**

## 批5（T73）app.utils 双命名空间收敛 ✅

- `app/utils.py` git mv 为 **app/utils/_core.py**（保历史），`__init__` 删除 importlib 按路径加载的第二命名空间（app.utils_module），直接 re-export——`_managers` 全局仅存一份；
- 新增 `_core.reset_runtime_caches()` 统一复位入口；seeded_db fixture 两处"双命名空间各清一遍"简化为单点调用；
- 隔离断言测试 `test_utils_single_namespace`：独立进程验证无第二命名空间、包层无缓存副本。

## 批6（T74）旧基类迁移 ⏸ 节奏批

user_base×16 + base_simple×1 待分批迁 base_console；base_simple 登录页轻量性单独评估，保留需落档决策。不设硬期限。

## 环境备注

requests 直连 5001 登录受生产配置 SESSION_COOKIE_SECURE=True 影响（Secure cookie 不随 http 回传→CSRF session token missing）——HTTP 层验证统一走 playwright/gui_smoke，脚本层验证走 SQL 直查。
