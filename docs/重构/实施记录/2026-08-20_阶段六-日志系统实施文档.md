# 阶段六·日志系统实施文档（L1 数据层批次）

| 项目 | 内容 |
| --- | --- |
| **文档版本** | v1.0（L1 批次） |
| **编写日期** | 2026-08-20 |
| **依据** | 《2026-08-19_日志系统设计.md》§3 数据层 / §9.2 实施批次 |
| **对应 TODO** | T38（L1 数据层）、T27（接入点矩阵——本批完成 4 类） |
| **提交** | `cc91a8a` |

## 1. 任务矩阵

| 任务 | 状态 | 说明 |
| --- | --- | --- |
| 迁移 0006（system_event_log + 4 索引） | ✅ | 类型按 G8（枚举 VARCHAR(20)/(10)、trace_id VARCHAR(64)、长文本 TEXT）；编号顺延（0004/0005 已用于 teacher FK/failed_logins——撞号修复见 `9f2d910`） |
| ORM 模型 backend/orm/system_event_log.py | ✅ | SQLAlchemy 2.0 风格，注册 env.py 元数据 |
| SystemEventLogger | ✅ | 复用 AuditLogger 模式：进程级类方法/独立连接/append-only/失败仅 warning；PII 脱敏（身份证/手机号/学号 9 位）；from_exception 自动填 source/stack |
| 接入点矩阵（4/8 类） | ✅ | breaker 翻转（open+恢复 closed）、errorhandler（AppError error + 未预期 500 critical）、auth 锁定触发、system 启动事件 |
| 接入点矩阵（4/8 类待接） | ⏸ L2+ | ocr（**冻结模块不改**——经调用方包装接入）、llm（agent/llm_adapter 可改）、upload、db |
| conftest 全局隔离 | ✅ | autouse fixture 把 SystemEventLogger._db_path 指临时库（接入点遍布 breaker/errorhandler/auth，防测试污染真实库——测试环境自持 R2） |
| 单测 | ✅ | 15 用例（写入/类别级别校验/PII 5 例/不阻塞 2 例/from_exception/建表兜底/接入点 2 例） |

## 2. 验证

- 15 L1 单测全过；全量回归 **335 用例全绿**（320→335）；ruff 门禁过
- 迁移 0006 测试库验证（表+4 索引+foreign_key_check 空）→ 真实库已应用（备份 `bak.2026-08-20-pre-0006.db`）
- Alembic 链：0001→0006 完整

## 3. 偏差与教训（L1 实测）

| # | 教训 | 详情 |
| --- | --- | --- |
| 1 | **`errorhandler(Exception)` 必须放行 HTTPException** | 兜底 500 handler 会拦截 CSRFProtect 的 400 / IDOR 的 403/404（HTTPException 子类）致安全测试 5 例失败——加 `isinstance(e, HTTPException): return e` 放行后恢复 |
| 2 | **FK→users 的表测试库必须先建 users 表且插行** | system_event_log.operator_id REFERENCES users(id)：空 users 表写入即 FK 违反（失败被吞表现为 log 返回 False，难排查）——fixture 补 USERS_DDL + 种子 user（R8 纪律再现） |
| 3 | **学号正则须按真实格式** | 本校学号 21 开头 9 位（212306413），非 20 开头——`(?<!\d)(2\d{7})\d(?!\d)` |
| 4 | **被吞失败的排查手段** | SystemEventLogger 返回 False 时先查库表结构（FK/缺表）而非逻辑——失败已被 warning 吞掉，表面无异常 |

## 4. L2 查询层（2026-08-20 完成，`8e16f09`）

| 任务 | 状态 | 说明 |
| --- | --- | --- |
| trace_id 全链路（T26） | ✅ | `before_request` 生成/透传（X-Trace-Id 优先）→ `g.trace_id`；run.py `TraceIdFilter` 注入 `[tid:xxx]`；LogFileReader 双格式解析（新含 tid / 旧兼容）；**修复存量缺陷**：`app._current_trace_id` 原为属性从未赋值（errorhandler 响应恒 None），改为函数统一从 g 读取 |
| LogFileReader | ✅ | tail（倒读高效）/ search（关键词·级别·时间）/ stream（seek 增量生成器，供 L4 SSE）；非日志行丢弃 |
| LogQueryService | ✅ | 三源查询（audit/system_event/review，各分页+过滤）+ `query_all` 跨源时间合并（review 字段异构大不并入默认视图——设计 §4.1 的 query_all 语义收窄，偏差已记） |
| MetricsSnapshot | ✅ | collect() 读 REGISTRY 全指标；archive() 快照写 system_event_log（category='system'），供 L6 定时任务 |
| 单测 | ✅ | 12 用例；全量回归 **347 全绿**（335→347） |

**L2 教训**：① 测试断言时间排序勿依赖真实时钟——SQLite `CURRENT_TIMESTAMP` 为 UTC，fixture 用固定过去日期（否则跨时区/时刻抖动）；② `parse_line` 非日志行应返回 None 而非 raw dict（混入结果破坏 tail 断言语义）。

## 5. L3 分析层（2026-08-20 完成，`1953b40`）

| 任务 | 状态 | 说明 |
| --- | --- | --- |
| LogAnalyzer | ✅ | 7 类聚合：动作分布（日期过滤）/错误趋势（按日分组）/审核瓶颈（submit 态等待时长，julianday 计算）/活跃用户 TopN/AI 健康（熔断+LLM 成功率）/留痕健康/每日摘要 |
| AlertEngine | ✅ | A001 OCR 失败率>30%、A002 积压>20（超 48h）、A003 熔断 Open、A004 留痕失败率>1%、A005 认证失败 10 次/5min、A006 DB 错误；evaluate + archive_alerts（[alert] 前缀归档）+ get_recent_alerts；**缺表容错**（老库无 system_event_log 时规则跳过不崩） |
| PlanGenerator | ✅ | from_alert（规则→优先级/类别映射）/generate（优先级排序）/transition 状态机（非法跳转抛 ValueError 不静默）/daily_report；**状态不持久化**（设计开放问题 Q1 选 B 纯内存，L6 重估——诚实偏差） |
| 单测 | ✅ | 16 用例；全量回归 **363 全绿**（347→363） |

**L3 教训**：① **UTC 窗口二次踩坑**——system_event_log.created_at 是 SQLite CURRENT_TIMESTAMP（UTC），时间窗口过滤必须用 `utcnow()`；而 failed_logins.updated_at 是 login_guard 本地时间——**同一库两表时间基准不同**，过滤窗口须按表区分（已固化到代码注释）；② analyzer 的 execute 拼接 WHERE 后忘传 params（测试当场抓出）；③ 告警/统计查询对缺表必须容错（老库演进期）。

## 6. L4 路由层（2026-08-20 完成，`d353fde`）

| 任务 | 状态 | 说明 |
| --- | --- | --- |
| admin_log 蓝图 | ✅ | 19 接口：四源查询（audit/system/review/app+tail）+ 分析 5（actions/errors/bottleneck/activity/ai-health）+ metrics/daily-report + 告警当前/历史 + 计划列表/acknowledge/resolve；HTML 页面留 L5 |
| SSE 流 | ✅ | source=app（文件增量）/audit/system（DB id 轮询）；open→log→ping(30s)；每管理员并发 ≤2 超限 429；进程内计数（多 worker 统一随 R-014 Redis 迁移） |
| 鉴权修复 | ✅ | `require_role_api_json` 的 is_api 判定补 `/admin/api/` 前缀——原判定漏掉致未登录 302 而非 401（实测踩坑；全量回归确认无破坏面） |
| 单测 | ✅ | 13 用例（权限 3/查询 4/分析 2/告警计划 2/SSE 2）；全量回归 **376 全绿**（363→376） |

**L4 重大事故（R-027）**：推送时暴露——工作区被外部切至 `feat/m2-route-dedup` 分支，此前所有 `git push -q origin main && echo PUSHED` 实为静默 no-op（推的是本地 main，无新提交），**阶段五~六 37 个提交滞留本地分支约两天**。已 `--ff-only` 合并推送（`32f8691..d353fde`），工作零丢失；教训与验证步骤固化为 R-027。

## 7. L5 前端（2026-08-20 完成，`13bd08d`，方案见《2026-08-20_前端重构方案.md》）

| 任务 | 状态 | 说明 |
| --- | --- | --- |
| Phase 1 地基 | ✅ | `base_console.html`（控制台基类：240px 深色侧边栏 include 现有 sidebar/顶栏/块对齐 base.html）+ `console_tokens.css`（CSS 变量双主题 + severity 五级阶 + `.console-shell` 作用域隔离旧页面零影响）+ `console_theme.js`（15 行 vanilla，localStorage+系统偏好，**与 Bootstrap `data-bs-theme` 双轨同步**——DeepSeek 方案的 Tailwind/Alpine 改道落地） |
| Phase 2 组件 | ✅ | `_metric_cards.html`（vital 宏：呼吸圆点/脉动数字，**生命体征带签名元素**）+ `_filter_bar.html`（筛选栏宏） |
| Phase 3 L5 页 | ✅ | `admin/logs/console_logs.html` 三 Tab + `admin_logs.js`（三源分页/行式日志流[severity 左竖条]/行展开 JSON 详情/六 ECharts 读 token 随主题重绘/SSE EventSource 重连提示/计划 acknowledge-resolve）+ `/admin/logs` 路由 + 侧边栏「日志管理」 |
| Phase 4 验证 | ✅ | playwright 冒烟全通：三 Tab/系统事件 12 条（启动事件）/六图 canvas 全渲染/亮暗切换 `#f4f5f7↔#141414`/SSE"实时流已连接"/计划空态；377 全绿 + lint |

**L5 教训**：① JS 手写 URL 三元表达式漏 else 分支两处（node --check 当场抓出——**手写 JS 应先过 node --check 再冒烟**）；② `.gitignore` 的 `logs/` 规则误伤 `app/templates/admin/logs/`（改根锚定 `/logs/`）；③ **端口残留旧进程多绑定**（3 个进程同听 5001，请求随机路由旧代码致 404 假象）——冒烟前 `netstat` 查监听数，杀净再启。

## 8. T9 批量迁移（2026-08-20 完成，`469d739`）

原计划交接 DeepSeek-V4-Flash 的批量迁移由本会话直接执行（用户指示"继续开工T9"）。**54 个 admin 页全部换壳为控制台新体系**，base.html 体系在 admin 侧清零。

| 任务 | 状态 | 说明 |
| --- | --- | --- |
| 换壳范围 | ✅ | `app/templates/admin/` 55 页中 54 页（`logs/console_logs.html` 已是新体系）；12 个 tab 片段（achievements/awards/templates/data_export）与动态 fragment（`/admin/api/achievements/*`）全链路落于 `.console-shell` 内，tokens 作用域生效无需逐片段改类 |
| 迁移模式 | ✅ | 原位改造：extends 切 `base_console` + 新增 `topbar_title`；card→c-panel / card-body→p-3 / card-header→panel-head / card-footer→panel-foot（后两者为本次在 `console_tokens.css` 新增组件类）；按钮静态映射（长模式优先）：主操作→c-btn-primary、次要→c-btn、行内→c-text-btn（danger/ok 色阶）、实心 danger/success 整块操作保留（Bootstrap 暗黑自动适配）；规则固化于方案文档 §7.2 供后续新页复用 |
| 验证 | ✅ | 17 个主页面 GET 200 + console-shell 结构 + 无 Jinja 错误；5 个动态 fragment 200 + c-panel；暗黑切换 shell/panel/文字变量全对位（`#f4f5f7↔#141414`、`#1f1f1f`、85% 白字）；ruff E9/F63/F7/F82 通过 |
| 提交 | ✅ | `469d739` 推送并 `git ls-remote` 核实远程 ref 一致（R-027 纪律） |

**T9 教训**：① 迁移前先扫体积（file_import/results 70 个 card、awards/edit 20 个）识别大页，避免逐页手改——脚本化机械替换 + 逐文件 diff 审查是正确节奏（51 页一次跑完，残畄变体 `card-header py-2` 等带附加类模式用通配规则二轮清理）；② 页面内嵌 `<style>` 的 `card-header/.card-body` 选择器在类改名后成死代码，注释保留（无匹配元素）比删除安全；③ 全量回归需与 CI 显式清单对齐——默认 `pytest tests/` 会收进写真实 DB/依赖外部服务的存量测试（19 例失败与本次无关，HEAD 同样失败，R-026/R-028 同源待专项，见方案文档 §7.3）。

## 9. L6 收尾（2026-08-20 完成，`bc4d684`）

| 任务 | 状态 | 说明 |
| --- | --- | --- |
| 行动计划持久化 | ✅ | 取消纯内存（Q1 落库）：`action_plans` 表（幂等建表 IF NOT EXISTS，老库演进容错）；`PlanGenerator.generate` 合并持久化计划（已有状态/时间保留、新告警插 open）、`transition` 状态机校验后写库、`load` 读取；admin_log plan API 无需改即获持久化 |
| metrics 定时归档 | ✅ | `backend/utils/log_scheduler.py` daemon 线程（create_app 非 TESTING 启动）：每 5 分钟 `MetricsSnapshot.archive()` 写 system_event_log |
| 每日报告 | ✅ | 每日 09:00 窗口 daily_report 摘要留痕 system_event_log（推送通道后续可接）；`daily-report` API 供拉取 |
| 容量控制 | ✅ | 每日清理 90 天前 system_event_log + 已 resolved 计划；ignored 计划 7 天重评估置 open |
| 测试 | ✅ | `tests/unit/test_plan_persist.py` 6 例（落库/跨调用持久/非法跳转/resolve 链/缺表容错/ignored 重开）；相关 63 用例 + ruff |
| 真实库冒烟 | ✅ | plan API 200 + `action_plans` 表建 + 计划 0（当前无告警） |

**L6 教训**：①轻量定时不引 APScheduler——daemon + 防重（last_daily 标记）+ 任务内 try（循环存活）；②持久化表 `IF NOT EXISTS` 幂等建（老库演进同 0006 原则）；③scheduler 仅非 TESTING 启动，防测试环境后台线程写真实库。

## 10. 阶段六总结

L1-L6 收官（`cc91a8a`→…→`13bd08d` 前端→`bc4d684` L6）：数据层→查询层→分析层→路由层→前端→L6 收尾全链路闭环。遗留登记待办：`tab2YearFilter` 模板缺陷（存量 19 例）、daily_report 推送通道（邮件）、AI 助手页分端风格（T44）。
