# 阶段一 · 止血（Stop-the-Bleeding）实施文档

| 项 | 内容 |
| --- | --- |
| **阶段目标** | 修复阻断性与高危安全漏洞，恢复核心功能可用（SRS 8.2 阶段一） |
| **执行周期** | 2026-08-17（单日，四批共 16 分支 27 commit） |
| **状态** | ✅ **完成（12/12 项闭环，v1.1 定稿）**——tag `stage1-stopbleed` 已打（commit `e730583`） |
| **过程日志** | 批次明细已并入本文档 §1/§3（原批次日志与本文档重复，2026-08-17 合并删除） |
| **验证基线** | **84 用例全绿**（tests/security×6 + tests/unit×5）；数据库断言全过（fk 空/integrity ok/重复 0）；应用级冒烟通过（首页/登录 200、无 token POST 400、受保护页 302） |

## 0. 定稿补遗（v1.1，L1/L2 收尾批）

| 项 | 落点 |
| --- | --- |
| **L1 CSRF（P1-1）** ✅ | CSRFProtect 全局 + `csrf.js` 一处覆盖全站（fetch/XHR 自动注入头 + 40 模板 POST 表单动态补 hidden）+ 三套基础模板（base/user_base/**base_simple 登录页**）注入 meta token；6 用例 + 全站冒烟 |
| **L2 IDOR（P1-4）** ✅ | 新端点 `GET /files/achievements/<id>`：按 ID 查库（杜绝路径猜解）+ 归属校验（非本人非 admin → 403）+ 穿越兜底 + 强制 attachment；旧路径式接口标记废弃（一个迭代后切换移除）；4 用例 |
| **热修 1 例** | 迁移 §1.5 的 version 列炸毁 `PendingAchievement(**data)` 解包（pending 管理器/首页初始化即崩）——**由本批 CSRF 冒烟暴露的存量炸弹**，dataclass 补 `version:int=1` 即修；根因=首批迁移只做了 DB 断言未做应用级冒烟，**教训入纪律：DB 变更后必须起真实 app 冒烟** |
| **代码缺陷 1 例** | 新端点 `except Exception` 吞掉 `abort(403)` 变 404——测试首抓即修（HTTPException 分层抛出） |

---

## 1. 任务矩阵（对照 SRS 8.2 阶段一任务表逐项）

| SRS 任务 | 对应问题 | 状态 | 落点与证据 |
| --- | --- | --- | --- |
| 修复 OCR secret key 注入 + 重置运行态 | P0-1 | ✅ | 运行态禁用记录清除 + loader 键名兼容；venv 实测 baidu 恢复首位高精度引擎（v1.7 落档） |
| ~~核实并修正 LLM 模型名~~ | ~~P0-2~~ | ✅ 撤销 | `deepseek-v4-flash` 经实际核实为官方模型名，原判断有误（v1.7 落档） |
| 关闭生产 debug/reloader，默认 env 改 production | P0-3 | ✅ | `run.py::_should_enable_debug` 双条件门控 + settings env=production；7 用例 |
| 抽取公共连接工厂（外键/WAL/busy_timeout） | P0-4/5/7 | ✅ | `backend/utils/db_connection.py`；接入 student/teacher/pending 三核心 Manager（**其余 10 个 Manager 移交阶段二**）；主库三契约实测生效；5 用例 |
| 清理已产生的 4 条孤儿数据 | P0-4 | ✅ | 迁移执行日清除（lab 三表复合主键，语义条件删除命中）；`fk_check` 空 |
| 移除默认密码 + 强随机 SECRET_KEY | P1-2/P1-3 | ✅ | 兜底两处删除 + 29 位空 hash 教师随机初始密码（清单线下分发）；弱 key 生产拒启 + .env 换强随机；18 用例 |
| 引入 Flask-WTF 全局 CSRF | P1-1 | ✅ | v1.1 补齐（见 §0-L1）：CSRFProtect 全局 + csrf.js 一站覆盖（fetch/XHR/40 表单）+ 三基底 meta 注入；6 用例 |
| 文件访问加归属校验 | P1-4 | ✅ | v1.1 补齐（见 §0-L2）：`/files/achievements/<id>` 按 ID 查库 + 归属 403 + attachment；旧路径接口废弃过渡；4 用例 |
| （超出任务表的先行完成） | P0-8/9/10, P1-23, FR-INIT | ✅ | 竖图旋转删除(3用例)/awards 唯一护栏+5索引/上传三重校验+公开路由登录墙(6用例)/2GB→100MB/种子体系(84白名单+默认管理员,5用例)——**均为 P0 级或 Must 级提前闭环** |
| （配套）迁移脚本 §1 执行 | P1-11/12/13/15 前置 | ✅ | 关联表重建 PK/FK/CASCADE、audit_log 建表、version 列、admin bug 20 行；备份 10.3MB 未动用 |

## 2. 阶段一额外产出（按用户指令先行落地，原属阶段二）

| 项 | 对应 | 说明 |
| --- | --- | --- |
| 密码强度策略 | P2-28 | 普通>8位/管理员≥12/四类≥3种/禁5位键盘连续（正反序）；25 用例 |
| AuditLogger 留痕骨架 | P1-13 主体 | append-only/不阻塞/三动作接线（提交/AI 快照/通过+入库）；5 用例 |

## 3. 关键偏差与教训（全程如实记录）

1. 管道吞退出码致一次"未绿先提交"，当日修正并改为显式 RC 判断
2. 测试隔离缺陷（dotenv 收集期注入 FLASK_ENV）——测试环境自持原则确立
3. 存量测试债 5 例（T21）基线即挂，与本阶段无关，不冒领不掩盖
4. `granted_role` 词汇统一因中文展示依赖推迟阶段三（配套映射）
5. 三次测试自身返工 → 确立"优先零 mock 等价测法"原则

## 4. 遗留移交（转阶段二）

| # | 遗留项 | 原因与计划 |
| --- | --- | --- |
| L1 | ~~CSRF 全局防护~~ **已于 v1.1 补齐** | 见 §0 |
| L2 | ~~IDOR 文件归属校验~~ **已于 v1.1 补齐** | 见 §0 |
| L3 | 其余 10 个 Manager 接入连接工厂 | P0-4/5/7 全量化收尾（机械替换，低风险） |
| L4 | admin.py 4 处默认密码重置改造（T22）+ 旧路径式文件接口移除（一个迭代后） | 与 UI 文案耦合，阶段二 |

## 5. 结论

阶段一十二项任务**全部闭环**（v1.1 补齐 L1/L2），P0 十条**全部清零**，P1 前四条（1/2/3/4）闭环；核心链路（上传→抽取→AI→审核→入库）在安全基线就位后可用；测试资产从 0 → **84 用例**。**tag `stage1-stopbleed` 已打，阶段一正式收官**，L3/L4 及加固项转阶段二。
