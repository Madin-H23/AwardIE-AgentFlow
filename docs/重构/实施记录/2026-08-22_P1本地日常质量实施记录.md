# P1 本地日常质量实施记录（2026-08-22）

> 覆盖率提升（T24）+ review_logs 停写（T57/M4）+ GUI 冒烟回归常态化。
> 验收：全量 `pytest -p no:langsmith tests/ --import-mode=importlib` =
> **627 passed + 150 skipped + 0 failed**；覆盖率 **TOTAL 35%**（口径 `--cov=app --cov=backend`，
> 26575 语句）；gui_smoke 当前 HEAD **17/17 全绿**。

## 1. T24 覆盖率提升

- **基线实测修正**：goal 引用的"12.85%"是旧口径（`--cov=app --cov=backend/utils`，2026-08-18）。
  本轮按完整口径 `--cov=app --cov=backend` 实测基线即 **34%**（A 阶段大量新测试的自然成果），
  终验 **35%**（615→627 用例）——≥20% 目标达成。
- **新增 tests/unit/test_core_routes_coverage.py**（8 例）：三角色登录态核心列表页冒烟
  （admin dashboard/achievements/logs、teacher review/achievements、student 提交/成果页）
  + 未登录重定向守卫 + 越权访问拦截（teacher/student 访问 admin 页 → 302/403）。
  针对 teacher.py/student.py 两个最大路由文件（原覆盖 8%/12%）。
- **两个环境坑（已修并固化注释）**：
  ① fixture 必须用 `config.flask.get_config`（Flask Config 类）；误用 `config.loader.get_config`
  （ConfigLoader 实例）时 `from_object` 取不到 SECRET_KEY → 会话接口 open_session 返回 None
  （报"Session backend did not open a session"）且 CSRF 抛 "secret key required" → 全请求 500；
  ② 生产配置 `SESSION_COOKIE_SECURE=True` 使 test_client(http) 拿不到会话 cookie，fixture 显式置 False。

## 2. T57 review_logs 停写切换（M4）

- **前提核查**：achievement_audit_log 已覆盖全部决策类动作
  （提交1/AI审核2/通过6/驳回7/入库8/改字段9/撤回10/放弃11/删除12；动作5 复核查看无决策价值暂缓）。
- **改造**：`ReviewLogManager.create_log` 改为 no-op（保留签名兼容唯一调用点
  `review_service._log_review_action` 的 try/except 结构，返回值本就未被使用），调用仅记 info 日志；
  历史数据经 query_logs/get_recent_logs 只读路径继续可查，`/admin/logs` 展示不受影响
  （gui_smoke 留痕核对步骤实测通过）。附带收益：此前每次审核动作的
  "NOT NULL constraint failed: review_logs.submitter_id" ERROR 噪音日志消失。
- **测试 tests/unit/test_review_logs_freeze.py**（3 例）：create_log 后 review_logs 行数不变；
  同一动作周期 audit_log +1 而 review_logs 不变（audit 接管实证）；历史行只读可查。
- 相关回归（audit_logger/audit_complete/reject_archive/timeline/frontend_timeline 等）40 例全绿。

## 3. GUI 冒烟回归常态化（约定）

**核心链路改动前后运行一次三端冒烟，作为合并前的固定回归动作：**

```bash
# 服务运行中（PORT=5001）执行：
D:/venvs/awardie/Scripts/python.exe scripts/gui_smoke.py
# 预期输出：冒烟结果: 17/17 通过（自带数据清理，可重复执行）
```

适用时机：改动登录/上传识别/审核入库/留痕/AI 助手/日志查询任一链路的前后；
连续两次全绿视为稳定（本次 P1 基线：当前 HEAD 单次 17/17 全绿，留档于本文）。

## 4. CI 与台账

- ci.yml 清单追加 test_core_routes_coverage.py / test_review_logs_freeze.py，
  YAML safe_load + 续行链校验通过，本地等价跑绿（20 passed 含迁移/pending 用例）。
- TODO 索引：T24 标记阶段达标（35%）、T57 标记完成（M4 停写落地）。