# P0 本地数据资产保护实施记录（2026-08-22）

> 背景：本地使用场景下，主库是唯一数据资产。R-031（_save_award 缺 commit）修复前
> 成果入库从未落库——197 行历史成果仅存在于 `database/bak.2026-08-21-pre-0010.db`。

## 1. 历史 awards 恢复（T62）

- 脚本：`scripts/restore_awards_history.py`（默认 dry-run，--apply 执行；幂等可重复运行）。
- 流程：列集预检（0009 态 vs 0011 态五表列名逐一比对一致）→ guard 备份
  （bak.pre-restore-*.db）→ 单事务逐行 INSERT（id 已存在跳过 / image_hash 冲突跳过并计数）
  → 关联表仅搬已并入 award 且 (award_id, 对方id) 未存在的行 → sqlite_sequence.awards
  提升至 max(id)（防自增复用）→ integrity_check + foreign_key_check 自检。
- 结果：**awards 3 → 198**（并入 195，hash 冲突跳过 2）；award_student_winners +189、
  award_teacher_winners +23、award_related_students +33、award_supervisors +0；
  integrity=ok、fk 空、seq=1281≥max(id)=1280；幂等复跑"将并入=0"验证通过。
- 数据健康：198 条全部有竞赛关联；年份分布 2025×172 / 2024×16 / 2023×5 / 2022×5。

## 2. 每日自动备份（T63）

- 主保障：**log_scheduler 每日 09:00 窗口挂 `_daily_backup()`**——subprocess 调
  `scripts/backup.py`（三库+chroma+files+.env，30 天滚动+WAL checkpoint+对账自检），
  服务常开即每日备份；失败仅 warning 不影响其余每日任务。
- 实测：手动触发 run_daily → 新备份 `backups/20260822_145819` 生成且
  "对账 3 表计数一致"；回归测试 test_run_daily_triggers_backup 断言每日窗口必调备份。
- OS 计划任务（schtasks 与 PowerShell Register-ScheduledTask 各建过
  `AwardIE-DailyBackup` 每日 02:00）：本机安全策略拦截计划任务启动 python
  （LastTaskResult=1，最小 python 探针同样失败），任务定义已保留，环境放行后即生效，
  属可选增强；backup.py 已自带 logs/backup.log 文件日志供核对。

## 3. 验收对照

| goal 要求 | 实据 |
|---|---|
| awards=恢复前+并入数 | 3+195=198（脚本报告） |
| 4 关联表同步并入 | +189/+23/+33/+0（脚本报告） |
| integrity/fk | ok / 空 |
| seq≥max(id) | 1281≥1280 |
| 计划任务存在+运行对账绿 | 任务已注册（本机策略拦截执行）；对账绿由应用内每日备份实测达成（145819） |
| 恢复前后备份 | guard bak.pre-restore-20260822_144410.db + 定时备份多份 |
| TODO 回写 | T62/T63 已登记完成 |
