请帮我编写自动化测试程序，用于验证我的代码是否正确。

我编写了“docs/测试/核心业务测试方案.md”，请根据这个方案，设计自动化测试程序（tests/test_main_services.py），用于快速回归。

具体如下：

1. 测试程序要输出测试结果，以html文件格式导出保存到`tests/reports/核心业务测试结果.html` 

2. 请记录日志，将出现的错误记录到`tests/bugs/bug_{date}_{time}.html`

3. 一旦出现错误，就要停止测试，按照如下过程重复：修复代码-->重启测试-->检查测试结果-->修复代码....

4. 测试报告，可以参考其他测试tests/下程序的做法，生成测试报告，并且调用浏览器打开报告。

   请阅读我的测试方案，参考`docs/文件管理/文件管理设计.md` 了解主要的业务过程；`docs/文件管理/测试报告_20260125_093654.html`,`docs/extract/抽取框架设计.md`,`docs/extract/大创抽取器设计.md` `docs/extract/LLM模块设计.md`

5. 遇到故障后的修复，需要遵循下面的原则：
   *  绝对不允许考虑后向兼容的问题，一定都以当前最新的设计为准，如果意外可以出异常 
   *  不要使用任何降级方案，不行就是不行一定要坚决  。如果并没有很优雅的解决方法，请停下来向我解释并且询问我的意见
   * extract,ocr,模版,llm都是重构后的模块，已经经过测试不能轻易修改的。如果需要改动，请停下来询问我的意见
   * 一定要考虑到模块的独立性和松耦合，不要直接使用模块内的变量进行复杂的逻辑操作，如果确实需要，可以考虑增加类的方法，最好也是停下来询问我。
   *  对于不使用的代码一定要坚决删除，宁可现在去查问题，不要留下垃圾。
   * 一定要考虑到数据的一致性。比如不能直接操作数据库，这样会导致内存和数据库的不一致。
   *  务必不要硬编码，如果确实需要请告诉我。
   * 注意编码的优雅，不要特别大篇幅的函数。
   * 对于遇到现有代码中的上述问题，请记录下来，并且在工作结束的时候提醒我查看。

