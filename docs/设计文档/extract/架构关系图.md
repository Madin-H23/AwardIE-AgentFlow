# 抽取模块架构关系图

本文档通过图表清晰展示抽取框架、OCR、各个抽取器、LLM之间的关系，以及OCR和LLM内部的简易关系。

## 一、整体架构关系图

### 1.1 核心组件关系

```mermaid
graph TB
    subgraph "用户调用层"
        User[用户/业务代码]
    end
    
    subgraph "抽取框架层"
        Framework[ExtractFramework<br/>统一入口与调度器]
    end
    
    subgraph "服务层"
        OCR[OCREngine<br/>OCR识别引擎]
        LLM[LLMEngine<br/>LLM调用引擎]
    end
    
    subgraph "抽取器层"
        Award[AwardExtractor<br/>奖状抽取器]
        Innovation[InnovationExtractor<br/>大创抽取器]
        Patent[PatentExtractor<br/>专利抽取器]
        Software[SoftwareExtractor<br/>软著抽取器]
        Other[OtherExtractor<br/>默认抽取器]
    end
    
    subgraph "数据层"
        OCRCache[(OCR缓存数据库)]
        LLMCache[(LLM缓存数据库)]
    end
    
    User -->|extract文件路径| Framework
    Framework -->|注册管理| Award
    Framework -->|注册管理| Innovation
    Framework -->|注册管理| Patent
    Framework -->|注册管理| Software
    Framework -->|内置| Other
    
    Framework -->|调用| OCR
    Framework -->|调用| LLM
    Framework -->|传递上下文| Award
    Framework -->|传递上下文| Innovation
    Framework -->|传递上下文| Patent
    Framework -->|传递上下文| Software
    
    Award -->|可选调用| OCR
    Award -->|可选调用| LLM
    Innovation -->|读取Excel| Framework
    Patent -->|可选调用| OCR
    Patent -->|可选调用| LLM
    Software -->|可选调用| OCR
    Software -->|可选调用| LLM
    
    OCR -->|读写| OCRCache
    LLM -->|读写| LLMCache
    
    style Framework fill:#e1f5ff
    style OCR fill:#fff4e1
    style LLM fill:#ffe1f5
    style Award fill:#e1ffe1
    style Innovation fill:#e1ffe1
    style Patent fill:#e1ffe1
    style Software fill:#e1ffe1
    style Other fill:#f0f0f0
```

### 1.2 数据流向图

```mermaid
sequenceDiagram
    participant User as 用户/业务代码
    participant Framework as ExtractFramework
    participant OCR as OCREngine
    participant LLM as LLMEngine
    participant Extractor as 抽取器
    participant Cache as 缓存数据库
    
    User->>Framework: extract(file_path)
    
    Framework->>Framework: 检查文件存在性
    Framework->>Framework: 匹配扩展名
    
    alt 图片类型文件
        Framework->>OCR: get_text(file_path, is_precise=False)
        OCR->>Cache: 查询OCR缓存
        alt 缓存命中
            Cache-->>OCR: 返回缓存文本
        else 缓存未命中
            OCR->>OCR: 执行OCR识别
            OCR->>Cache: 保存OCR结果
        end
        OCR-->>Framework: (ocr_text, cached)
        
        Framework->>Framework: 关键词匹配抽取器
        
        alt 多个抽取器命中
            Framework->>LLM: chat(选择抽取器提示词)
            LLM->>Cache: 查询LLM缓存
            alt 缓存命中
                Cache-->>LLM: 返回缓存响应
            else 缓存未命中
                LLM->>LLM: 调用LLM API
                LLM->>Cache: 保存LLM响应
            end
            LLM-->>Framework: (extractor_names, cached)
        end
        
        Framework->>Extractor: extract(ctx, ocr_text已提供)
    else 非图片类型文件
        Framework->>Extractor: extract(ctx, ocr_text=None)
    end
    
    alt 抽取器需要额外OCR
        Extractor->>OCR: get_text(file_path, is_precise=True)
        OCR-->>Extractor: (ocr_text, cached)
    end
    
    alt 抽取器需要LLM抽取
        Extractor->>LLM: chat(结构化抽取提示词)
        LLM->>Cache: 查询/保存缓存
        LLM-->>Extractor: (structured_data, cached)
    end
    
    Extractor->>Extractor: 内部验证
    Extractor-->>Framework: ExtractResult
    
    Framework->>Framework: 应用验证器（可选）
    Framework-->>User: ExtractResult
```

### 1.3 组件职责说明

| 组件 | 职责 | 关键方法 |
|------|------|---------|
| **ExtractFramework** | 统一入口，管理抽取器注册与调度 | `extract()`, `register()`, `_extract_image()`, `_extract_non_image()` |
| **OCREngine** | 图片/PDF文本识别，支持缓存和精度分级 | `get_text()`, `clear_cache()` |
| **LLMEngine** | LLM调用，支持缓存和多种Provider | `chat()`, `clear_cache()` |
| **AwardExtractor** | 奖状证书结构化抽取 | `extract()` |
| **InnovationExtractor** | Excel大创项目抽取 | `extract()` |
| **PatentExtractor** | 专利证书结构化抽取 | `extract()` |
| **SoftwareExtractor** | 软著证书结构化抽取 | `extract()` |
| **OtherExtractor** | 默认抽取器，处理无法匹配的文件 | `extract()` |

## 二、OCR内部关系图

### 2.1 OCR架构关系

```mermaid
graph TB
    subgraph "OCREngine层"
        OCREngine[OCREngine<br/>统一接口]
    end
    
    subgraph "Provider工厂层"
        Factory[ProviderFactory<br/>Provider工厂]
    end
    
    subgraph "OCR Provider层"
        Zhipu[ZhipuOCRProvider<br/>智谱OCR]
        Baidu[BaiduOCRProvider<br/>百度OCR]
        Rapid[RapidOCRProvider<br/>RapidOCR]
        Paddle[PaddleOCRProvider<br/>PaddleOCR]
    end
    
    subgraph "缓存层"
        CacheDB[CacheDB<br/>缓存数据库操作]
        OCRCache[(ocr_cache表)]
    end
    
    subgraph "工具层"
        Preprocess[图片预处理<br/>EXIF校正/旋转/压缩]
        PDF2Image[PDF转图片<br/>第一页转换]
    end
    
    OCREngine -->|创建| Factory
    OCREngine -->|使用| CacheDB
    OCREngine -->|调用| Preprocess
    OCREngine -->|调用| PDF2Image
    
    Factory -->|根据配置创建| Zhipu
    Factory -->|根据配置创建| Baidu
    Factory -->|根据配置创建| Rapid
    Factory -->|根据配置创建| Paddle
    
    OCREngine -->|根据is_precise选择| Zhipu
    OCREngine -->|根据is_precise选择| Baidu
    OCREngine -->|根据is_precise选择| Rapid
    OCREngine -->|根据is_precise选择| Paddle
    
    CacheDB -->|读写| OCRCache
    
    style OCREngine fill:#fff4e1
    style Factory fill:#e1f5ff
    style CacheDB fill:#ffe1f5
```

### 2.2 OCR执行流程

```mermaid
flowchart TD
    Start[get_text调用] --> CheckFile{文件是否存在?}
    CheckFile -->|否| Error[抛出异常]
    CheckFile -->|是| CheckType{是PDF?}
    
    CheckType -->|是| PDF2Img[PDF转第一页图片]
    PDF2Img --> CalcHash[计算文件哈希]
    CheckType -->|否| CalcHash
    
    CalcHash --> CheckCache{使用缓存?}
    CheckCache -->|否| SelectProvider[根据is_precise选择Provider]
    CheckCache -->|是| QueryCache[查询缓存数据库]
    
    QueryCache --> CacheHit{缓存命中?}
    CacheHit -->|是| CheckPrecision{缓存精度匹配?}
    CheckPrecision -->|完全匹配| ReturnCache[返回缓存结果]
    CheckPrecision -->|低精度缓存+需要高精度| SelectProvider
    CacheHit -->|否| SelectProvider
    
    SelectProvider --> Preprocess[图片预处理<br/>EXIF/旋转/压缩]
    Preprocess --> OCRCall[调用Provider.ocr_image]
    OCRCall --> SaveCache[保存到缓存]
    SaveCache --> Return[返回识别结果]
    ReturnCache --> Return
    
    style Start fill:#e1f5ff
    style Return fill:#e1ffe1
    style Error fill:#ffe1e1
```

### 2.3 OCR精度分级说明

| 精度级别 | 说明 | 适用场景 | Provider示例 |
|---------|------|---------|-------------|
| **高精度** (`is_precise=True`) | 使用大型模型，识别精度高，消耗更多资源 | 最终识别、重要文档 | zhipu, baidu |
| **低精度** (`is_precise=False`) | 使用轻量级模型，速度快，资源消耗低 | 快速筛选、初步识别 | rapid, paddle |

**缓存匹配策略：**
- 高精度缓存 + 需要高精度 → 直接返回缓存 ✅
- 低精度缓存 + 需要低精度 → 直接返回缓存 ✅
- 低精度缓存 + 需要高精度 → 调用高精度OCR并更新缓存 🔄
- 高精度缓存 + 需要低精度 → 直接返回缓存（精度更高可用）✅

## 三、LLM内部关系图

### 3.1 LLM架构关系

```mermaid
graph TB
    subgraph "LLMEngine层"
        LLMEngine[LLMEngine<br/>统一接口]
    end
    
    subgraph "Provider层"
        Provider[LLMProvider<br/>Provider管理器]
    end
    
    subgraph "具体Provider实现"
        ZhipuAPI[ZhipuProvider<br/>智谱API]
        KimiAPI[KimiProvider<br/>Kimi API]
        OllamaLocal[OllamaProvider<br/>本地Ollama]
        DeepSeekAPI[DeepSeekProvider<br/>DeepSeek API]
    end
    
    subgraph "缓存层"
        CacheDB[ExtractCacheDB<br/>缓存数据库操作]
        LLMCache[(extract_cache表)]
    end
    
    subgraph "配置层"
        Config[配置文件<br/>config/settings.json]
    end
    
    LLMEngine -->|使用| Provider
    LLMEngine -->|使用| CacheDB
    
    Provider -->|根据default_provider选择| ZhipuAPI
    Provider -->|根据default_provider选择| KimiAPI
    Provider -->|根据default_provider选择| OllamaLocal
    Provider -->|根据default_provider选择| DeepSeekAPI
    
    Config -->|读取配置| Provider
    Config -->|读取配置| LLMEngine
    
    CacheDB -->|读写| LLMCache
    
    style LLMEngine fill:#ffe1f5
    style Provider fill:#e1f5ff
    style CacheDB fill:#fff4e1
```

### 3.2 LLM执行流程

```mermaid
flowchart TD
    Start[chat调用] --> CheckCache{使用缓存?}
    CheckCache -->|否| CallLLM[直接调用LLM Provider]
    CheckCache -->|是| CalcHash[计算提示词哈希<br/>基于user消息content]
    
    CalcHash --> QueryCache[查询缓存数据库]
    QueryCache --> CacheHit{缓存命中?}
    
    CacheHit -->|是| ReturnCache[返回缓存响应]
    CacheHit -->|否| CallLLM
    
    CallLLM --> SelectProvider{选择Provider}
    SelectProvider -->|API类型| CallAPI[调用API Provider<br/>zhipu/kimi/deepseek]
    SelectProvider -->|本地类型| CallLocal[调用本地Provider<br/>ollama]
    
    CallAPI --> SaveCache[保存到缓存]
    CallLocal --> SaveCache
    SaveCache --> Return[返回LLM响应]
    ReturnCache --> Return
    
    style Start fill:#ffe1f5
    style Return fill:#e1ffe1
```

### 3.3 LLM Provider类型

| Provider类型 | 说明 | 配置字段 | 示例 |
|-------------|------|---------|------|
| **API类型** | 调用远程API服务 | `type: "api"` | zhipu, kimi, deepseek |
| **本地类型** | 调用本地模型服务 | `type: "local"` | ollama |

**缓存机制：**
- 缓存键：基于所有user消息content的SHA256哈希
- 相同提示词 → 相同哈希 → 命中缓存
- 支持独立控制：`use_cache` 参数

## 四、抽取器与框架交互关系

### 4.1 抽取器注册与调用

```mermaid
graph LR
    subgraph "框架初始化"
        Init[ExtractFramework.from_config_loader]
        Init --> CreateOCR[创建OCREngine]
        Init --> CreateLLM[创建LLMEngine]
    end
    
    subgraph "抽取器注册"
        Reg1[framework.register<br/>AwardExtractor]
        Reg2[framework.register<br/>InnovationExtractor]
        Reg3[framework.register<br/>PatentExtractor]
        Reg4[framework.register<br/>SoftwareExtractor]
    end
    
    subgraph "抽取流程"
        Extract[framework.extract]
        Extract --> MatchExt[匹配扩展名]
        MatchExt --> MatchKey[关键词匹配]
        MatchKey --> LLMSelect{需要LLM选择?}
        LLMSelect -->|是| LLMCall[调用LLM选择抽取器]
        LLMSelect -->|否| DirectCall[直接调用抽取器]
        LLMCall --> DirectCall
        DirectCall --> Validate[验证结果]
    end
    
    Init --> Reg1
    Init --> Reg2
    Init --> Reg3
    Init --> Reg4
    
    Reg1 --> Extract
    Reg2 --> Extract
    Reg3 --> Extract
    Reg4 --> Extract
```

### 4.2 抽取器基类接口

```mermaid
classDiagram
    class Extractor {
        <<abstract>>
        +str name
        +str description
        +List~str~ keywords
        +str judgment_text
        +List~str~ extensions
        +matches_extension(ext) bool
        +matches_keywords(text) bool
        +extract(ctx) ExtractResult*
    }
    
    class AwardExtractor {
        +extract(ctx) ExtractResult
        -_load_fields()
        -_build_prompt()
        -_parse_llm_response()
    }
    
    class InnovationExtractor {
        +extract(ctx) ExtractResult
        -_read_excel()
        -_parse_data()
    }
    
    class CertificateExtractor {
        <<abstract>>
        +extract(ctx) ExtractResult
        -_build_prompt()
    }
    
    class PatentExtractor {
        +extract(ctx) ExtractResult
    }
    
    class SoftwareExtractor {
        +extract(ctx) ExtractResult
    }
    
    class OtherExtractor {
        +extract(ctx) ExtractResult
    }
    
    Extractor <|-- AwardExtractor
    Extractor <|-- InnovationExtractor
    Extractor <|-- CertificateExtractor
    CertificateExtractor <|-- PatentExtractor
    CertificateExtractor <|-- SoftwareExtractor
    Extractor <|-- OtherExtractor
```

## 五、关键交互场景

### 5.1 图片文件抽取流程

```
1. 用户调用 framework.extract("award.jpg")
   ↓
2. 框架检查扩展名，匹配到 AwardExtractor
   ↓
3. 判断为图片类型，调用 OCR.get_text("award.jpg", is_precise=False)
   ↓
4. OCR查询缓存，未命中则调用Provider识别，保存缓存
   ↓
5. 框架使用OCR文本进行关键词匹配，命中AwardExtractor
   ↓
6. 框架调用 AwardExtractor.extract(ctx, ocr_text="...")
   ↓
7. AwardExtractor内部：
   - 检查关键词匹配
   - 调用 LLM.chat(结构化抽取提示词)
   - LLM查询缓存，未命中则调用API，保存缓存
   - 解析LLM响应为结构化数据
   - 内部验证
   ↓
8. 返回 ExtractResult
```

### 5.2 Excel文件抽取流程

```
1. 用户调用 framework.extract("innovation.xlsx")
   ↓
2. 框架检查扩展名，匹配到 InnovationExtractor
   ↓
3. 判断为非图片类型，直接调用 InnovationExtractor.extract(ctx, ocr_text=None)
   ↓
4. InnovationExtractor内部：
   - 读取Excel文件
   - 解析列名和数据
   - 内部验证
   ↓
5. 返回 ExtractResult
```

### 5.3 多抽取器竞争场景

```
1. 用户调用 framework.extract("certificate.jpg")
   ↓
2. 框架检查扩展名，匹配到 PatentExtractor 和 SoftwareExtractor
   ↓
3. 判断为图片类型，调用 OCR.get_text()
   ↓
4. 框架使用OCR文本进行关键词匹配，两个抽取器都命中
   ↓
5. 框架调用 LLM.chat(选择抽取器提示词)
   ↓
6. LLM返回 ["patent"]，框架选择 PatentExtractor
   ↓
7. 调用 PatentExtractor.extract(ctx, ocr_text="...")
   ↓
8. 返回 ExtractResult
```

## 六、总结

### 6.1 核心设计原则

1. **统一入口**：ExtractFramework 作为唯一入口，统一管理所有抽取器
2. **服务解耦**：OCR和LLM作为独立服务，通过上下文传递给抽取器
3. **缓存透明**：OCR和LLM的缓存机制对上层完全透明
4. **配置驱动**：所有Provider、精度级别、缓存策略都通过配置控制
5. **可扩展性**：通过注册机制轻松添加新的抽取器

### 6.2 关键数据流

```
文件路径 
  → ExtractFramework 
    → OCREngine (图片类型)
      → OCR文本
        → 关键词匹配
          → LLM选择抽取器 (多命中时)
            → 抽取器.extract()
              → LLMEngine (结构化抽取)
                → ExtractResult
```

### 6.3 性能优化策略

1. **扩展名过滤**：先按扩展名过滤，避免无关类型走OCR/LLM
2. **关键词过滤**：单命中直接派发，不调LLM；仅多命中时才调LLM选择
3. **精度分级**：快速筛选用低精度OCR，最终识别用高精度OCR
4. **缓存机制**：OCR和LLM都支持缓存，减少重复调用
5. **顺序调用**：抽取器按顺序调用，首个非other即返回

---

**文档版本**: 1.0  
**最后更新**: 2026-01-25  
**作者**: Claude Code
