# 无 GPU 服务器使用此文件：pip install -r requirements-cpu.txt
# 与 requirements.txt 相同，但 PaddleOCR 使用 CPU 版 PaddlePaddle（无需 CUDA）

# Web 框架
Flask>=3.0.0
Werkzeug>=3.0.0
gunicorn>=21.0.0

# 文档处理
PyMuPDF>=1.23.0
Pillow>=10.0.0
python-docx>=1.2.0
openpyxl>=3.1.0

# 测试框架
pytest>=7.4.0
pytest-html>=3.2.0

# 数据处理
pandas>=2.0.0
numpy>=1.24.0

# LLM 相关
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
openai>=1.0.0

# 云服务 SDK
bce-python-sdk>=0.9.0  # 百度云
aistudio-sdk>=0.3.0    # 百度千帆

# 本地模型支持（可选）
ollama>=0.1.0          # Ollama 本地 LLM 支持
rapidocr-onnxruntime  # RapidOCR 本地 OCR（快速筛选）

# PaddleOCR CPU 支持（无 NVIDIA GPU 时使用）
# 注意：必须使用 3.2.0，3.3.x 在 CPU 上有 OneDNN/PIR 回归会报 NotImplementedError
# 安装：pip install paddlepaddle==3.2.0 -i https://www.paddlepaddle.org.cn/packages/stable/cpu/
# 然后：pip install paddleocr（或 pip install -r requirements-cpu.txt）
paddlepaddle==3.2.0    # 固定 3.2.0，避免 3.3.x CPU 推理 NotImplementedError
paddleocr              # 从官方源安装完整版

# 工具库
requests>=2.32.0
python-dotenv>=1.0.0
pyyaml>=6.0.0
pydantic>=2.0.0

# PaddleOCR 额外依赖（通过 paddleocr[all] 自动安装）
# 包括：shapely, scikit-image, imgaug, lmdb, natsort
prometheus-client>=0.20.0
