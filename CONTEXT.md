# AwardIE-AgentFlow v2

v2 二次重构(Java 21 / Vue 3 / PostgreSQL 16 / Python gRPC Worker)的领域语言。承接 v1 全部业务规则(BR-1~BR-7),以作品导向收缩范围,按 Strangler Fig 按域绞杀 v1。

## Language

### 范围

**作品导向**:
v2 的首要目标是产出可答辩、可演示的全栈作品,而非全量复刻 v1。范围、里程碑、验收口径均由此推导。
_Avoid_: 全量复刻、功能对齐优先

**纵切面 (Vertical Slice)**:
登录→成果提交→AI 审核→入库+时间线→AI 问答→导出的一条贯通前后端与 AI 进程的核心链路,是 P0-P2 的真范围。
_Avoid_: 核心链路(泛称)、P0 范围(歧义)

**Tracer (Tracer Bullet)**:
纵切面的第一张可运行 ticket:以最短路径打通"登录→提交→AI 审核→入库+时间线"。住主仓,是真实实现的第一段。
_Avoid_: 探针(那是丢弃式的)

**探针 (Probe)**:
为验证纸面定不了的假设而写的 throwaway 代码。住 `prototype/` 分支,答案折进设计后即归档,永不合入主仓。
_Avoid_: Tracer、demo

**高频扩展队列**:
纵切面之外、按演示价值排队进 v2 的功能(admin 五类成果管理、个人资料、竞赛管理等)。排到则做,排不到自然落入长尾并存。

**长尾并存**:
定性为长尾的功能组(看板、实验室、门户、管理台归档/模板/日志子域)永不在 v2 复刻,继续由 v1 服务,按路径分流给 v1。
_Avoid_: 遗留系统(贬义)、待迁移(误导——它们不迁)

### 迁移

**域 (Domain)**:
绞杀的切分单位:一组同生共死的路由、页面与数据表。切换、补迁、验收都以域为粒度。

**窗口制切换**:
某域写路径从 v1 切到 v2 的机制:短暂冻结 v1 对应入口→增量补迁数据→v2 接管写。按域二值切换,不按流量比例。
_Avoid_: 灰度发布、百分比切流(1%/10%/50% 口径已废弃)

**双库影子比对**:
窗口切换前的一致性验证:同一读请求分别发给 v1(SQLite)与 v2(PG),逐字段比对响应;零差异才允许切换。
_Avoid_: 影子读(单库语境的旧机制名,已不适用)

**稳态收束**:
P3 的软目标:纵切面与高频扩展稳定后,评估 v1 是否转只读存档。见好就收、可随时停止,不算失败。
_Avoid_: v1 下线(硬目标口径,已废弃)
