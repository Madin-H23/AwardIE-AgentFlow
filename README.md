# AwardIE-AgentFlow

基于大模型的信息抽取与多智能体协作的奖状智能管理系统

## 系统简介

本系统是 AwardIE-AgentFlow——基于大模型的信息抽取(IE)与 Agent 工作流系统，用于管理学生竞赛活动、奖状记录以及相关统计信息。核心能力包括 OCR + LLM 智能抽取、RAG 知识库问答、多智能体协作审核。

## 快速开始

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 3. 启动系统

```bash
python run.py
```

系统启动后，访问：`http://localhost:5000`

## 登录方法

### 管理员登录

- **用户名**：`admin`
- **密码**：`Mayy123`
- **登录后**：进入管理员仪表板，可以访问所有管理功能
- **功能**：奖状管理、竞赛管理、人员管理、活动管理等

### 教师登录

- **用户名**：工号（例如：`02114818`）
- **密码**：`P@ss301`（默认密码）
- **登录后**：进入教师仪表板，可以查看自己的指导成果
- **功能**：查看个人设置、浏览活动、查看指导成果等

### 学生登录

- **用户名**：学号
- **密码**：`P@ss301`（默认密码）
- **登录后**：进入学生仪表板，可以查看自己的获奖记录
- **功能**：查看个人设置、浏览活动、查看获奖记录等
- **首次登录**：使用默认密码首次登录后，系统会强制要求修改密码

## 账号说明

1. **管理员账号**：
   - 管理员账号已独立，存储在 `admins` 表中
   - 默认管理员账号：`admin` / `Mayy123`
   - 所有教师账号现在都是普通教师角色

2. **教师账号**：
   - 使用工号作为用户名登录
   - 默认密码：`P@ss301`
   - 例如：`02114818`（马云莺）现在是普通教师账号
   - 管理员可在教师编辑页面重置密码

3. **学生账号**：
   - 使用学号作为用户名登录
   - 默认密码：`P@ss301`
   - 首次登录后需要修改密码
   - 管理员可在学生编辑页面重置密码

## 测试

### 运行测试套件

```bash
# 运行所有测试
python -m pytest tests/ -v

# 运行特定模块测试
python -m pytest tests/unit/ -v      # 单元测试
python -m pytest tests/integration/ -v  # 集成测试
python -m pytest tests/ui/ -v        # UI测试

# 运行测试并生成HTML报告
python -m pytest tests/ --html=tests/reports/report.html --self-contained-html
```

### 测试覆盖

- **单元测试**: 检测规则模块、回归测试
- **集成测试**: 文档抽取API、检测规则API
- **UI测试**: doc_rec后台管理界面、主应用界面

更多测试信息请参考 [guide.md](guide.md)。

## 注意事项

1. **首次登录**：建议首次登录后修改密码
2. **管理员账号**：管理员账号独立于教师表，存储在 `admins` 表中
3. **密码安全**：生产环境部署前，请提醒用户修改默认密码
4. **账号激活**：确保账号的 `user_activated` 字段为 1（已激活）

## 技术支持

如有问题，请联系系统管理员。

